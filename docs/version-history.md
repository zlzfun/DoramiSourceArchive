# 波次史(version history,L1 活跃)

> 本文是 `CLAUDE.md` `## Versioning` 节的详细记录,2026-09-15 整体迁出(CLAUDE.md 超 150k 字符上限,
> 当时该节 64k 占全文四成)。**CLAUDE.md 的「波次年表」每波只留一行「现状是什么 / 落在哪」;
> 每波的设计取舍、目检返修、codex 检视返修与被否方案写在这里。新波次的详细记录追加于本文,CLAUDE.md 年表只加一行。**
> 版本机制(`src/version.py` 单一事实来源、`scripts/release.sh` 发版、两条部署脚本按 tag)见 CLAUDE.md *Versioning* 与
> [`release-process.md`](./release-process.md)。
>
> 记录形态:第一节是 3.0–3.43 的连续叙事,第二节是补丁节点,第三节起是 v3.44 之后逐波一段——**按记录先后而非版本号排序**,
> 标「版本号以实际发版为准」的段落是 tag 即发布(2026-09-14)后合入、尚未或刚刚发版的波次。
> 阅读提示:据本文查「怎么来的」;判断现状以 CLAUDE.md 与代码为准。

## 3.0–3.43 纪元叙事

纪元:1.x 采集归档原型 → 2.x 读者分发平台(PM2 app 名 dorami-backend-v2 即此纪元遗痕)→ 3.0.0 静默仪器全站重构收官 → 3.6–3.10 阅读器 Folo 化五波(四带式/容器模型/全站轨语言/设置柜归一/发现页) → 3.11 图床波(媒体库 + 媒体热点图) → 3.12 X 社交波(X API v2 采集 + 社交媒体第三容器) → 3.13 登录电影化(信号星座三维摄像机背景 + 居中强调→左移→登录卡浮现两幕开场,LoginScreen 重构)→ 3.14 登录跃迁波(登录成功「跃迁」终幕:星点由慢到快拉光轨 + 闪光跨切换 + App 三段式抵达幕 hold/settle/fading——主题色幕布兼慢网延迟护罩,认证成功即预热 runtime;质感波:登录页字形子集本地打包(思源宋 600/700 + Plex Mono,~10KB)、卡片 3D 倾斜、失败震镜+摇头、密码隐私幕、星等色温、摩擦帧率归一化、gloss 快扫慢歇)→ 3.15 Docker 部署重构(双容器 compose 取代裸机 PM2 路径:镜像内前端构建 + uv.lock 锁定依赖 + CPU torch 瘦身 + Playwright 环境固化;3.15.1 生产观察期满后删除 deploy.sh/ecosystem.config.js/ini [nginx] 节,PM2 正式退役;3.15.2 配置面收敛——删 requirements.txt 重复源/三个死配置节 [wechat][xiaoluban][image_host]/local.ini 备胎链,宿主 TLS 边缘配置入库为 docker/edge-nginx.conf.example)→ 3.16 RAG 依赖分离(sentence-transformers 入 rag-embedded extra,默认镜像 -1GB)→ 3.17 RAG 服务化(chroma server + TEI 双容器 compose profile ["rag"] 按需启用,vector_storage 嵌入/重排 provider 化、显式向量单路径;真 chroma 集成冒烟验证)→ 3.18 互通波(读者反馈收件箱 + 管理员公告横幅 + 远程内容同步——设计 `docs/engage-sync-wave-plan.md`)→ 3.19 管理员平权波(多管理员——任意数量/互相设立,唯一护栏「末位活跃管理员保护」;管理员双界面——轨底隐藏切换钮进出阅读器 + `default_surface` 登录落地偏好;管理写操作审计 `admin_audit_logs` + 运维页审计面板;移除 ini [auth] 账户种子,空库首启自动生成根管理员 admin/admin;3.19.1 铺开前打磨——媒体热点图年份切换轨[GitHub 式,后端 heatmap 加 year 参数与 years 列表]+卡片间距修复、登录跃迁收尾改全局隐出/隐入[弃白光闪切,auth-warp-flash/app-arrive 均平色幕布]、轨底 RailUserFlyout 用户滑出菜单[头像 hover 滑出工具钮+头像变关机退出钮]、轨 tooltip 提速 350→120ms、视图轨「发现」与内容容器间加分隔线、图表微调[排行柱厚 14px/订阅=青绿·收藏=琥珀呼应收藏星;通用亮色调色板两度试验后拍板回归主题色版];3.19.2 设置柜整备波——柜体 640→576 收纵向空置+设置行距 12→14、账户页按钮统一 32px M 档[pw-grid 拉伸 self-center 阻断/sett-seg 34→32]、内容门控改按「当前界面」[`readerSurface` 下沉:adminConsole=admin×管理台才见 MCP 开关/全库令牌文案/数据同步组,admin 切阅读器后设置柜与读者同观感;反馈页仅读者账号;MCP 取数范围说明拆管理版/读者版]、数据同步页重设计[双列虚线卡→单列整幅实线卡,根治 datetime 截断;gzip 换 ledger-switch/file input 换投放区行]、远程同步定时任务[KV `remote_sync:schedule` 凭据只写不回显(沿 X token 范式,手动同步仍不落库)+ GET/POST /api/admin/remote-sync/schedule + APScheduler job `remote_sync` 热生效/增量自游标/防重叠,前端定时卡=开关+凭据+频率预设(每天/每6时/每时/自定义 cron)])→ 3.20–3.21 日报页与阅读器打磨/小特性波(近期日报分页懒加载、hash 语义化 #/mcp→#/brief、发现页选源升级、反馈入口提级、文章阅读量)→ 3.22 新读者引导波(轨底退出两击防呆[首点入 4 秒待确认态+Toast 提示,防好奇误退]+ 新账号预置精选订阅[`DEFAULT_SUBSCRIPTION_SOURCE_IDS` 扩至 5 源:日报/量子位/IT之家AI/Anthropic/OpenAI,播种即建未读水位]+ 阅读窗空态轻引导[文案简化为「选择一篇文章以开始阅读」+ 一行直达发现页的小字,新老用户通用;欢迎卡方案实现后被否决拆除——太啰嗦];3.22.1 退出提示文案统一「再次点击以退出登录」——待确认态头像已翻面成退出钮,不再提「头像」;3.22.2 登录演出品牌豁免——不随 OS「减少动画」降级[Windows 关动画效果即报 prefers-reduced-motion,此前登录页只剩静止一帧;LoginScreen prefersReducedMotion() 恒 false + 全局 1ms 钳制规则放行 .auth-stage/.app-arrive/.app-arriving 子树,工作区仍全面尊重该设置];3.22.3 默认字母头像——单个大写首字母+按字母着色;3.22.4 文章质量校对波——行内空白保真+站点头尾噪声清剿[推广前 55 源全量抽查:_inline_markdown 保空白递归修全站性空格丢失、粗斜体/sr-only/零宽字符/播放器控件等下沉共享转换层,OpenAI/Apple MLR/DeepMind/Reddit/qwen/claude_blog/anthropic/qbitai/aiera 等站点级头尾噪声清剿;**修法与新源抽查清单沉淀于 docs/sources/node_audit_playbook.md《Content-quality proofing》节,补充新源必读**];3.22.5 源命名核对波——标题去渠道机制词+简介去采集话术+取名注册表现取[13 处改名:新智元 Website→新智元、OpenCode GitHub Releases→OpenCode Releases、Z.ai New Released→Z.ai 更新公告(文章标题模板同步归一 site_name 前缀)等;口径拍板:通用体裁词用中文(博客/新闻/公告)、页面专名英文保留(Changelog/Release Notes/Daily Papers)、分隔符统一「·」;发现页直出的 description 全改内容式名词短语;`vector_storage.friendly_source_name()` 成为取名运行时单点(现役源从 registry 现取,`SOURCE_FRIENDLY_NAMES` 只留日报特殊源+下线归档源兜底,名单同步 DECOMMISSIONED_FETCHER_IDS);**命名规范沉淀于 node_audit_playbook《Source naming & description》节;改名后存量 chunk 头部仍旧名,在意则 reindex-all**];3.22.6 新账号首屏竞态修复——预置订阅播种移至登录点[原播种只挂 GET /api/reader/sources,与阅读器首屏并行发出的文章列表(subscribed_scope=only)/未读计数请求竞态,新账号表现为空列表+积压未读被误判成「载入 N 篇新文章」提示;登录响应先于前端挂载,竞态根除;仅 runtime 读者面开启时播种、失败不阻断登录,reader 端点原调用保留兜底])→ 3.23 源扩容 wave4(Microsoft AI 模型、Artificial Analysis、Meta AI 博客、Kimi Research、MiniMax Research、Import AI、Arena 排行榜更新;全批 incubating,全文 RSS/精确正文圈选/单页 Changelog 拆条与真实正文质检)→ 3.24 源可见性波(管理面隐藏节点:KV 隐藏名单 + /api/admin/source-visibility + 节点管理检视器开关,观察期故障/文章缺陷不改代码即时下架;内容交付全量排除、目录呈现分订阅态——已订阅者源栏保留「暂不可用」灰显条目可退订可等恢复,未订阅者完全不可见;顺修 v3.19 双界面遗留缺陷:admin 停留阅读器界面刷新白屏[mountedTabs 刷新恢复未挂载 reader 面板])→ 3.25 分享波(读者把一篇内容发给同事,此前只能截屏。**站内深链**`#/reader/a/{id}`——纯前端 URL 不落库,收到的人登录后直达该篇[App 在导航体系 replace 掉 hash 前就地取走 id,admin 带深链进来一律落阅读器,压过 default_surface 与 sessionStorage 的界面记忆];**公开链接**`#/s/{token}`——免登录只读页 `ArticleShareRecord` + `GET /api/public/share/{token}`,有效期 7/30 天或永久、可随时撤销、管理面总闸 `public_share_enabled` 关闭即让已发出的链接全部失效;公开页在 main.jsx 于 App 之外分流[访客没有账号,不能走登录门]。顺带两修:**日报标题重复**[正文首行 `# 🤖 哆啦美 AI 资讯日报 · 日期` 与阅读窗 reader-pane-title 重复,渲染侧 `stripDuplicateLeadingHeading` 剥离——归档正文与导出契约原样不动,存量日报同修];**节点分类改信息角色单轴**[原「公司 → 板块」两级谱系只登记 17 家公司,现役 44 个 owner 里 27 家掉进「其他来源」兜底占半个目录,同家节点还因 owner 写法不同而分家;`groupBySection`/`SECTIONS` 退役为 `groupByRole`,管理面与阅读器源栏、发现页共用官方/媒体/个人/榜单同一套词汇,公司降为组内排序键(用 company.key 拉丁标识排,避免中文名浮到组首);顺修三处误分类:Import AI[expert_newsletter,Jack Clark 个人执笔]媒体→个人、Arena 排行榜更新[ai_benchmark_platform]官方→榜单、Artificial Analysis[ai_benchmark_analysis]媒体→榜单;节点行上的角色胶囊撤除,组头已承担])→ 3.26 凭据整合波(外部第三方凭据统一保管层 `services/credentials.py`——LLM api_key/X bearer_token 声明式命名空间注册,resolve/field_sources/save/sanitize 四能力统一「env/ini 基线 + KV 覆盖」解析序与只写不回显契约,KV key 沿用存量零迁移;remote_sync 定时凭据 blob 复用 secret 合并语义;LLM 配置响应补 field_sources 收敛与 X 的漂移、脱敏统一 ••••尾四位;前端 SecretField 统一凭据输入件三处就地复用;为内网 SSO 工号密码与微信/微博/小红书/知乎平台凭据预留登记位)→ 3.27 凭据整合台(设置柜 → 管理组新增「凭据」区[拍板 B,弃运维子页方案]——LLM/X API 两张单点编辑卡[SecretField+field_sources 来源徽记+stamp 状态章+测试连通/保存]+「其它凭据」只读回指[定时同步凭据柜内一跳数据同步、GitHub Token 部署级 env-only,`GET /api/admin/credentials` 只回存在性;标题瘦身:说明性副文案撤除];消费页表单撤除换 model-chip 回指:运维 AI 开关板撤 LLM 编辑器、内容页 X 区撤凭据表单[配额 KPI 留守],日报页/运维两 chip 均 openSettings('credentials') 深链;口径=「设置=写入口,运维=观测面」;两版样页 docs/design/dorami-credentials-{quiet,cabinet}.html)→ 3.28 右键菜单波(Folo 式右键上下文菜单——阅读器三落点[条目列文章卡/源栏源条目/社交推文卡]右键弹自绘菜单,动作全为既有函数的并联入口:读态/收藏首组→链接类次组→重操作/破坏性末组[跨落点肌肉记忆],admin 源菜单多「管理」分组[在读者面隐藏/恢复,与节点检视器同 API];`hooks/useContextMenu` 让位判定[卡内**既有**选区/输入域放行浏览器默认菜单;macOS 右键 mousedown 默认行为会自动选词、contextmenu 时刻分不清主动拖选,故 mousedown(button2) 捕获阶段快照右键前选区,自动选词不让位且拦截后清除]+`components/ContextMenu` portal 化单例[fixed 快照定位实测夹取+贴下缘上翻、滚动 capture 一网打尽有 ref 无 ref 容器即关、复用 useModalA11y+方向键循环、disabled 不隐藏];被右键卡挂 `.is-ctx-anchor` wash 锚定态[刻意非 .is-on 选中语义,关菜单即除];`.ctx-menu` 类族入 index.css 暗色 --sh-3 列表;首帧不可 visibility:hidden 遮测量——隐藏元素不可聚焦,useModalA11y 首焦会静默失败;样页 docs/design/dorami-context-menu-quiet.html)→ 3.29 移动端 H5 阅读器波 Wave1+2(手机浏览器/微信内直接打开即用的移动阅读器;小程序方案评估后弃选——request 合法域名须公网 HTTPS+备案、资讯聚合类目审核是硬约束,H5 内外网通吃零审核,小程序留作 H5 验证后的可选二期[推送/转发卡片是其唯一增量]。**Wave1 逻辑抽取**:ReaderTab 数据层整段抽入 `hooks/useReaderState.js`[源目录/订阅、未读体系、收藏、列表加载与无限滚动、正文与 AI 缓存、深链落地、右键/长按菜单 items 三份构建器],桌面只余四带 JSX+视图胶水[overlay 滚动条/latest-ref 行回调/右键弹层/品牌图回退],桌面行为零变化,ArticleRow/骨架屏改具名导出供移动壳复用;**Wave2 移动壳**:App.jsx 按 matchMedia(≤767px) **首帧一次性**分流 lazy `components/mobile/MobileReader`[不做拖窗热切换;登录门/runtime/主题/设置柜/Toast 管道全部共用,admin 手机上同落阅读器、管理台不下放],底部 4-Tab[文章/动态/社交/我的=视图轨的移动翻译,wash 块选中+文字标签]+正文 push 全屏页[衬线/进度线 scroll-timeline/速读卡/翻译/分享/上一下一篇,复用 .reader-pane-* 排印类]+源过滤抽屉[§4.1.2 选源是过滤器不占 Tab;聚合入口+角色分组照搬源栏语法]+**长按动作单**[`hooks/useLongPress` touch 计时+contextmenu 双路径互补——iOS 不派发 contextmenu、Android/桌面会,长按后吞合成 click;ActionSheet 的 items 与桌面右键完全同源同组序,跨端肌肉记忆;被长按卡同挂 .is-ctx-anchor]+我的页[承接 RailUserFlyout 全部职责,退出两击防呆改确认框——触屏 hover 展开会吞首击]+发现页整页层[低频目的地不占 Tab,从 我的/抽屉 进];`mobile.css` 全 .m-* 前缀经 index.css @import 引入、token 化亮暗自动翻转,viewport-fit=cover + env(safe-area-inset-*) 垫边;社交 Tab 本波复用 SocialFlow+CSS 收窄[动作钮常显补 iOS 长按不可达],Wave3 收口;收藏保持容器级过滤语义[我的页收藏入口=当前容器收藏];样页 docs/design/dorami-mobile-quiet.html)→ 3.30 检索扶正波(RAG 退役第一波,方案 **docs/rag-retirement-plan.md**;审视拍板:向量 RAG 从未在生产运行[0 向量化]、生产硬件跑不起推理栈、FTS5 已就绪且结构性零漂移,故取缔向量形态、检索改「LLM 计划检索 + FTS5」两段式。scope=subscription 问答改走 `services/reader_search.py`[规划(1 次轻量 LLM,中英关键词改写+日期窗+意图分流)→ FTS5 召回(订阅域∩日期窗,关键词并集)→ 选篇(1 次轻量 LLM,标题+引子挑 ≤8 篇;候选≤8 短路省调用)→ 全文注入;降级链自持:规划失败→原词 FTS→零命中→时序窗口,任何一级不 5xx;「日报即索引」:跨期盘点型问题在已订阅日报源时收敛检索日报正文;两次调用计费并入 ask];`assemble_reader_context` 收敛为 search_fetch 闭包注入(D11 形态保留);MCP `search_articles`/`get_rag_context` 同名换 FTS 芯[出入参兼容,distance 退役,**RAG 关不再等于 MCP 无搜索**];前端撤 MCP 工具「RAG 未启用」灰显与 bge-m3 硬编码、问答范围选项加 hint。RAG 开关自此无读者面语义)→ 3.31 退役清仓波(Wave B,与 Wave A 连做——跳过观察期的论证:向量层不构成新管线的回退路径[生产 0 向量化,从未运行]、生产硬件从未支持、删列零信息损失。向量子系统整体下架:删 chromadb 依赖与 rag-embedded extra[镜像构建改单清单,requirements-rag.txt 删除]、compose rag profile[chroma+TEI 双服务]与 WITH_RAG 构建分支、[rag]/[models] 配置节与 DORAMI_RAG_* 环境变量、/api/vector*·/api/vectorize*·/api/rag* 全部端点与 routers/vector.py·storage/impl/vector_storage.py·services/vector_reconcile.py 三模块、04:00 对账巡检与 auto_vectorize 抓取钩子、ArticleRecord 的 is_vectorized/index_status 两列[Alembic `f2c9d4e07a11`:SQLite 先删索引再 batch 删列,重建表会连带掉 FTS triggers 故收尾 ensure_fts 幂等补回;历史 index_status 回填迁移加列守卫兼容新库收养回放]、前端向量雷达页签/台账向量列与自动向量化开关/api.js 十函数/EmptyState·StatusBadge 孤儿原语/.vector-status-* 等死 CSS ~150 行/运维内容板「向量化率」KPI;friendly_source_name+SOURCE_FRIENDLY_NAMES 迁至 services/source_naming.py[8 模块引用改址];公开订阅检索端点 /api/public/subscriptions/{id}/vector/search 路径留存换 FTS 芯[与 MCP search_articles 同源];tests/rag/ 评测 harness 与四册向量测试删除。重新引入触发器与轻形态约束见方案 §4)→ 3.32 阅读面 AI 打磨波(拍板方案 A,样页 docs/design/dorami-ask-quiet.html。**问答面板重设计**:浮层 380→440,读者问句保留右缘 wash 胶囊、哆啦美回答**去框化**[无框全宽正文块+行首琥珀星标身份行,面板内 markdown 字阶降档],范围下拉换 composer 内 seg 胶囊[本文/我的订阅],空态建议问题[目检返修后定稿:居中无框引号轻文本行,题池每 5s 轮动/悬停暂停/点击填入],**阶段化等待态**[客户端 ask_id + 内存进度 dict + GET /api/reader/ai/ask/progress 轮询,检索管线 plan→search→select→answer 逐阶段上报;流式 SSE 挂 backlog 以此过渡];**引用联动**:上下文编号 [n] 即引用锚——build_numbered_context/build_sources_payload 同源同序[sources 补 id/source_name/publish_date],QA 提示词改行内 [n] 标记且禁自列清单,前端 [n]→引用 chip[ReaderMarkdown citations 扩展点,越界剥除]可点站内跳转[useReaderState.openArticleById——深链落地抽成可复用函数,引用跳转与 #/reader/a/{id} 同路]+末尾出处列表由 sources 确定性渲染[行内标记尽力而为,集中列表保底必有];**范围四档**:核心层扶正「文章名单→编号上下文+sources」,article=n1 特例[单篇也带出处]/articles=显式多篇≤12[后续多选问答/收藏集问答的原语]/subscription/all=检索圈定[all 走 feed_service.resolve_all_visible_source_ids 全库可见源,发现页预览同口径、隐藏源照旧排除],后端全量落地、前端仍只露两档;目检返修七项:输入法组字回车不误发/composer 聚焦压掉全局 textarea:focus-visible 矩形环/出处行无原文链接时保留等宽外链占位[右缘对齐]/点面板外即收[灯箱 portal 豁免]/FAB 星标与面板字头统一琥珀[撤暗色靛蓝覆盖]/标准态面板给显式高度 600[auto↔定值不可过渡,是尺寸切换生硬的根因]+开合软过冲曲线+轮次浮现动效;二轮返修:规划新增 **chat 意图分流**[闲聊/对话元问题(「你是谁」「我上一个问题是什么」)plan 后直返空上下文纯对话作答,不检索不落时序窗口]+**等待态改动态阶段清单**[只画实际发生的阶段,chat 型只见「理解问题→组织回答」两行;首行文案「规划检索」改「理解问题」;**阶段历史由服务端权威累积**(entry.stages 全量随 GET 返回)——search 阶段存活仅几毫秒,前端轮询采样必漏,靠采样拼历史曾把「没轮询到」误判成「无需检索资料」]、IME 判据加固为 composition 事件对+isComposing+keyCode 229 三重[Safari 确认回车在 compositionend 后到达,单判据漏网]、focus 外环压制移至文件尾未分层区[全局环未分层,@layer 内 outline:none 恒败——项目已知陷阱又一例]、建议问题降至 11.5px faint 且轮换放缓[8s 一换/淡出浮现各 ~0.5s+,建议是氛围不是通知];三轮返修(出处降噪):选篇提示词改**宁缺毋滥**[具体主题只选真正对题的,「沾边但不对题」明令不选——FTS 宽召回是设计内,松的是选篇]+前端出处列表只列**回答里真正引用过的 [n]**[保留原编号与行内 chip 对应,标签「本次回答引用」;通篇无标记才回退列全量并标「参考」];四轮返修(时效感知补课):此前只有检索规划层知道今天日期,选篇层无基准、**作答层既无今天日期也看不到各篇发布日期**——「最近一周」类问题可能把旧闻当新闻;现 build_numbered_context 块头带「来源名 | 发布日期」、QA/选篇 user prompt 均注入今天日期、QA 系统提示词加时效甄别原则;实测弱模型仍会把 7 月旧闻包装成「最近一周」,故再加**机械化诚实层**:降级阶梯改「窗口内零命中 → 先放宽日期窗保主题 → 再丢主题看最新」,且降级一旦改写检索语义就由检索层把事实以「(检索说明:…)」**确定性注入上下文头部**[窗口无命中版/主题无命中版两款文案,时效浏览型是本意不注说明]——诚实性不靠作答模型自觉对照日期;规划提示词的日期折算改硬性要求[「最近一周」类相对表达必须折算 date_gte];顺修 App.jsx **runtime 失败落锁**缺陷[fetchRuntimeInfo 失败时能力位以全 false 落地且 runtimeLoaded 闸门不再重试——dev 后端重启/热重载窗口期撞上一次,AI 入口与管理页签就"消失"到重登录为止,像配置丢失(DB 实际原样);以 account_role 缺失为哨兵每 3s 安静重拉自愈];移动壳问答下放留后续波)→ 3.33 AI 渐变身份色波(AI 家族统一**蓝→紫渐变身份色** `#3f7bf6→#8b52f4`[样页 docs/design/dorami-ai-identity-quiet.html 三轮迭代:琥珀/靛/青瓷三方案→全站渐变试探→收束为 AI 专属;线性中点≈现行靛,与品牌同族、与收藏琥珀色相分家,v3.32 的 FAB 琥珀方案退役、琥珀归还收藏专用]。触点六处:问答 FAB 星/面板字头星/回答身份行[星+「哆啦美」渐变小字头]/空态星/速读卡[渐变 wash 底+渐变字头]/译钮激活态[is-blue→**is-ai**:渐变 wash 底+渐变「译」字,桌面 .reader-pane-iconbtn 与移动 .m-iconbtn 同语;分享/搜索钮的 is-blue 不动]——**全站唯一的渐变=AI 触点,见渐变即 AI**;纪律:渐变只上身份标记,面板内操作件(发送钮/seg/引用 chip/建议问题)与全站主题保持 solid accent。基建:`components/AiGradientDefs.jsx` 文档级 SVG defs[#ai-grad 正向/#ai-grad-inv 反色底——墨底 FAB 亮色主题需亮端、暗色主题底翻浅需深端;stop-color 吃 CSS 变量随 [data-theme] 自动翻转;App 桌面/移动两渲染根各挂一份,同帧仅存其一]+ :root 令牌 `--ai-g1/g2(-inv)`/`--grad-ai`/`--grad-ai-wash`[暗色块端色翻转];图标渐变经 CSS `stroke: url(#ai-grad)`(lucide 描边图标),一律带 `var(--dorami-accent)` 兜底色;文字渐变共享类 `.ai-grad-text`(background-clip: text)。哆啦美速读打磨(目检拍板:否决拆句/句首符/未生成胶囊钮,原卡形态保留):生成中「⟳ 正在总结…」改**三行 shimmer 骨架**[.reader-ai-summary-skel 92/78/85% 错落行宽,复用全局 .skeleton 含暗色与 reduced-motion 降级,桌面/移动同步])→ 3.34 AI 链路优化波(日报与问答两链路的十项审视修复一次落地。**日报 reduce 改确定性渲染**:markdown 由 `render_brief_markdown` 从结构化条目直接排版[分节/来源行/总结/点评/附录格式忠实沿用原 reduce 契约],汇编段 LLM 只剩 `cross_day_dedup` 一次小 JSON 决策[对照近几天日报条目标题判 drop/「(接前报)」增量注,失败降级不查重、要求全删触发安全阀]——整篇长输出的截断/漏条/复制篡改风险根除,REDUCE_SYSTEM_PROMPT 保留仅作技能包风格契约、/api/daily-brief/pipeline 的 reduce_system_prompt 字段改回跨天查重词;日报顺修两处:map 失败条目不再以默认 3 分混入正选[降入「📎 其它收录」附录]、候选裁剪加观测[collect_candidates 回传扫描总数,last_run 记 candidates_scanned/used,被裁条目随游标跳过一事不再静默]。**检索质量**:FTS 召回引入 bm25 相关性主序[`fts_search_ranked` rowid→rank,日期倒序降为次序,两阶段取数免全量载正文——此前纯日期序会把对题旧文挤出候选池]+ 短于 trigram 下限的两字实体词(「豆包」)走标题 LIKE 补召回并集[此前静默失配]+ 检索规划带最近对话[`_history_text` 压缩注入,提示词加「结合上文还原追问再规划、追问不算闲聊」——此前每轮只看当前问句,「再展开讲讲第二点」类追问规划必失败]+ 检索档上下文预算按选中篇数摊分[单篇长文不再只喂开头 2000 字符]。**基建三件**:`[llm] aux_model` 辅助轻模型档[同端点第二模型名,凭据注册表全链+设置柜 LLM 卡输入框;检索规划/选篇、日报 map/聚类/跨天查重走 `for_aux()` 且不下发思考参数,作答/翻译仍主模型——主模型旗舰/思考档时轻调用不陪跑高延迟高成本]、`chat_completion` 支持共享连接[`client_session` 上下文管理器,日报 map 全段与翻译分段共用连接池免逐次 TLS 握手]、译文/速读缓存加正文指纹[sha256 旁键,重抓后失配即重生成,存量无指纹缓存沿用]。**读者面 AI 全局日 token 预算**:KV `ai_beta_daily_token_budget`[0=不限],`_require_reader_ai` 前置 429[只计 translate/ask/summarize,系统任务不占],与逐用户日调用限额互补护全站总成本[IM bot 代答渠道的放大器];/api/admin/ai-beta/global 扩展读写,运维 AI 面板总闸旁加「日预算」输入与今日已用读数)→ 3.35 日报权威与官源波(生产实证驱动:拉近 10 期日报+8 天素材逐条核对,官方源条目在头部名次仅 1/30、76% 条目挤在 7/8 整数分平分段[顺序实际由抓取顺序随机决定]、「官方迟到」论断被时序数据推翻[Grok 4.6/Gemini 3.7 Flash/GPT-5.6 官方全部与媒体同天入库,输在排序;真缺席的 Qwen3.8-27B/GLM-5.3 是**官源缺位**:Qwen 官博未发 27B 博文(一手=HF 模型卡+X)、智谱一手宣发迁至 z.ai/blog 而所盯 docs.z.ai 发布页停更于 GLM-5.2]。**权威机械层**[三处确定性代码,不靠 LLM 自觉]:`source_naming.source_role()` 后端角色镜像[与前端 sourceTaxonomy.sourceRoleOf 同判定序 个人→榜单→媒体→官方,scope 集合两侧须逐字同步;注册表查不到默认 media 不给官方待遇]→ BriefCandidate.source_role 穿透;①同事件代表权:dedup_clusters 簇内官方在分差 ≤1 内优先当代表[门限防官方一行推文压过媒体深度整理,官博自然赢官推],媒体退 extra_sources;②同分排序:select_top 改 `score+0.5×官方` 有界加成排序[整数分布下=同分段官方置顶且绝不跨分数段,官方外围产品照沉,score 本值不改];③跨天查重官方例外:官方条目被判 drop 机械降级为 followup「官方一手确认」[对照物只有标题分不清前报是官方还是转述,删官方=系统性压制一手信息],提示词同步注明+entries 带角色标。**日报缺陷四修**(上轮评估落地):同日重跑改**增量合并**[load_existing_brief_state 从 extensions.items 重建条目(items 补 id/source_id/source_role/followup_note 四键,历史日报按位对齐 included_article_ids 兜底)+merge_same_day 旧∪新去重→同事件聚类→有效分重排,不裁 top_n——旧行为整篇覆盖丢早间条目产残报;fetch_recent_brief_items 加 exclude_date 排除当日自身,此前今天的日报混进对照物把增量剔光]、跨天剔条**回补**[top_n+buffer(max(3,n//3)) 预选→查重→有效分裁回 top_n,回补条目天然过了跨天检查]、候选**两段式轻列取数**[游标重置/长停摆恢复不再全库正文进内存,顺删 seen_ids 死代码]、map 瞬时失败**串行重试一轮**[LLM 端点中段抖动不再把一批文章永久定格成附录裸标题]。**map 提示词**:0.5 步长小数分+厂商主次甄别锚点[头部厂商名号不加分:旗舰模型/核心 API 8-10,外围功能/客户端更新/企业案例/营销叙事压 4-6]。**新官源**:x_zai_org preset[@Zai_org,user_id 留空走 handle 解析 KV 缓存;智谱唯一可稳定采集的一手通道]+hf_qwen_models preset[Qwen 开源权重一手位];z.ai/blog 浏览器源与 x.ai/news 记 backlog;生产日报名单建议勾入 x_alibaba_qwen/x_deepseek_ai/x_openai/rss_deepmind_blog)→ 3.36 新账号 AI 默认开波(新增可配置开关「新账号默认开启 AI」:KV `ai_beta_new_user_default` 默认开,`create_user`/根管理员播种按它落逐账户 `ai_beta_enabled` 初值——只影响创建时刻,存量账户/逐账户开关/总闸互不牵动,总闸仍是即时熔断层;/api/admin/ai-beta/global 增 `new_user_default` 字段[与总闸/日预算同端点三字段独立可改],运维 AI 开关板总闸旁加「新账号默认开」ledger-switch;test_reader_ai 两个「逐账户未开 403」用例改显式关闭再断言)→ 3.37 HN 热门博客源扩充波(7 个人博客 RSS preset[sean_goedecke/giles_thomas/max_woolf/geohot/geoffrey_litt/martin_alderson/anil_dash],名单出自 Karpathy 分享的 Evan Schwartz 2025 HN 人气 RSS 榜,全批 incubating 全文 RSS;共享转换层 compact_text 围栏代码块/嵌套列表缩进保真[全站生效];3.37.1 geoffrey_litt talk 模板头部噪声剥离[日期 h3+重复标题 h1 成对剥,初抓 21 篇质检唯一站点级缺陷])→ 3.38 源合集波(发现页策展合集,方案 docs/source-collections-wave-plan.md。**定调=目录呈现层的策展视图+批量动作,不是订阅实体**——feed/MCP/未读/交付链路零感知,合集新增成员不自动推给已订阅者[「跟随合集」留二期];策展存代码注册表 `services/source_collections.py`[与 preset「代码即记录」同路线,首发五枚:hn-popular-blogs-2025 七源/frontier-labs-official 八源/cn-open-models 国产开源模型动态十三源(跨三形态)/ai-coding-tools 六源/ai-deep-writing 七源,provenance_note 支持公告白名单 markdown 子集;构思纪律=不复刻角色/形态既有轴,被毙与暂缓候选记于方案实施记录];API 三端点 `GET /api/reader/collections`[轻载荷,订阅态由前端与 sources 目录 join]+`POST/DELETE .../collections/{id}/subscribe`[批量,复刻 ensure_default_subscriptions 循环:隐藏/注册表外成员跳过回报 unavailable、include_hidden 幂等基线、单事务一次 commit;退订清水位,同属其它合集的成员也会退——无绑定记录的诚实推论];前端发现页头部「源 ⇄ 合集」二段 seg[合集视图藏形态 seg/排序,搜索过滤合集名/简介/成员名],合集卡=成员头像堆叠+n 源·已订 m+批量按钮三态[订阅全部/订阅其余 k 个/已全部订阅→hover 退订合集,退订走两击确认 4s 回落、title 如实列出将退订成员],点卡进合集详情[返回签+元信息头+成员源卡网格,SourceCard 自源视图提取共用];详情态 discoverCollectionId 提升 useReaderState——移动端注册为独立返回键历史层[返回先退详情再退发现页],批量后不做逐源乐观调整、重拉源目录校准) → 3.39 部署路径双轨波(裸机 PM2 路径自 intranet 分支回迁 main 扶正,与 Docker 并列为两条官方部署路径。动因=真实场景:一台**公网**机不便安装 Docker,而 `deploy.sh` 当时只存在于内网适配分支——它服务的已不是「内网适配」而是「宿主没有 Docker」,分支名与用途脱节。回迁面=`deploy.sh` + `ecosystem.config.js` + `production.example.ini` 的 `[server]`/`[nginx]` 两节;**`[network] disable_tls_verify` 及其 httpx 接线不回迁**——出网跳过 TLS 校验是内网 MITM 网关的专属妥协,公网开它等于自毁,intranet 分支的独有面自此收敛为该开关 + 须知块 + `.claude/` 钩子。回迁时清理三处内网痕迹:RAG 形态判定与 `[models]` 模型目录校验整段删除(向量子系统已随 v3.31 退役,`rag-embedded` extra 早已不存在,ini 若残留 `[rag] enabled=true` 会让部署去装一个不存在的 extra 而失败)、依赖安装收敛为 `uv pip install -e .` 单路径、「内网源」措辞中性化为「受限源/镜像加速」。文档 `docs/deploy-baremetal.md` 新立并与 `deploy-docker.md` 互指,两处「唯一部署路径」措辞同步纠正;example.ini 头部登记两节的**路径相关语义**(Docker 下不生效 / 裸机下是站点生成输入)。公网姿态相对内网姿态的四处反转写进文档:`disable_tls_verify=false`/`disable_ca_bundle=false`/`cookie_secure=true`/`cors.allow_origins` 具体域名——后两者在生产姿态下由启动安全校验兜底(占位符 secret 与 `*`+credentials 直接拒绝启动)。HTTPS 走两趟部署且必须用 `certbot certonly` 而非 `--nginx`:`write_nginx_site_config` 每次部署整体覆写站点文件,certbot 插进去的行会被静默抹掉) → 3.40 用户自定源波(读者自助添加私有 RSS 源,参照 Folo,方案 **docs/user-custom-rss-wave-plan.md**。定调=**私有订阅资产而非归档策展资产**(方案 B 隔离):隔离面四收口——不进公共发现页目录[仅订阅者可见]/不进 `resolve_all_visible_source_ids`[scope=all 问答、admin MCP、IM bot 全不吃]/不进日报[allowlist 天然隔离+`user_rss_` 前缀机械双保险]/不进 archive sync export,另加 articles 按 source_id 直查轻门槛[非订阅者得空,admin 不受限]。**最简正文拍板**:配置行固定 `fetch_detail_if_missing=false`,feed 给什么存什么、不分型不补抓,正文尾部一律「阅读原文 ↗」;preview 仅守门(解析出条目才能存)。技术底座全复用:`SourceConfigRecord` 加 `owner_username` 列(迁移 `d9de7994582c` 带收养回放列守卫)+generic_rss 参数驱动+读者目录 `_configured_source_meta` 既有覆盖。**同 URL 去重共享**:source_id=`user_rss_`+sha256(规范化URL)[:12][碰撞全长重建],第二人添加=订阅既有行,owner 记首建者仅溯源不承担权限;**系统源撞库检测**:撞 registry preset feed_url∪平台 config → 转引导订阅系统源,撞隐藏源统一 404「暂不可用」防影子源。服务层 `services/user_sources.py`;reader 端点四个 `/api/reader/custom-sources[/preview|/{id}]`;删除语义=退订+无人订阅即物理删(配置+文章+SourceState+各用户水位;收藏/读态孤儿沿「无害」口径)。调度=APScheduler `user_rss_refresh` interval job(间隔 KV 可配热生效 `reload_user_rss_schedule`,防重叠 max_instances=1)+连续失败≥10 自动停抓(再添加即复活)+媒体预取豁免(`schedule_media_prefetch` 按前缀滤,私有源图走 proxy 按需)。配置分档:总闸 `user_sources_enabled`(KV,默认开,关=添加/preview 403+调度跳过+runtime 能力位隐藏入口,数据不动)+刷新间隔(KV,默认 60 分钟)可配;三护栏常量(20 源/日增 10/停用阈值 10,沿 DAILY_SHARE_LIMIT 范式)。取名链补第三级 config 表兜底(`friendly_source_name`,顺修 X config 源同病)。admin 治理收敛运维管理→内容「用户自定源」区(`admin/UserSourcesPanel`:总闸+间隔写入口、KPI、源列表停用/启用/两击删除[级联清全部订阅行],`/api/admin/user-sources*` 自动入审计;读者自助动作沿 reader-surface 豁免不入审计);前端「自定源」独立第五分组(`sourceTaxonomy` custom 档,前缀短路判定,后端 source_role 无需镜像)、发现页头部「添加源」入口+`AddCustomSourceModal` 两步流(URL→预览样例+配额→确认,桌面/移动壳共用)、源栏减号与右键「移除自定源」统一走移除路径。测试 `tests/test_user_sources.py` 26 项)→ 3.41 账户管理 V2 波(150 人上量的管理面可用性,审计 M05+M06+M07+M19 一波落地,方案=docs/admin-usability-audit.md 整改分层第二梯队。**M05 批量**:`POST /api/accounts/batch`(`accounts.batch_update_users`)一次改一批账户的角色/启停/AI 开关——**原子语义全成或全不成**[任一账户不存在或整批终态活跃管理员归零即 400 回滚,末位保护按**整批后终态**裁决,天然覆盖「两个 admin 同批停用」组合;幂等项不计 updated 不 bump updated_at;上限 500],批量入审计[中间件 body 白名单加 batch 路径,语义摘要「批量关闭 AI N 个账户(名单前三)」];前端账户表加复选列+页级全选+`ledger-batchbar` 批量条[开/关 AI、启/停用、设为管理员/读者,停用与角色变更过 confirm、含自己时预警,AI 总闸关闭时 AI 批量钮让位提示]。**M06 筛选排序**:`GET /api/admin/accounts` 加 `role/status/ai` 组合过滤(与 q 叠加)+`sort/order` 服务端排序(username/created_at/last_login/logins/reads/ai_calls/ai_tokens/subscriptions;非法键回落不 5xx;summary 与 Top 榜恒聚合全量不随过滤收窄);前端筛选条[表顶 `.acct-filterbar` 三组 mini-seg]+可点排序表头[`.acct-sortbtn` th 内按钮,同列翻向/换列取惯用初始方向,aria-sort+激活列 accent-ink+▲▼]+账户 KPI 下钻[「管理员 N/停用 N」小字 `.kpi-sub-link` 点击即筛,再点还原];过滤/排序/搜索/时间窗变化归位第一页并清勾选。**M07 聚合 SQL 化**:`usage_by_user`/`reads_by_user`/`logins_by_user`/`reads_by_source` 全部改 SQL GROUP BY,不再把整个时间窗明细行载入内存 Python 累加;账户面三张 by_user 聚合同时排除删号墓碑 `deleted:*`[账户列表/活跃榜只看现存账户,墓碑消耗仍进 `ai_usage.summarize` 成本看板口径——历史成本不消失]。**M19 抽屉管理闭环**:活动详情抽屉顶部加就地管理动作行[开/关 AI、设为/取消管理员、启/停用、重置密码、删除,复用表格行同一批 handler;单账户操作若发生在抽屉打开的账户上顺手刷新抽屉快照,activity 端点 account 载荷补 `role` 字段;删除该账户即关抽屉]。Playwright 真渲染验收通过[筛选/排序/批量条/抽屉四路径,pageerror=0])→ 3.42 运维列表规模化波(审计 M08+M09+M11+M17,整改分层第三梯队;测试 tests/test_ops_scaling.py 7 项。**M08 自定源治理**:`admin_overview` N+1 消除——新 `active_subscribers_by_source` 把「每源一次全订阅扫描+每源一次用户查询」压成「一次订阅表扫描+一次用户存在性查询」,查询数与源数解耦[判定语义与 active_subscriber_usernames 逐字一致,测试守卫];UserSourcesPanel 加搜索(名称/地址/创建者)×治理状态过滤(正常/失败中/已停用)×分页(20/页,本地——载荷本就全量轻列)。**M09 运行历史真分页**:`/api/fetch-runs` 与 `/api/collection-job-runs` 响应改 `{items,total}`(破坏性换形,消费方三处同步)+ `days` 时间窗 SQL 过滤;FetchRunsTab 拉取带窗口、上限 100/200→500,total>载入量时页脚**诚实提示截断**(状态警示非常态文案)——「较老失败记录静默消失/日统计与列表矛盾」根除;完全服务端分页刻意不做(父子聚合展开模型需子运行 lazy 化,折中与触发条件记 backlog)。**M11 审计覆盖+检索**:AUDIT_PATH_PREFIXES 补 `/api/articles`(录入带标题/编辑/删除/批量删除带篇数)、`/api/fetch`(手动触发采集)、`/api/archive/import`,body 白名单同步;audit-log 端点加 `operator`(操作者子串)/`q`(跨摘要·目标·路径,contains autoescape)/`status`(ok|denied)检索,与时间窗/分页叠加;审计面板配操作者输入+搜索框+结果 mini-seg,zone-hint 描述文本撤除(M23 docstring 文档债顺手清:「尚未实现留存清理」→retention 已实现)。**M17 消息治理**:反馈收件箱 `q`(正文/提交者)+`category` SQL 端过滤(与状态/分页叠加,counts 恒全量),前端搜索框+分类下拉;公告列表服务端分页+total(默认 limit 200 兼容存量语义)、dismiss_count 只对当前页聚合,前端历史公告 6/页分页。裁剪(记 backlog):反馈批量流转/负责人/优先级、运维数据导出(归 M18)、FetchTab 节点板搜索。**目检返修(多轮,与账户 V2 返修同一审美纪律)**:「列头即操作」沉淀为共享组件 `components/admin/TableTh.jsx` 三件套[ThSort 排序/ThFilter 轮换筛选/ThSearch 就地搜索(inputWidth 可调,长 placeholder 不截断)],账户/审计/自定源三表共用一套操作语言;账户「用户」列排序撤除改列头就地搜索[排序无意义,zone-head 独立搜索框拆除];审计三控件(操作者搜索/操作搜索/结果档)全部入列头,区头只剩刷新;自定源列表从卡行重写为 acct-table 表格[列:源(定宽200+列头搜索 名称/地址)/地址(吃弹性,URL 不截断)/创建者(独立列头搜索)/状态(列头轮换+三态章)/订阅·文章·最近成功(列头排序)/操作(启停/删除小图标 hover 浮现,删除改标准确认框),usrc 卡行 CSS 类族清仓];反馈收件箱头部三轮定稿[计数紧随标题、分类 select 改与状态同款 mini-seg 右上角上下两行、搜索框在标题正下方——左右各两行高度平衡];表头工程三修[**列名纵向参差**=三种列头 display(inline-flex/inline-grid/输入框)合成 baseline 不同+cell 默认 baseline 对齐,根治=`.acct-table.is-fixed .acct-th` 定高 36px+middle;**表头行高随搜索框展开跳变**=36px 定高整个吸收 22px 输入框;**聚焦双环**=全局 input:focus-visible 外环(未分层,恒胜层内 outline:none)溢出行界像"行变高",文件尾未分层区钉掉、聚焦指示收敛为 1px accent 内描边];`table-layout:fixed` 由账户表推广至审计/自定源表,静态行 `.is-static` 去误导 pointer)→ 3.43 数据生命周期与可靠性波(审计 M10/M12/M13/M14/M15/M16/M21/M22 一波收官,整改分层第三梯队完结、25 项仅余 M18/M20/M24/M25 随量低频项;测试 tests/test_data_lifecycle.py 13 项。**M10 收口**:负责人确认内外网两台均 `[runtime] role=all`,当前无风险;顺势把 `retention_cleanup` 注册从 collector 门控解耦——lifespan 里调度器任何角色都启动、留存清理恒注册,抓取类任务(load_tasks/远程同步/自定源刷新)仍只在 collector 注册,将来真拆分部署 reader 库不再无限膨胀。**M14 留存扩面**:`collection_job_runs` 180d(与 child fetch_runs 同窗)+条件性清理三表[谓词不止时间窗:`jobs` 终态 90d(queued/running 永不清,epoch 浮点单独换算截止)、`feedbacks` 已了结 365d(open/处理中永不清)、`article_shares` 已失效(撤销或过期)180d(存活含永久档永不清)];**刻意不清** `announcement_dismissals`(行数有界且承载「一次性通知」语义——公告下线复上不应再打扰关闭过的读者)。**M21 计量唯一约束**:`ai_usage`(day×user×purpose×model)/`reader_reads`(day×user×source)建唯一索引,迁移 `a7e2f95c1d40` 先对存量重复行求和合并再建索引(带收养回放守卫);写侧 `record_usage`/`record_read` 改 SQLite `INSERT…ON CONFLICT DO UPDATE` 原子累加,「先查后插/递增」的并发双插与丢增量竞态根除。**M13**:`/api/source-health`(45s 轮询)运行史回退只查「无 SourceStateRecord 快照」的节点,不再无条件把全部节点 180 天运行行载入内存(自定源照旧不回退)。**M15 刷新语义零新 UI**:AdminOpsTab 接 `active` prop——子页间切换重取目标子页、从其它 Tab 切回重取当前子页(消息子页经 wakeTick 换 key 强制重挂面板自取数),长开运维台不再静默过期。**M16**:`summarize` 的 `by_day_user` 服务端 Top-6(按窗口 total_tokens)+按日聚合「其它」行,载荷有界总量守恒,前端 pivotDaily「其它」中性槽无缝承接、`by_user` 全量榜不受影响。**M12**(落网之鱼,前波「未动名单」漏记):FetchTab 组头全选与单行同一口径——组头复选集只含 `!user_source` 组内成员(全自定源组直接禁用),`runChecked`/`saveCheckedAsJob` 消费端再过滤一层防御。**M22**:单用户抽屉最近登录兜底改 `last_login_for_user` 单人 MAX 标量查询(此前为看一个人全表 GROUP BY)。烟测实证:隔离栈 Playwright 断言五个刷新时机(网络请求计数)+五组组头复选态,pageerror=0;旧 smoke.db 走真实启动路径自动升至 `a7e2f95c1d40` 双唯一索引在位,生产升级路径实证通过)。

## 补丁节点

补丁节点:3.23.1 阅读器滚动条修复——文章/动态中栏经发现页或社交流条件卸载后重建时，自绘滑块重新绑定新 DOM，修复内容滚动但滑块停滞的偶现问题。3.23.2 Meta AI 列表解析修复——按单文章边界跳过任意语言 CTA 并固定请求语言，从详情页回退标题/发布日期；Meta 显式开启权威元数据刷新，再抓可修正已归档的错标题或抓取时间伪日期，正文不覆盖。3.23.3 动态空态口径统一 + 预置订阅补动态/社交各一源。3.24.1 阅读器切容器残影修复——loadArticles 失败时 catch 不清列表，上一容器旧条目会被新容器错误渲染（文章条目被 SocialFlow 当推文画出、卡片显示非社媒源名；请求成功时骨架屏遮挡故从不显形，需网络级失败触发，内网含内容审查网关的链路首次暴露）；切作用域 effect 先清 articles/articlesTotal 再发请求，失败呈干净空态+Toast。3.25.1 节点管理两优化——吸顶组头透缝修复[Chromium 的 sticky top:0 以滚动容器**内容边**为基准，`.board` 的 4px 顶部 padding 成为吸顶组头上方的透视缝，滚动时节点行从缝里露出；顶部 padding 归零，呼吸空间由组头自身顶距承担]+ 信号条角色筛选[官方/媒体/个人/榜单第二筛选轴放进既有信号灯统计条，与健康灯正交：同 `.signal-stat` 语法、3px 色标条与分组组头 marker 同色呼应，点选过滤/再点还原；健康灯计数随所选角色范围收窄，角色计数恒全量与组头一致，当前健康档在该角色下为空时自动回落「全部」]。3.29.1 移动波收口——社交流 headless 化[SocialFlow 加 headless:移动顶栏统一承担三容器的 seg/标读/搜索控件行,卡片流本体与桌面同源,双层头部合一;桌面社交头原样]+ 设置柜移动页面栈[components/mobile/MobileSettings:一级分组清单→二级分区页,分区组件与桌面设置柜完全同源(components/settings/),移动壳恒读者观感、管理组(数据同步/凭据)不下放;「我的→反馈」深链直落分区并 mark-seen]+ 返回键握手[hooks/useLayerHistory:正文页/发现页/抽屉/动作单/设置两级共六层接浏览器历史——层开着按返回=关最上层而非退出站点(微信内浏览器肌肉记忆),程序性关层消费对应历史条目不堆栈,模块级栈协调多层、与 App hash 路由零干扰]+ 长按单「分享给…」[Web Share 环境注入,载荷=站内深链;不支持不画]+ 登录页 ≤480 轻档[排版收纳,星座环境漂移与跃迁终幕原样——静谧版而非残缺版]+ 公告横幅 safe-area 顶距;顺修 .m-page 壳外取不到 --rd-* 变量的设置栈透底缺陷[变量作用域 .m-shell 扩至 .m-page]。3.29.2 转换层富格式补课——共享 HTML→Markdown 转换层(article_extractor)三处保真升级,全站源一处生效、只惠及新抓正文(存量在意则重抓):<pre>→围栏代码块[缩进/空行原样保留,语法高亮 token span 不破坏空白,language- class 识别为围栏语言标注,内容含 ``` 自动加长定界符;此前 pre 走段落分支被折叠空白]+ <code>/kbd/samp/tt→反引号行内代码[含反引号内容按 CommonMark 加长定界符垫空格;表格单元格「保 code」自此真实成立]+ 有序/嵌套列表[<ol> 保编号含 start 起点,嵌套子列表按父项标记宽度缩进,<br> 多行改同项续行;此前一律压平成 - 平行项]+ 嵌入媒体占位链接[iframe/video/audio 移出 _MD_SKIP_TAGS,降级为「▶ 视频/▶ 音频/嵌入内容 ↗」可点原链而非静默丢弃,块级/行内(p/figure 包裹)双路径覆盖;无 http(s) 地址与 svg 仍丢弃,播放器子树仍不渲染]。前端零改动(react-markdown 原生渲染围栏,.markdown-body pre/code 样式已在);语法高亮未加(需新依赖+亮暗主题适配,独立决策)。3.33.1 日报空正文事故修复——LLM 思考模式参数 + 空产三层防护[生产 2026-08 事故:LLM 切 deepseek-v4-flash(思考型,默认思考 effort=high)后日报 reduce 的 completion 被思考吃满 8192 上限,`message.content` 空串→客户端只拦 None 放行空串→`db_storage.save()` 的 summary 兜底把空串写成 NULL 且 has_content=True、status=success 照写,阅读器「暂无正文」而神灯流水线消费 extensions.items 无感;修复=①`[llm] thinking_mode` 配置(空=不发送/disabled/low/high/max,凭据层+KV+env `DORAMI_LLM_THINKING_MODE` 全链,client 翻译成 `thinking:{type}` + `reasoning_effort`,端点不支持 400 自动降级去掉重试,设置柜 LLM 卡加选择器)②client 空串 content 与 None 同判抛 LLMError、finish_reason=length 打截断告警 ③`_persist_brief` 落库前断言正文非空——失败不写库不推游标,候选下轮重来]。3.32.1 测试库隔离修复——「重启后 AI 已开启状态丢失」的真凶不是重启而是**跑测试**:tests/test_mcp.py 三个测试播种账户前漏了 monkeypatch db_sink,「删除重建 admin/user」直接砸在真实 data/cms_data.db 上(dev 管理员密码被重置为 admin、ai_beta_enabled/头像清零;多年注记「test_mcp admin 登录失败是环境使然」即此因)。修复=三处补 tmp sink + **tests/conftest.py 会话级沙箱**(收集期把 DORAMI_CONFIG_FILE 指向临时 ini,任何疏漏测试都砸不到真实库;外部显式设置时尊重不覆盖);验证:全套跑完真实库 users/articles 快照 byte 级一致。3.33.2 问答语料称呼修复——scope=all 的降级检索说明曾硬编码「读者订阅内容」(`subscription_context` 两档共用同一文案),内网 IM 机器人(all 档代答渠道)据此回出「基于你订阅的文章」而 IM 用户并无订阅概念;`corpus_label` 参数让措辞随检索域走(all=「哆啦美收录内容」,subscription 默认不变)+ QA 系统提示词时效示例话术中性化(「你订阅的内容里…」→「与此相关的最新资讯是…」,防模型照抄归属)。同波拍板:**IM 机器人问答档位 subscription→all**——策展旋钮从未被使用、且有「平台里有但 bot 订阅外→答不知道」的诚实性缺陷,all 本就是 v3.32 给代答场景的预留原语;dorami-bot 的订阅自此仅日报 dfeed 拉取依赖(日报源必须保持订阅)。3.36.1 MCP 管理员令牌空作用域修复——feed_service 管理员 dfeed_ 分支曾返回 `[]` 表「未限定来源」,与 mcp_server「空列表=零订阅应返回空」契约相撞(`source_id IN ('__none__')`),管理员令牌经 MCP 检索恒空而聚合 feed 正常,表现酷似「账号没订阅」;内网 bot(p_dorami 管理员账号)接入实锤。修复=admin 分支改返回 `resolve_all_visible_source_ids` 显式枚举(与问答 scope=all 同口径,隐藏源照旧排除),`[]` 歧义哨兵消灭、空列表自此只有「零订阅」一种含义;附带收益:管理员令牌接 bot 时新上源自动进检索域,免「全量订阅+新源补订」运营纪律。3.39.1 裸机依赖钉版——v3.39.0 首次公网裸机部署即撞上 **mcp 2.0**(2026-07-28 把 `FastMCP` 改名 `MCPServer` 并变更 API):`deploy.sh` 走 `uv pip install -e .` 现解依赖,按 `mcp>=1.0.0` 装到 2.x,后端 `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` 起不来(表现为 127.0.0.1:8088 connection refused),而同版本 Docker 镜像因走入库钉版清单安然无恙——**「宿主现解依赖」是裸机路径的固有弱点,不是这次的偶然**。双修:①`deploy.sh` 改为按 `docker/requirements.txt` 装运行时依赖 + `uv pip install -e . --no-deps` 装项目本身,两条部署路径自此共享同一份版本事实来源(清单缺失时退回现解,由 pyproject 约束兜底);②pyproject 的 mcp 加 `<2` 上限(代码用 1.x FastMCP API,上限是硬约束而非保守),重导出清单后版本未变(仍 mcp==1.27.1)。 3.39.2 日报 map 锚点返修——生产 0827 日报漏报「Claude in Chrome is generally available」(claude.com 官博):逐环节排查(抓取/名单/候选/聚类/跨天查重全清白)锁定 map 评分,生产模型重打 7.5/8.5/8.5 摇摆正卡 8.0 入选线——v3.35「厂商主次甄别」的「外围产品小功能、客户端更新压 4-6」按字面正中浏览器扩展 GA 公告,而 7-8.5 档只点名模型/API,重大产品里程碑落在锚点缝隙。MAP_SYSTEM_PROMPT 三改:①压分条款收窄为「企业客户案例/区域上线/营销叙事/周边生态/纯增量小修小补」(删「外围产品小功能、客户端更新」字面);②新增「Agent/产品线正式 GA、重大里程碑发布」7-8.5 正向档;③新增风向标厂商例外——OpenAI/Anthropic(Claude/Claude Code)的产品功能即使体量小也常有巧思/通用性/范式级讨论,按「新交互范式/新能力面/广泛适用性/讨论度」评估不机械压分。生产模型双向验证:目标文 8.5×3 稳定过线,边角料回归(QwenWork 公测 6.5/获奖认证 4.5/合作稿 5.5)仍远低入选线、压分未破。漏报当期不回补(游标语义,拍板)。 3.38.1 迁移链多头容忍——内网分叉仓自带迁移支线时,合入 main 新迁移后 DAG 双头(git 零冲突),启动路径 `ensure_migrated` 的 `upgrade("head")` 无条件报 "Multiple head revisions" 起不来;改为检测多头即 `upgrade("heads")` 并行全升(main 单链下恒等价于 head,纯为下游分叉仓兜底);同轮更正一处误判:「create_all 出生无版本库被收养回放会撞重复列」不成立——迁移链本按收养回放安全纪律写,test_ensure_migrated_adopts_legacy_db 即该护栏,勿再给收养路径加「探哨兵 stamp head」类捷径(会跳过带数据语义的迁移)。 3.40.4 P0 安全收口波——管理面可用性审计(docs/admin-usability-audit.md,Claude+codex 双独立审计 M01–M25)的 P0 四项一次修复:**M01** `/api/mcp/toggle` 曾对读者裸奔(处理器无授权检查,`/api/mcp` 在 READER_API_PREFIXES 短路放行)→单列进 `account_admin_required` 并入审计前缀(摘要「切换全站 MCP 总闸」),status 保持读者可读;**M02** summarize 三层护栏穿透(端点不接日配额+`_AI_DAILY_CALL_LIMITS` 无键+`VALID_PURPOSES` 漏登记致 record_usage 静默丢行,全站日预算永远读到 0)→三处补齐(50/日),读者面新增 AI 用途须同步三处的纪律注释入 VALID_PURPOSES;**M03** 删号残留与同名继承→`delete_user` 单事务收口,**语义拍板=资产物理删除+计量墓碑化**(订阅/令牌/收藏/公开分享/读态水位/反馈/公告 dismiss 物理删,自定源随退订走「无人订阅即物理删」`purge_account_user_sources`,AI 用量/阅读/登录事件改写 `deleted:<原名>` 保看板与成本历史,审计日志留原名),router 两段提交废除;**M04** 改密不吊销会话/删号重建复活旧 Cookie→`users.session_epoch` 会话世代列(迁移 `b4a1c7e5d2f8`),token 带 `gen` 且校验须与列一致——改密/重置即吊销全部既有 Cookie(自助改密就地续签本人不断线),建号随机世代令同名重建不复活;存量行/旧 token 按 "" 对齐、升级不强制重登。两个历史假绿灯测试(读者 toggle 断言成功、summarize 只断言传参不断言落库)同步修正,新增 7 项护栏测试。 3.43.1 交叉检视返修——v3.43.0 经 codex(gpt-5.6-sol xhigh)独立检视抓出 6 个真问题一次修复(讨论达成一致后执行,终审通过):**高·墓碑撞唯一键**[v3.43 计量唯一索引 × v3.40.4 删号墓碑化的交互回归,codex 实证复现:同名账号「删→重建→同维度计量→再删」时 delete_user 裸 UPDATE username→deleted:x 撞聚合键唯一索引抛 IntegrityError、整个删号事务回滚接口 500;修复=`_merge_into_tombstone` 合并式墓碑化——逐行 ON CONFLICT 累加进墓碑键再删原行,计数守恒、updated_at 取较新值,LoginEventRecord 无唯一键仍裸改写];**中·M10 解耦做一半**[retention 回调夹带的自定源孤儿 GC(物理删配置/文章)随全角色注册跑到 reader 上,且协程回调同步删表阻塞事件循环;修复=自定源维护加 runtime_collector_enabled() 门控(不看总闸,F8「关闸后 GC 无人收」语义保留)+两段清理均 asyncio.to_thread];**中·wakeTick 换 key 丢草稿**[强制重挂销毁管理员写一半的公告/回复草稿;改 refreshTick prop 只驱动两面板加载 effect,不重挂——草稿/检索词/展开态全保留];**中·loader 竞态**[多刷新时机下旧响应后到覆盖新快照;AdminOpsTab 七 loader 各自 claimGen keyed 代次守卫(实例级 useRef,错误 Toast 也过守卫),两消息面板另加本地代次 ref];**低·「其它」用户名碰撞**[「其它」是合法用户名,进 Top 时尾部用户被并进同名桶污染系列;聚合桶键改冒号 sentinel `other:`(用户名禁冒号天然不撞),前端渲染层映射(sentinel→「其它」/真实「其它」→「其它（用户）」),用户维度 pivot 改 topN=Infinity 消除前端二次裁剪(后端已裁 ≤7 系,二次裁剪会再挤真实用户并回桶甚至产重复 key)];**低·分享双时间戳**[过期已久又被补撤销的链接多留一窗;谓词简化 or_(revoked_at<cutoff, expires_at<cutoff),NULL 比较自然为假]。测试补 4 项+2 处反假绿灯加固:墓碑回归/双时间戳/「其它」不并桶/**upsert SQL 结构断言**(record_usage/record_read 必须是单条 ON CONFLICT INSERT 且写前无本表 SELECT——防将来悄悄改回读改写)、M13 测试加 SQL 参数监听断言收窄本身(有快照节点 id 不得进运行史查询参数)。烟测补「切走切回草稿保留且数据仍重取」断言。残余记 backlog:「其它（用户）」极低概率 identity 碰撞(彻底解法=图表层 identity/display 分离)、拆分部署下自定源归属故事未定义。 3.43.2 前三波补充检视返修——codex 对 v3.40.4/v3.41.0/v3.42.0 三波实现补做交叉检视(当时只有测试+烟测未走检视环节),抓出 7 个存活问题+终审再拦 3 个残余,全部达成一致后修复:**高·详情抽屉操作错账户**[快速点开 A→B 时 A 的迟到响应错挂进 B 的抽屉,动作按 detailData.account 实际操作 A;修复=claimGen('detail') 代次守卫+closeDetail 统一关口(bump 代次作废在途)+**detailUserRef**(终审拦截:refreshDetailIfOpen 在操作 handler 的 await 之后调用,读 state 是旧 render 闭包——操作 A 在途时关 A 开 B 仍会作废 B 的请求并挂 A 数据,经 ref 读最新抽屉归属)];**中·AI 成本闸误拦缓存**[translate/summarize 的逐用户配额+全站预算前置在缓存判定之前,缓存命中零成本也 429;修复=授权(_require_reader_ai)与成本闸(_enforce_ai_cost_gates)分拆,成本闸经 service 的 pre_llm_check 回调在缓存 miss 后、真实 LLM 调用前执行,ask 无缓存端点直调;check-before-call 的并发 TOCTOU 窗拍板为**软上限**记 backlog——硬上限需原子预留-核销下沉 LLM 调用层,内网收益低];**中·公告假分页**[前端仍 limit=200 全量拉+本地 slice,第 201 条起永久不可见、末页删空卡空页;修复=真服务端分页 load(page)+total+发布回第一页+越界回退];**中·删号漏用户名级 KV**[reader_defaults_seeded:/feedback_seen:/user_sources_added: 三类标记残留——同名重建拿不到默认订阅播种、继承旧身份当日自定源额度;修复=delete_user 同事务清理,**LIKE 通配符转义**(终审拦截:用户名允许下划线,不转义则 a_b 删号误删 axb 的 key,escape='\\\\' 三字符转义+专项测试),key 格式由测试与三处权威常量比对钉住];**中·审计面板迟到响应覆盖**[非第一页改检索条件时「新条件旧页」与「归位第一页」并发,旧页后到覆盖;loadGenRef 守卫 then/catch/finally];**低·批量审计人数失真**[摘要按原始数组计数而服务层 strip/去空/去重,body 超 32KB 未采集时伪造 0;修复=同口径规范化+「数量未知」如实记录];**低·审计摘要规则错配**[/api/fetch/batch 被通用规则记「手动触发采集节点 batch」、文章路由 {article_id:path} 含斜杠 ID 被 [^/]+ 拒配退化空摘要;修复=batch 精确规则前置+文章规则吞全路径+app.py body 白名单补 /api/fetch/batch]。前三波其余重点逐项 verdict=OK:M01/M04(会话世代无旁路)/M05 数据事务语义(双 admin 同批停用等组合正确)/M06/M07/M08(订阅者映射逐字一致)/M09(消费方全换形)。测试 +8 项(册内 21 项),全套 671 过;TOCTOU 软上限与拆分部署自定源归属记 backlog。codex 额度耗尽于最后一眼,三处终审返修按其点名修法实现+自查推演/专项测试代替签字。 3.43.3 站内深链登录门 hash 导航丢失修复(issue #2)——用户实报:非登录态点深链,登录(密码或内网 SSO)后落空白阅读器空态而非目标文章。Playwright 逐路径排查:新 tab 打开/登录门刷新(v3.33.3 修过)/admin/读者/移动端标准路径全部正常,唯**停在登录门的 tab 被同源 hash-only 导航带入深链**(IM/微信内嵌 webview 复用已开 tab、或往登录页 tab 地址栏粘链接)时不 remount——`deepLinkArticle` 只在 App mount 一次性取值,运行时深链承接的 popstate 监听又只在 authenticated 后注册,登录门阶段 hash 变化无人接,登录后 commitNav 重播覆写 hash,id 永久丢失。修复=App 补登录门专属 hashchange 监听(非 authenticated 阶段挂载,见深链即 setDeepLinkArticle,非深链 hash 不动、与 onPop 同口径),登录后由既有链路(admin 界面切换+initialArticleId 消费)接管。排查顺带确认:深链指向自定源(`user_rss_`)文章而收件人非订阅者时同样呈空态,那是 v3.40 隔离语义的设计内静默(fetchArticle 404→silent),不属本 bug。codex(gpt-5.6-sol)交叉检视抓出 2 项返修,实证后修复、复检通过:**高·admin 覆写竞态**[登录门 hashchange 路径不更新 hadDeepLink,认证后深链 enterReader effect 与 default-surface effect 同 commit 先后跑、后者读同轮闭包 surfaceMode===null,default_surface=console 时把刚设的 'reader' 覆写回 'console'——Playwright 实证文章在隐藏阅读器被消费(titleVisible=false/adminRailVisible=true);修复=default-surface 判定纳入 deepLinkArticle 并加依赖];**低·登录门陈旧深链**[捕获深链后用户退回非深链 hash 再登录仍开旧文章;登录门监听改镜像当前 hash(非深链即清空 pending)——与 onPop 的「非深链不动」刻意不同口径:登录前无在途消费,不清才是错的]。 3.45.1 阅读窗摘要复用与精简(issue #13,顺修 #12)——阅读窗「内容价值分」理由段与「哆啦美速读」卡曾上下两段近似摘要;根因不是两次调用,是**同一次分析调用的 `score_reason` 与 `summary` 两个字段被渲染成两块**(提示词只说「可直接展示的理由」,模型写成评价性全文概述;dev 实测理由均 131 字、摘要均 236 字)。修法核心=复用与精简:①分析产出精简为「一句 ≤40 字纯理由(要求**先于分数输出**,是最便宜的校准手段,原本分数在前理由在后起不到先想后打分的作用)+ 唯一 summary」,**取缔 `one_sentence_summary`**(消费方个人早报条目行/公共日报 adapter 都已有 summary 回退;迁移 `d8b3f1a6c9e2` 删列,历史 edition 快照仍带该键前端保留回退读取),解析层 score_reason 上限 1200→120 字截断,提示词版本 v3→v4(调度器自动重跑近 7 天存量,更早等 `full_analysis` 回填);②`reader_ai.summarize_article` 有分析 summary 即短路返回(不进成本闸不计费不写 extensions,响应带 `source:analysis`),只在源关闭分析/私有自定源/尚在排队时走自己的轻提示词;③读者面两块合一,**速读卡从零重设计**(五轮目检定稿,共享组件 `components/AiReadingCard.jsx` 桌面/移动同源):AI 渐变 wash 底 + **衬线渐变大数字**是这张卡的全部身份,小星取缔;两栏——左只有一个数字(内容价值分 38px/移动 32px,数字专用字体栈 `Charter, Cambria, Baskerville, Georgia`——要**齐线数字**,Georgia 只有老式数字,7/5 带降部让小数点看着像居中点「7·5」;`.ai-grad-text` 渐变**落在每个数字元素自身**——挂外层时 background-clip:text 画不进 overflow:hidden 格子里的子元素,实测数字整个消失只剩小数点;排印钉在文件尾未分层区,`@layer` 内被全局 `button{font:inherit}` 压掉——与生成链同坑),右 = 两层同构内容(小标 11px/700/accent-ink 字距 0.06em + 正文 13.5px/1.8/ink-2):「AI 速读」摘要层与「评分依据」层(一句理由 + 免责 faint 小字);**入场里程表式滚动**(用户第一版反馈「逐帧跳变、卡高随数字跳」——真因是 rAF 逐帧 setState 换文本 + Georgia 比例宽数字牵动右栏折行;现每位数字住 0.64em 定宽格,格内 0–9 竖条 transform 2.2s 滑到目标、十分位晚 220ms 起步,无逐帧重渲染,实测 8 帧卡高/左栏宽/右栏 y 零变化;减少动画直落);**点数字在卡内切换两层**(用户 idea,替代浮层——浮层遮正文且叠成两层卡):叠同一 grid 格,卡高由较高者撑住切换零跳动,480ms 慢速淡切+8px 位移(按下态曾做数字微缩 0.96,细微到被当 bug,撤),再点/Esc/换篇回摘要,`aria-pressed` 表态;悬停微浮+饱和微增作可点暗示;无分数时左栏不渲染,右栏承接骨架/生成链;`.reader-ai-summary .reader-ai-layer-text` 双类压过 `.markdown-body p` 段距;**卡只在账户 AI 开启时渲染**(用户拍板,AI 关闭整卡不出现);标题区只余无框标签行 `.reader-pane-tags`,`.reader-analysis-summary` 框仅管理面抽屉/弹窗仍用;`.brief-item-summary` 两行截断;④#12 假 0 分:`qualityScoreText` 加 `value == null` 守卫 + `ArticleRow` 加 `quality_score != null` 前置。**评估后明确不做**(记 backlog 展望):拆两次调用「先打分再理解」(prompt 翻倍、短路覆盖面 4%、真收益只在 scoring_version 频繁变化时)、公共日报并入(摘要契约不同且 adapter 只事后覆盖不省调用)。总 token 只省约 5%,收益是体验与结构。 3.49.1 评分链路检视返修(issue #33 §1)——生产近 7 天 699 篇文章导出后本地跑 v3.48 链路(生产同款 deepseek-v4-flash),62 篇分层黄金集由 Claude 与 codex(gpt-5.6-sol)独立打分比对,确认六处结构性问题一次修复:**F1 论文整层偏高**[HF Daily Papers 121 篇 83% ≥6.0,理由模板化「有明确新意」;两位评审对论文也分歧最大(4.5 vs 6.9),锚点本身歧义]+**F5 正文长度偏置**[流程比两位评审都低的篇目正文中位 178 字符、都高的 1785:官方短公告/推文被「信息稀薄」误伤,长篇媒体稿的「行业级」措辞被当真]→ 提示词 `article-analysis-v6`/`news-value-v2`:论文档改可判定条件(常规论文默认 4.0–5.5,里程碑/开源权重+SOTA/头部实验室引发讨论三者之一才 6.5+,「新意」是准入门槛不是加分理由;上行条件不解除车载上限)、新增【篇幅不是分量】(官方发布页/推文正文空短按标题所指事件评;看不出事件的裸链接/截断转推上限 4.0;媒体转述与传闻上限 5.9)、要求一位小数拉开同档;**F2 分数吸附 .5 刻度且 5.5/6.5 双峰卡在 6.0 两侧**+**F3 同篇复评 11–22% 跨线翻转**+**F4 清淡日日报塌缩**[09-07 名单内 19 篇过阈仅 3 篇]→ 日报硬阈值改**软阈值**:KV `daily_brief_min_items`(默认 8,0=不保底)正文保底——过线不足时从近线带 [min_score−1.0, min_score) 按有效分补足正文(照常写要点点评,报头如实注明「按新闻价值分补足 N 条」),近线带其余条目在正文未满 top_n 时以标题+链接填进附录(只填空槽,忙日一条不加),低于近线带仍 pass;**缺口与空槽都按最终成品算**(codex 检视两轮 7 条 P2 返修):当日已有正文与本批过线稿、近线带陪跑稿(名额 = 缺口×2,陪跑稿之间也会互为重复)进**同一次**同事件聚类,缺口 = min_items − 归并后合格簇数(早间批与增量批报同一事件只算一簇;代表若是近线官方稿、簇里有过线稿仍算合格簇,靠 `ScoredItem.merged_ids`);补足稿多带一条备用,跨天查重剔条后按剩余缺口终裁、没用上的备用稿退回附录候选、被剔的不退回;附录空槽 = top_n − 本批正文 − 当日已有正文(被新代表并簇的只扣一次)− 当日已有附录——否则同日重跑会被增量批一轮轮补稿、附录长成近线稿倾倒场;**保底是一轮编辑调用内的尽力目标而非终态不变量**:过线稿被跨天查重剔掉后只有备用一条可顶,不为此给每条过线稿预留近线备用稿(忙日会多跑数条大概率用不上的编辑调用),有意的边界;`last_run` 增 min_items/threshold_backfilled/near_miss_appendix,配置端点与面板加「正文保底条数」;**F6 体裁越界整篇作废**[模型输出闭集外 `model_evaluation`,`validate_analysis_payload` 当 base field 无效抛弃整篇]→ 降级为 `other` 记 warning `unknown_genre_fallback`,分数缺失/非法仍失败。验证(同 699 篇重跑):黄金集 MAE 1.03→0.84、档位一致率 66%→68%,论文 ≥6 从 101 篇降到 5 篇,.5 刻度占比 74%→55%,官方短正文均分 4.71→5.24,传闻类(「GPT-6 灰测」9.5→5.8、「OpenAI 囤 Mac」8.5→5.5)与营销通稿照旧压下;日报重放清淡日正文由 3 条回到 6–8 条+附录补位。**稳定性未随提示词改善**(近线带 45 篇复评 22% 跨线翻转,一位小数让完全重复更少)——这正是阈值改机制的理由,不靠模型自觉。黄金集沉淀为 `tests/fixtures/golden_news_value.json`(62 篇正文截 6000 字+两位评审分+v5 基线),门禁脚本 `scripts/eval_news_value_golden.py --model deepseek-v4-flash` 改提示词前后各跑一次比 MAE/档位一致率(62 次 LLM 调用,不进 pytest)。残余:无正文官方模型卡(hf_deepseek_models)两轮 8.0/5.5 摇摆;个人早报 5.0 门槛未动(新分布下博客/论文落 4.5–5.5,观察期看空报率再调)。 3.51.1 个人早报重编入口收口(issue #33 §5)——兴趣/订阅变更只记录不再自动重编当日早报(此前兴趣页连点几个标签、发现页连订几个源就连开几个 revision,读者感受「早报自己变了」);重编只剩手动「重新编排」与次日 08:30 定时,两者按当时最新兴趣与订阅编排;今日端点多 `interest_stale`/`scope_stale` 两位(冻结兴趣版本/权限边界 vs 当前解析结果,只在今日端点算),页面一行「你的关注/订阅已更新，下次编排生效 · 立即重编」把主动权交给读者;原样重存兴趣不改写 updated_at(否则误标落后);系统侧保留公共日报就绪追加与管理员下架来源全员重编两处维护性触发;枚举值保留只不再有读者面写入;今日版面里被退订源的条目不消失(不可变快照,与历史版本同口径)。方案 `docs/personal-brief-rebuild-entrypoints.md`。 3.52.1 个人早报标题中文化(issue #33 §4)——条目快照补 `title_zh`,卡片中文主标题 + 英文原题副行(`.brief-card-title-orig` 沿阅读窗译名视觉语言);来源按成本从低到高:文章 `translation_zh_title` 缓存(指纹一致)→ 近三期公共日报同篇 `title_cn`(只展示不写回)→ 编排后批量译(`services/personal_digest_titles.py`:aux 轻模型、阅读窗同一提示词、并发、整体 20s 预算,译文写回缓存让阅读窗与其他读者受益);中文标题不译不画副行、失败回退原标题、LLM 未配置只做前两级、不并入分析调用;钩子在 `process_pending_edition` 版本 ready/degraded **之后**,只补展示字段、异常不把版本打成 failed;新计量用途 `personal_digest_title` 归 system 不进读者预算;历史版本不回填。方案 `docs/personal-brief-title-localization.md`。 3.52.3 早报卡「兴趣 · 标签」chip 回归修复——v3.50.0 加重大事件 chip 时把兴趣命中 chip 那一行替换掉而非追加,自此早报卡不再显示命中的关注标签(报头「N 篇命中你的兴趣」照常、卡上无标记);恢复该行,顺序 兴趣 → 重大事件 → 标签。**同版口径统一(用户拍板,理解 2:动词也统一)**:读者面「关注」一律改「兴趣」——早报卡 chip「兴趣 · 标签」、§5 提示行「你的兴趣已更新」、兴趣页「我的兴趣 / 兴趣 n · 屏蔽 n / 点击标签加入兴趣」、选篇理由句「命中你的兴趣「X」」;代码标识符(interest_*/follow/mute)与 v3.52 方案文档旧口径注记不动。 3.54.0 个人早报「订阅 ∪ 兴趣」+ 页面体现个性化(issue #33 §3;方案 `docs/personal-brief-interest-union.md`,样页 `docs/design/dorami-brief-personal-quiet.html`)——**选篇**:兴趣半候选池由「订阅源」扩成「订阅源 ∪(全站可见源 ∩ 命中兴趣标签 ∩ 非屏蔽)」,订阅外只进兴趣通道、门槛 6.0(公共日报入选线;订阅内仍 5.0)、每源每期硬上限 2(不参与放宽),订阅内命中排前,质量半仍只从订阅源选;命中判据订阅内外同一尺(主标签或相关度 ≥ 0.8,与阅读器兴趣透镜同定义,屏蔽仍看全集);订阅为空但设了兴趣出「只有兴趣半」的版本(≤5 篇),两者皆空才空报;冻结/就绪/feed/MCP 语义不动;两枚参数为管理面 KV 旋钮(运维 → 分析 → 「早报订阅外兴趣」卡)——生产 7 天回放样本未留本地故先按推荐值上线、观察后调;订阅外池在 SQL 里以命中指派的 exists 收窄,零 LLM 调用。**页面**:报头之下一行编排说明(渐变星 + 渐变「哆啦美」= AI 身份;「从你订阅的 N 个来源和 K 个兴趣 · M 篇里选出 P 篇,x 篇命中你的兴趣(y 篇来自订阅外)」;订阅为空 / 降级各有句式;右端「调整兴趣」深链发现页兴趣段),§5「兴趣已更新 · 立即重编」、来源/分析未完成、重编排队三条提示行并入尾句一次只显一条;选篇理由四词进卡头右侧(兴趣 · 标签 / 重大事件 · 官方一手|N 家来源 / 新闻价值入选 / 最新更新——不用「今日最重要」),脚部 chip 只剩主题标签(单轴);订阅外卡源名后 faint「未订阅」悬停翻「+ 订阅」就地订阅;「M 篇里」来自新列 `personal_digest_editions.selection_stats_json`(迁移 `b2d8e4f6a9c1`,历史版本 null 省略半句);空订阅 + 无兴趣的空态给「去发现来源 / 设置兴趣」双动作。

## v3.44 起逐波记录(按记录先后)

**减少动效改应用内开关(issue #73,2026-09-14;版本号以实际发版为准)**:Windows「关闭动画效果」(远程桌面/虚拟机常为性能默认)让浏览器报告 `prefers-reduced-motion: reduce`,v3.45 轨面悬停揭示因此在 Windows 上只剩瞬时切换——与 v3.22.2 登录页「只剩静止一帧」同一机制。盘点后拍板不再信任 OS 查询:全站幅度最大的登录演出早已无条件播放,仍被门控的工作区动效几乎全是 300ms 内、12px 内的反馈过渡,门控保护的反倒是风险最低的部分,而该查询在内网 Windows 读者里多数是误报。落地=**默认全员完整动画**,「减少动效」成为 设置 → 外观 的应用内开关(`frontend/src/motion.js`:localStorage `dorami-motion`,生效为 `<html data-motion="reduce">`,`index.html` 首帧脚本先行设置;外观分区自持 hook,桌面设置柜与移动设置栈同一组件);index.css 的 11 个局部块与文件尾全局钳制全部由 `@media (prefers-reduced-motion)` 改挂 `:root[data-motion="reduce"]`(登录/跃迁品牌子树仍放行,兴趣页一处空媒体块删除),速读卡里程表改读 `motionReduced()`,两处 `scrollIntoView`/`scrollTo` 平滑滚动补入同一判定(此前 CSS 钳制管不到 JS 的 behavior 选项)。纪律 `docs/frontend/conventions.md` §7 同步改写:新增动效不再写 OS 媒体查询。**codex 本地协商式检视(gpt-5.6-sol)首轮 3 条全部成立、协商后一次修完、复检通过**:①recharts 3.9 的 `isAnimationActive` 默认 `'auto'` 会自己读 OS 查询(与拍板相悖,且 CSS 钳制管不到它的 rAF 插值)→ 运维页 7 个图元/Tooltip 显式传 `isAnimationActive={!useMotionReduced()}`(严重度协商 P1→P2:admin-only、只在装载时跑一次);②首版 hook 只在外观分区挂载期存在且不听 `storage` 事件,其它已开标签页不同步 → `motion.js` 改模块级 store(`useSyncExternalStore` + 模块顶层常驻 `storage` 监听,setter 与回调都先写 DOM 属性再通知);③`:root[data-motion="reduce"] .skeleton` 特异性 0,3,0 压过 `[data-theme="dark"] .skeleton`,暗色+减少动效丢白叠加渐变 → 仅此一条改 `:where()`(不全量改:轨标签一节有条历史上写在基础规则之前、靠全局钳制兜底的无效局部规则,全量 `:where()` 会把它打回无效,记观察项)。

**源静默停产修复波(issue #79,2026-09-15;版本号以实际发版为准)**:v3.57.1 上线后公共日报连续两天篇数不足(09-15 正文 4 条),排查结论是**候选池枯竭而非管线回归**——日报按设计跑(扫描 42 → 有正文 23 → ≥6.0 仅 1 条 → 近线带只有 3 条可补),三个高产源因站点改版**静默停产**(`fetch_runs` 里 `status=success` 但 `fetched_count=0`,一天都不报错),且都早于部署、`src/fetchers` 自 v3.39.2 零改动。三处真因与修法(均在生产容器/真站实证):①**IT之家 AI**——09-10 起文章 ID 跨过一百万,链接由 `/0/956/628.htm` 变成 `/1/002/341.htm`,`ithome.com/0/` 子串匹配整页对不上而列表结构未变;改按形状 `ithome\.com/\d/\d{3}/\d{3}\.htm` 匹配,百万位再进位不用再改。②**新智元**——09-05 起官网重建为静态 ASI 门户,列表/单篇都是客户端壳,单篇页自己拉 WordPress REST `/wp-json/wp/v2/posts/{id}?_embed=1`;采集器改为直接消费同一个 WP REST 列表接口(标题/时间/摘要/正文/分类/头图一次取回,不再逐篇抓详情,`link`=`asi-post.html?id=N` 只作访问地址、身份按 `guid.rendered` 计算),模板清洗兼容加粗的「新智元报道」头与加粗推广尾。③**OpenAI 正文**——09-09 起详情页退化成 130 字 RSS 摘要,真因是 Cloudflare 对**同一浏览器上下文**的二次导航恒回 403 挑战且 headless 下永不放行,而空 cookie 新上下文首访直接 200(生产实测共用上下文 1 过 3 卡、逐篇新上下文 4/4 秒过);`PlaywrightRenderer` 改为每次尝试新开上下文(浏览器进程仍复用),换进生产容器跑真实采集路径 4 篇正文全部取回。**Anthropic 官网是误判**:RSC 解析仍取到 264 条,newsroom 就是 09-01 后没发新文(sitemap 的 9 月 `lastmod` 是批量修改时间),不改代码。**工作项 4「分析 worker 排除日报记录」经检视撤回**:生产 12 条 `daily_brief_*` 分析行全部 succeeded(首次尝试瞬时失败、重试即成功,每天那条 `Article analysis failed (article_id=daily_brief_…)` 是噪声不是故障),而个人早报对订阅了公共日报的读者正是靠这条分析行(候选查询与 `article_analyses` 内连接 + `DAILY_BRIEF_READY` 重编)把日报条目带进版面——排除会让这些读者的早报永远标「分析未完成」并丢掉日报条目;「日报不经通用分析行进入早报」的专用投影记 backlog。**codex 本地协商式检视(gpt-5.6-sol)首轮 7 条全部成立、一次修完**:上述撤回(2 P1);新智元 `limit`>100 按 `page=N` 翻页补足 + 身份改用 `guid.rendered`(旧式永久链接,与改版前归档的 `source_url` 同形,老文章重抓不换 ID);IT之家匹配收紧为主机白名单 + 路径 fullmatch、crawl4ai `ithome-article` Profile 的 `^/0/` 同步放行 `/1/`;渲染器 `new_page`/`page.close` 抛错时上下文照关;文档改口。生产日报名单运维改动 `rss_the_decoder` 加入(当前最高产源,09-10 起 44 篇 33 篇 ≥6.0 却不在名单)、`rss_hn_ai` 移出(只贡献附录标题项还占扫描名额)。**排查手法**:「日报变少」先看 `fetch_runs` 里 fetched_count=0 且 success 的源,再看 `daily_brief_last_run.score_histogram`;本机与生产机各取一次列表页比字节数与文章链接数,一次分清「站点改版」与「IP 封锁」。部署后待办:09-09 起入库的 ~18 篇 OpenAI 摘要正文不会自动重抓,需一次性重渲染回填。

**Issue #7 P0 Podcast RSS 播放器基线**：`source_type=podcast` 路由到 `generic_podcast_rss`，抓取时只解析 feed 与 enclosure 元数据，不下载音频或 transcript；单集继续复用 `ArticleRecord/extensions_json`，列表/详情 API 投影轻量 `podcast` 对象。Reader 桌面与移动端新增 Podcast 容器和原生播放器；`duration_seconds > 1800` 目前只是处理提示，ASR、精华博客、TTS、持久播放器、新表与权利状态机均尚未实现，后续边界见 `docs/podcast-wave-plan.md`。

**Issue #7 P0.1 Podcast 导航收口**：桌面视图轨与移动 TabBar 均将 Podcast 紧邻文章；从 Podcast 容器进入“发现更多来源”时切换为专用“添加播客”目录的锁定方案**未进 main**;v3.53(issue #55)改为四容器统一「带形态作初值」——从哪个容器进入,发现页就先落该形态,页内仍可切,不隐藏合集/形态 seg/添加源入口。

**Issue #7 P0.2 精选播客目录**：内部「欧研观澜」样本的 36 个节目收敛为 `services.podcast_catalog` 策展目录，2026-09-03 逐源验证 RSS/XML/enclosure 后 35 个可直接复用 `generic_podcast_rss`，`Voices from DARPA` 因当前 Blubrry TLS EOF 显式阻断；应用启动时幂等安装 36 个公共节点，不覆盖管理员本地元数据、不替用户订阅。Issue #34 起，公共 Podcast 不设源级启停开关，以逻辑 source ID 进入与博客相同的采集任务、Cron、批量运行和运行历史；是否运行只由任务成员与 `CollectionJobRecord.is_active` 决定，执行边界统一解析到 `generic_podcast_rss` 与最新 SourceConfig 参数。节点检视器复用博客的源审查、运行参数和任务动作。抓取只涉及公开 RSS 元数据，不直接进入 ASR/TTS。另提供管理员 catalog 查询/幂等导入 API 与默认 dry-run 的 `scripts/import_podcast_catalog.py` 做选择性运维。新建配置统一进 `incubating` 观察期，feed 响应封顶 20 MiB、单轮 20 集；博客 RSS 与 Podcast RSS 继续以 source type/content type 分轨治理。

**v3.44 资讯分析与个人早报波**：文章可靠落盘后由租约 worker 异步生成评分（v3.48 起为新闻价值分）、摘要、内容类型、规范标签、Candidate evidence 和最多 6 个灵活展示标签；受版本控制的 96 项 Taxonomy v1 目录覆盖 Topic/Industry/Entity、Alias、层级、Entity 类型和用户可选资格，必须经目标库审核回执和人工发布。读者兴趣简化为“关注 / 屏蔽”（v3.56 起只剩关注），首次登录引导与左侧工具栏共用目录；个人早报严格冻结订阅范围，兴趣最多 50%，不足由质量补齐，无订阅不从公共池补。历史重跑走可暂停/恢复的 `full_analysis` 作业；公共日报 adapter 已随 v3.48 退役（日报直接复用分析评分）。上线与历史兼容见 `docs/taxonomy-v1-deployment.md` 和 `docs/full-analysis-backfill.md`。

**v3.45 阅读器可发现性波**(issue #9;新用户引导,样页 `docs/design/dorami-onboarding-quiet.html`):上量后新用户反馈三处「找不到」——视图轨 icon-only 读不出页面组织、阅读窗右上角灰图标被当成「没有跳原文/翻译」、轨底反馈/设置常态不可见;定性为同一根因(信息密度纪律把可发现性一并压掉),修法**结构性常态可见而非导览层**(导览本波不做:v3.22 欢迎卡已因啰嗦被拆、v3.44 首登已有兴趣引导)。三处落地:①**视图轨微标签**——钮 38×38 icon-only → 52×46「图标 + 10px/500/字距 0.08em 微标签」(目检五档并排拍板:11px/600 与源栏组头 10.5/700 几乎同分量像同层级文字,标签是图标注脚归入 kicker/组头的「小字号宽字距 faint」家族),轨宽 56→64(`--vrail`/`--rail-w`/`.app-rail` 同步),两字短名(早报/文章/动态/社交/发现/兴趣;轨底 主题/反馈/设置,管理台三字唯一例外);**呈现方式目检后改为「轨面悬停整体揭示」**(样页原拍板永久常显,目检嫌与组头同重不顺眼):常态只见居中图标(与旧轨同观感),指针进入整条轨/键盘焦点落入即**全部标签一起**淡入上浮、图标同步上移 7px 让位,离开后挂起再一起缓缓淡出——节奏刻意远慢于工作区 motion token(用户三轮拍板「太仓促」:进 720ms / 退 1400ms / 挂起 2400ms,集中在 `.reader-vrail`/`.app-rail` 的 `--rail-reveal-*` 三变量),触屏 `hover:none` 标签常显、reduced-motion 只切换;与旧逐钮 tooltip 的差别=点任何轨钮都必先进轨面,进面即见整套名字;tooltip 退为「全名长于标签」时的补充(渲染层只在此时挂);管理台应用导轨共用 `.reader-vrail-*` 类族同上标签(tabs 加 `short`)。②**轨底去 hover 滑出**——`RailUserFlyout` 退役(2026-07-24 拍板反转:hover 触发对触控板外接屏/触屏本/键盘用户都是坏的,「常态只见头像」实证让新用户找不到设置),主题/反馈/设置(admin:阅读器或管理台/主题/设置)提为常态轨钮;头像改 `RailLogoutAvatar` 只剩退出——**点击**翻面进 4 秒待确认态(红圈脉动 + Toast),再点即退,超时/Esc/点别处回落(v3.22 两击防呆原样,只去 hover 前置);反馈通知点回到反馈钮本体。③**原文/翻译下沉标题下**——「原语言 | 译为中文」二段 + 「查看原文」胶囊(v3.45.1 目检:二段原写「原文」与「查看原文」撞词却不同义——前者是本页正文语言档位、后者是跳出站外,改「原语言」;二段在前、跳原网页在后)(`.reader-pane-actions`/`.reader-tr-seg`,激活态「中文」沿 AI 渐变身份,AI 未开启只余原文,无 source_url 只余二段)放在 meta 行之下、正文之前;顶部工具条只留收藏/标读/分享(读完后的元动作,常驻不滚走);正文末尾「查看原文 ↗ · 域名」行由 v3.40 自定源专属推全站(`hostOf` 助手);移动壳顶栏译钮**不动**(桌面先行拍板)。目检两返修:**标题同译**——此前只译正文,切「中文」后大标题仍英文;`translate_article` 现并发译标题(`prompts.TRANSLATE_TITLE_SYSTEM_PROMPT` 单行输出,缓存 `extensions_json.translation_zh_title` + 标题自身指纹旁键,存量只有正文译文的缓存命中时补译一次标题,标题翻译失败回退原标题不拖垮正文),响应加 `title`;前端译文态 h1 换译名、原标题降为其下小字 `.reader-pane-title-orig`(桌面/移动同),`stripDuplicateLeadingHeading` 对译名同样剥离。**中文源不画翻译二段**——`looksChinese`(CJK > 拉丁 × 0.2,前端 utils/readerText 与后端 reader_ai.looks_chinese 同一启发式,后端用它跳过中文标题的 LLM 调用)判标题+正文开头,中文源桌面二段与移动顶栏译钮均不渲染。隔离栈 Playwright 验收(admin 导轨/读者轨/两击退出三路径/动作行/暗色/移动尾行,pageerror=0)。

**v3.48 统一新闻价值评分波**(issue #22,方案 `docs/unified-news-scoring-plan.md`;与同事讨论后拍板「直接砍掉阅读价值维度」):文章级评分只剩一把尺子——**新闻价值**(这件事有多重要),不再量「这篇写得好不好」。①入库分析提示词(`llm/article_analysis_prompt.py`)以生产实证校准过的日报 MAP 锚点体系为底改写(档位/厂商主次甄别/风向标例外/车载降权/营销重罚原样保留),新增领域相关性、非新闻体裁按影响力锚点、不按发布时间打分三条;输入补喂来源友好名与来源角色;`score_reason` 改「为什么重要/不重要」;版本键 `article-analysis-v5` / `news-value-v1`(旧结果按版本失效重跑,v3.44 未发布故不做迁移;`quality_score` 列名保留,语义以文档为准)。②**公共日报复用该评分、软依赖**:`load_stored_scores` 批量读当前尺子的分析结果,缺分的候选由 `score_candidates` 就地调**同一个** `analyze_article_with_llm`(空标签闭集)补评,结果只用于本次生成、**不写回分析表**——日报与 worker 互不知晓,总闸关着/一篇都没分析也照常出报,最坏成本等于旧 map;低于 KV `daily_brief_min_score`(默认 6.0,配置面板可调,0=不设)的候选直接 pass(不进正选不进附录,随游标跳过);选篇前字段全由分析产出机械推导(classification←genre 映射、realm←首个 topic/industry 标签、company←首个 entity、聚类 hint←分析摘要);`editorial_polish` 只为预选池十几篇逐篇写 title_cn/要点/点评(`EDITORIAL_SYSTEM_PROMPT`=原 MAP 去掉 score/classification),失败保留分析摘要作要点、不降附录;`extensions.items` 形状不变(神灯零感知,`score_reason` 仍不进 items);进度阶段 collecting/scoring/selecting/editing/reducing/persisting,`last_run` 增 scored_stored/scored_inline/below_threshold/min_score;退役 MAP 逐篇打分、adapter 开关与 shadow 指标 KV,用量 purpose `daily_brief_map`→`daily_brief_editorial`(补评计入 `article_analysis`,归属触发者/system)。③个人早报:门槛 `PERSONAL_DIGEST_MIN_QUALITY_SCORE` 7.0→5.0(新尺子下博客/教程落 4～6,7.0 会让博客订阅者早报常态为空);**去等待**——打开/重编/08:30 三条路都立即用库里现有内容生成,`sync_stale`/`analysis_incomplete` 按生成时刻事实落成版面标记,`check_after`/`deadline_at` 不再门控(列保留作记录),前端等待态只剩「正在编排」;backlog 的「早报 API 测试时段依赖」随之消失。④读者面统称「新闻价值」:速读卡/早报卡/管理面抽屉与弹窗注脚、aria-label、台账筛选档位(7+ 重要资讯/8+ 头条候选/9+ 重大事件),免责文案四处手写副本收敛为 `SCORE_DISCLAIMER` 常量。明确不做:阅读价值第二轴、延期名单/附录未评估、覆盖率等待、点评并入分析调用、列改名;黄金集门禁记 backlog。**同 PR 收口审视**(`docs/analysis-brief-review-plan.md`,链路落地后逐环节审视、十项全做):①补评喂 worker 同一套 `load_relevant_active_tags` 闭集,topic/entity 只取规范名 + entities 名(两条来路 realm 配额键同一词汇);②无正文候选按标题走同一把尺子——过线进附录、低于门槛 pass(附录不再是无门槛后门);③`scan_analysis_backfill` 改轻列 LEFT JOIN 只挑「无分析行 / skipped / 版本过期 succeeded」三类行,不再每分钟载入 7 天正文逐篇比哈希(`scanned` 语义=需要动作的行数);④版本键过期的重跑每 tick 最多 `VERSION_REFRESH_PER_CYCLE=16` 篇慢滴、新文章永远优先(指标 `version_stale` 透出剩余);⑤编辑输入带分析摘要与 `score_reason` 作已知事实;⑥`last_run` 增 `score_histogram`/`scored_inline_pending`,运维分析面板加分数分布迷你柱状图,日报面板对「补评占比 ≥ 50%」给黄字提示;⑦上线手册补「开总闸 / cron 排在 worker 之后 / 版本重跑慢滴」三条;⑧`precluster_same_event` 同厂商高相似标题(数字串一致、短题不参与)机械并簇,LLM 失败时即兜底;⑨跨天查重对照物每条附要点首句(`entries`,`titles` 保留)。

**v3.55 管理面用户隐私与账户增长波**(issue #31,方案 `docs/admin-root-admin-and-account-growth.md`;负责人收窄为两点):①**用户明细仅根管理员**——多管理员平权之上唯一例外,判据「活跃管理员里的 `admin`,缺则最早活跃管理员」不设权限列零迁移,`/api/accounts`·`/api/admin/accounts` 中间件 403、`ai-usage`/`overview` 按会话剥用户维度、`runtime.root_admin` 能力位、根管理员不可降级停用删除;②**账户增长曲线**——`GET /api/admin/account-growth` 按创建日 GROUP BY(带服务端 `as_of_day`),前端按运维页头时间窗逐日折桶,一张组合图(柱=新增 左轴 / 折线=累计 右轴,用户验收拍板合一张、撤独立粒度 seg),现存账户口径。有意保留给全体管理员:反馈收件箱/自定源创建者列/非账户类操作审计。codex 本地协商式检视(新流程首次)返修三处 P1:自定源含凭证 feed 地址对非根管理员遮罩为 `scheme://host/…`(url/params/params_json 三处同口径,`user_rss_` 行的通用 source-config 写口非根 403)、账户操作审计行(`/api/accounts%`)非根整行隐藏(count/items/q 同口径,只有根能做账户操作故互查无意义)、根身份转移后前端经 `onRefreshRuntime` 返回值决定是否重拉名单+`rootAdmin` 翻转 effect 收界面;P2:`as_of_day` 服务端今天、口径 hint、uv.lock 项目 stanza。关键节点标注与其它指标曲线记 backlog。

**落地页早报波(issue #56,2026-09-14;版本号以实际发版为准)**:读者登录 / 刷新落地 = **个人早报页**而非空白文章容器——`ReaderTab` 的 `briefOpen` 与 `MobileReader` 的落地 Tab 初值按「`personalDigestEnabled` 且未带站内深链」取早报(深链 `#/reader/a/{id}` 与引用跳转仍压过落地页;admin 的 `default_surface` 只决定进管理台还是阅读器,进阅读器后同样落早报;总闸关闭维持文章容器;不记住上次停留,首屏恒为早报,`briefReturn` 已能送回上次列表);移动 TabBar 早报居首。**新用户旅程拍板 B「早报前置、引导内嵌」**(v3.22 欢迎卡被嫌啰嗦的经验:首屏给内容比给表单更能说明早报是什么):首登强制落发现页兴趣段的 effect 退役,引导横幅(`.interest-onb.brief-onb`,与发现页兴趣段同一形制)挂早报页报头之上——「设置兴趣」→ 发现页兴趣段,「稍后再说」= 专用 `POST /api/reader/interests/onboarding/complete`(无 body,只做引导完成的条件迁移、不碰兴趣集合;横幅与发现钮红点一起消失);**首登引导首次完成且已有有效兴趣时后端就地重编一次**——兴趣页「完成」走 `PUT /api/reader/interests`(兴趣替换与迁移同事务)、横幅「稍后再说」走上述 POST(迁移成功且已有 active 标签的兴趣时),两者共用 `_rebuild_after_onboarding`:迁移是单条条件 UPDATE(`WHERE interest_onboarding_completed_at IS NULL`,rowcount 判抢占,并发只一个重编)、只对 `role=user`、经 `personal_briefs._ensure(reason=INTEREST_CHANGED)` 同步生成;两端点响应统一 `brief_rebuild_status`(None=未触发或无可编排内容 / ready / degraded / pending / generating / failed,封闭值域,异常回滚后记 failed)+ `brief_rebuilt`(仅 ready|degraded 为真),失败不影响兴趣保存;这是 v3.51.1「兴趣变更只记录不重编」的唯一显式例外,零兴趣跳过/再次保存都不触发;早报空态与失败态补「先去看文章」出口(首屏不能是死胡同)。**默认订阅名单重定并改 KV 可配**(`services/reader_defaults.py`):按生产近两周逐源 ≥6.0 产量(The Decoder 51/71、IT之家 36/78、量子位 30/69、X·OpenAI 21/32、OpenAI 15/27)与中文 / 头部官方优先重定 9 源——日报 + 量子位 / IT之家 AI / 新智元 + The Decoder + OpenAI / Anthropic / DeepMind 官方 + X·OpenAI,移除 Claude Code Changelog(均分 4.45,对早报零贡献),播客暂不进(内网音频可达性未验证);HN·AI / r/LocalLLaMA / HF Daily Papers / GitHub Trending 四个高量零命中源绝不进默认。代码缺省 `DEFAULT_SUBSCRIPTION_SOURCE_IDS` + KV `reader_default_source_ids`(JSON 数组)整体覆盖,空数组 = 不播种,删 KV 回落;`GET/POST /api/admin/reader-defaults`(`source_ids:null` 恢复缺省;未知 id / `user_rss_` 私有源 400;入审计带条数与前三名)+ 运维 → 内容「新账号默认订阅」卡(chip 名单 + 加入下拉 + 恢复缺省);只影响播种时刻,存量账号不回填、退订过的不复活(`app.py` 同名常量保留为兼容再导出)。早报候选池本就排除公共日报、不按形态排除,X 与播客都能进质量通道。**codex 本地协商式检视(gpt-5.6-sol)一轮 9 条全部成立、一次修完**:首登迁移改单条条件 UPDATE(rowcount 判抢占,并发双 PUT 只一个重编)且只对 `role=user`;响应改 `brief_rebuild_status`(None/ready/degraded/pending/generating/failed,异常记 failed)+ `brief_rebuilt` 只在 ready|degraded 为真,前端 Toast 三档(`utils/briefRebuild`);「稍后再说」改专用 `POST /api/reader/interests/onboarding/complete`(无 body,不碰兴趣集合,迁移成功且已有兴趣时走同一重编例外)并先 `flushPendingInterestSaves()` 等本标签页排队的兴趣保存落定(保存链抽成 `utils/interestSaveQueue.js`),跳过后早报页立即重拉;默认名单拒绝模板源、GET 带与 POST 同域的 `candidates` 供下拉(不再借读者目录)、审计摘要按落库口径计数;管理卡 `defaultsBusy` 互斥 + 代次守卫 + 失败复位 select(前端无组件测试框架,以隔离栈 Playwright 交错用例验证)。

**个人早报三处收口(issue #74,2026-09-14;方案 `docs/personal-brief-grid-and-sections.md`,样页 `docs/design/dorami-brief-grid-quiet.html`)**:全新账号实测抓出三处——①**公共日报混入个人早报**:候选查询只在全站分支排除 `dorami_daily_brief`,订阅池分支没有,而默认订阅第一项就是日报、分析补偿扫描也不排除 `daily_brief` 体裁,聚合稿在新闻价值尺子下稳进质量通道;修法=范围层一刀切(`resolve_personal_digest_source_ids` 剔除,报头「订阅的 N 个来源」不再数它)+ 候选查询两分支与降级兜底的机械双保险 + 退役 `DAILY_BRIEF_READY` 触发链(`notify_public_daily_brief_ready`、日报落盘后的调用、`calculate_due_source_ids` 日报特例;枚举值为历史行保留),默认订阅名单与分析入队层不动。②**板块顺序随排名漂移**:此前 `position` 是通道顺序的副产品、前端按首次出现建组;现 `digest_selection.SECTION_ORDER` 固定(重大事件 → 模型发布 → 行业资讯 → 开源动态 → 学术论文 → 工程实践 → 观点洞察 → 技术大会 → 社交动态 → 资讯聚合 → 其它 → 订阅源最新更新),落库前 `_assign_positions` 板块按序、板块内按分数降序(缺分殿后、同分保序,通道不再影响顺序);记录重排前刻意不 `session.add`——三段构建之间的查询会 autoflush,先按临时 position 写入再改写会撞 `(edition_id, position)` 唯一键;历史版本不回填、前端分组时按快照分数再排一次。③**网格残行**(「3 + 1」留白):样页六策略对照后拍板用户提出的**分值驱动**(策略 F)——网格改 6 等分单元,`utils/briefGrid.planSectionSpans` 按分数给每张卡 `--span`(2 小卡 / 3 半宽 / 4 ⅔ / 6 通栏):分数 ≥ 9.0 独占一行(与「9+ 完整渐变」同档)、并排分差 ≥ 1.0 用 ⅔ + ⅓ 否则各半、余 1 头卡高出一档才通栏否则头两行 2 + 2、余 2 头对配对、整行恒三小卡;每一处横跨都有理由,「2 + 2」只在四张分数都接近时出现;头版恒通栏,≥ ⅔ 才分栏(⅔ 卡右栏 150px),列数由 `ResizeObserver` 量报纸面内容宽给出(3 / 2 / 1,断点与旧 auto-fill 一致),两列只保留通栏 / 各半、单列全通栏(移动壳 CSS 显式 `1 / -1` 防 span 撑出隐式列),骨架屏固定「通栏 + 一行小卡」;T / Δ 是常量不是旋钮。

**图片理解波(issue #69,分支 `feat/issue-69-vision`;方案 `docs/image-understanding-wave-plan.md`)**:平台补「视觉模型」第三档位,文章配图被识别成**结构化文字说明**并当作正文补充喂给所有理解文章的链路——入库分析(评分/摘要/打标)、公共日报(补评 + 编辑)、读者问答(显式篇目缺则识 / 检索档只读缓存)与速读兜底;**未配置视觉模型时说明文本恒为空,各链路与既有逐字一致**,读者面没有任何「配图已识别」标记,差异只体现在回答与摘要「看得见图里的数字」。模型 = DeepSeek 2026-09-10 发布的 `deepseek-flash`(V4.1-Flash 原生多模态;`deepseek-v4-pro` 不支持图片),同端点同 api_key 只多填一个模型名。**接线**:`[llm] vision_model` / env `DORAMI_LLM_VISION_MODEL` / KV `llm_vision_model`(凭据注册表 + 设置柜 LLM 卡输入框 + `/api/llm/config` 透传),`LLMConfig.for_vision()` 换模型名并**显式关闭思考**(与 aux 的「不发送」不同:deepseek-flash 默认开思考,识图短 JSON 会先被思考吃满 max_tokens 空产,真机实测 finish_reason=length;不支持该参数的端点由客户端 400 降级);`/api/llm/config/test` 在 vision_model 非空时附 `vision` 子结果(内置程序生成的纯色 PNG 问主色,主通视觉不通仍 200 由前端如实转述)。**客户端**:`ChatMessage.content: str | list[part]` + `text_part/image_part/image_data_url` 助手,图片只能在 user 消息;一律媒体库本地文件 → base64 data URL(内网 MaaS 拉不到公网图链),detail=high。**存储** `services/image_insights.py` + `ImageInsightRecord`(表 `image_insights`,迁移 `df8f95fe0439`):**一图一行、主键 = 图片字节 sha256**(与媒体库内容去重单元同源,同图跨 URL 跨文章零重复调用);**无逐文章状态**——文章 → `extract_image_urls` → `media_assets.url_hash` → `content_hash` → 本表三跳纯查询,正文改动图链自然变、不给 `extensions_json` 多添写者;字段 kind(chart/table/screenshot/diagram/photo/logo/other)/relevant(装饰图 false 不进上下文)/caption/details(表格逐行 Markdown 转写、图表读数、截图文字、架构组件关系)/ocr_text,失败行按 fail_count 退避负缓存。**护栏**:排除 `podcast_episode`(封面)与不可外送源(`source_content_may_leave_deployment` 同一道闸);媒体库关即整体关;每篇 ≤4(KV `image_insight_max_per_article`,0=关)按正文出现位置取;任一边 <200px 跳过(自解析 PNG/JPEG/GIF/WebP 头,无 Pillow);单图 >8MB 跳过。**两种取法**:`cached_notes_map`(同步零 LLM)/ `ensure_notes`(缺则识,整体预算 worker 20s / 问答 15s,超预算的在途任务转后台跑完写缓存,失败一律退回缓存绝不抛);**能力门「关 = 关」**(检视 F1):`enabled = 视觉配置 ∧ 旋钮>0 ∧ 媒体库开` 同时管读缓存与识别,两种取法都要 `llm_config`,关掉后旧缓存不再进任何链路、重开零调用回归;**全局并发信号量**(检视 F2)= `[llm] map_concurrency`,包住读整图 + base64 + 调用,筛选阶段只读 256KB 文件头判尺寸。**接入**:`AnalysisInput.image_notes`——`process_claimed_analysis` 在 `_input_for` 后、评分调用前经 `image_notes_provider`(可注入)取说明,user prompt 的 `<untrusted_article>` 多 `image_notes` 字段、system prompt **仅有说明时**追加【图片内容使用规则】(评分锚点不变——图里有 SOTA 表不等于行业级事件),`analysis_input_hash` 随之变化、**prompt_version 不 bump**(存量不重跑,issue 拍板不回填);日报 `_score_one`/`_polish_one` ensure(编辑 prompt 加 `image_notes`);问答 `build_numbered_context(notes_by_id)` 的 per_article 是**正文 + 说明的组合预算**(检视 F4:说明份额 min(per_article, max(1000, min(6000, per_article//2))),正文拿扣除实际说明后的余额,篇数与 main 守恒)、纯图推文也算有内容,说明预算全站单一默认 `IMAGE_NOTES_MAX_CHARS=6000`(输入字符数,不复用 max_tokens;单图识别输出上限才跟 max_tokens 走)、渲染层公平截断(先保每张图标题行再均分正文,验收实测顺序填充会把文末对比图整张截掉;首条标题也受硬预算),`assemble_reader_context(llm_config, usage_meta)` 显式档 ensure、`reader_search` 检索档 cached-only;**注入边界**(检视 F8):识图 prompt 把标题 / alt / 邻近正文 / 图片声明为资料非指令,下游 editorial / summarize / QA **只在实际带说明时**追加 `IMAGE_NOTES_UNTRUSTED_RULE`,QA 依据 `sources[].has_image_notes` 显式布尔而非嗅探正文;**隐藏源闸**(检视 F3):translate / summarize / ask 显式档对非 admin 会话按隐藏名单同形 404(main 既有缺口一并堵,admin 只豁免隐藏层);worker 侧识图与评分共享一个 deadline(检视 F6,识图耗尽预算直接 timeout 不调评分);脱敏单点 `services/error_redaction.py`(401/403 正文整体丢弃、`sk-`/Bearer/`provided:` 形状打码,article_analysis 与 image_insights 共用);计量用途 `image_insight` 不进读者预算三处;`/api/admin/analysis/metrics` 加 `image_insights` 块;独立兼容修复:主模型 ping `max_tokens` 16→256(默认开思考的现役模型 16 会空产误报连接失败)、客户端两类 400 协议降级不再消耗重试次数。真机(本机 dev key,2026-09-14):World Labs Atlas 文 4 图 5s 全部识出(推文截图双语原文/架构图组件/偏好对比表逐行 75–94%),Gemini 3.8 Flash 文单张 13×7 基准表逐行转写 8s,有图分析的 summary 多出价格与各基准领先/落后项而分数 7.8→7.6 基本不动。**边界**:不回填存量;Archive Sync 不同步 `image_insights`(内网检索档拿不到说明);识别失败不阻断也不重跑分析;同图跨文章复用首篇语境的说明;不做图片生成/以图搜图/OCR 专用引擎。测试 `tests/test_image_insights.py` 18 项。

**v3.54 阅读面小特性波**(issue #54 + #55;样页 `docs/design/dorami-score-tiers-quiet.html`)。**#54 新闻价值分按分值分档着色**:此前条目列/早报卡/速读卡三处分数全挂同一 AI 渐变,9.2 与 4.5 视觉分量一样、分数只能靠读数字才有信息。现按值分档只改颜色(字号/字重/字体/位置不动):灰线 **6.0**(取自评分尺子自身的档界:提示词把 1.0–5.9 定为泛泛/宣传/重复、6.0 起才「有用或深入」;公共日报默认入选线恰好同值,但**不随 KV `daily_brief_min_score` 联动**——视觉刻度随尺子版本走不随运营旋钮走,与台账 7+/8+/9+ 固定档位同口径;codex 检视 P2 拍板)以下退成与旁边元信息同一个 `--dorami-faint` 灰、不画身份色;其上按整数分一档(6.x/7.x/8.x/9+ → `is-score-1..4`),渐变两端色各自向 faint `color-mix`(混合比 85/60/30/0%,「陡峭」档——13px 小字下平缓档拉不开),9+ 是完整渐变——「有渐变 = 值得看」本身成了信息,v3.33「见渐变即 AI」不破。用户拍板放权后自主定灰线/分档/跨度(样页三组控件可对比连续插值与 5.0 灰线);前端纪律 `docs/frontend/conventions.md` 的「不绑阈值着色」同步改写(codex 检视 P1)。档位判定单点 `utils/analysis.scoreTierClass`(无分数返回空串,缺分是缺席不是 0 分);CSS:`@layer` 内 `.ai-grad-text.is-score-*` 改 `background-image`(必须分别混两个端色,对渐变整体 filter 会连 wash 底一起变),`--score-mix` 三档变量放文件尾未分层区供读者面渐变与管理面纯色共用;管理面台账 chip / 抽屉 / 弹窗无 AI 身份纪律,只把 accent-ink 向 faint 混、灰线以下同退 faint。速读卡里程表的档位类挂在每位数字自身(渐变落元素自身,挂外层画不进 overflow 格子——v3.45.1 同坑)。**#55 发现页带入容器初始形态**:发现页形态 seg 原是页内局部态、初值恒「全部」,从「动态」点「发现更多来源」落到混着四形态的全站目录还得再点一次;现 `useReaderState.discoverShape` 受控,`openDiscover({shape})` 缺省取当前容器(经既有 `modeRef` 读,保持稳定引用——它是零订阅自动进入 effect 的依赖,若随 mode 变化零订阅者从发现页预览某源会被立刻弹回),是初值不是锁定;带形态的入口:视图轨 Compass / 源栏底「发现更多来源」/ 条目列与阅读窗空态 / 移动源抽屉;落「全部」的非容器入口:零订阅自动进入 / 早报页「管理订阅」/ 移动「我的」/ 首登兴趣引导。顺修:「发现更多来源」类入口段位必须落「源」(`openDiscoverSources`)——发现页段位是粘性的,首登引导落过兴趣段后一直停在那,明说「来源」的入口开到标签清单是错的;Compass 全局入口沿用上次段位。

**v3.56 兴趣去屏蔽波**(issue #27 第二波,2026-09-14 拍板):兴趣只剩「关注」一极,屏蔽整体退役。讨论「兴趣轴是否露屏蔽」时把问题上提一层——不关注是中性而非负向,但在 55 个策展源、96 项目录的窄语料里,细粒度负向工具的边际价值远低于通用阅读器;而屏蔽的成本不是一次性的,是每个新消费方都要交的税(早报「有屏蔽时等打标完成」的就绪依赖、重大事件通道的硬排除例外、每次列表请求的逐条 `interest_muted` 标注与折叠行、兴趣页三态 UI、backlog 里 feed/MCP 屏蔽要改交付契约、问答先验也要考虑屏蔽),趁消费方只有早报与列表两处撤除成本最低。落地:迁移 `c4d8e2f6a1b3` 删存量 mute 行 + `stance` CHECK 收窄为 `follow`(列保留作兼容,与 `priority` 同类);`InterestStance` 只剩 FOLLOW、`UserInterestDTO.stance` 缺省 follow;`PUT /api/reader/interests` 不再接受/回显 stance;`reader_interests.InterestMap` 只有 followed、`annotate_interest` 只标 `interest_hits`;`digest_selection.eligible_for_selection`/`select_breaking_events` 去 muted 参数;`personal_digest` 撤 `has_mutes`→`require_tagging_complete` 整条就绪依赖(候选/降级区不再等打标)、`selection_stats` 去 `muted_count`;`taxonomy._merge_interest_rows` 只合并 priority;前端撤 `MutedFoldRow`/折叠展开态/`.is-muted` 降调/`listPlan` 只剩分组、兴趣页撤屏蔽幽灵钮与三态、台账「兴趣 n · 屏蔽 n」→「兴趣 n」。升级前冻结的 pending 快照若残留 mute 行按不存在处理;被删屏蔽行会改变兴趣版本哈希,今日早报如实标 `interest_stale`。**流程同波起变更**:PR 后不再 GitHub @codex,改本地 Herdr 起 codex 协商式检视(见「协作流程」节)。

**v3.52 兴趣即透镜波**(issue #27 第一波,方案 `docs/interest-lens-wave-plan.md`,样页五稿 `docs/design/dorami-interest-axis-quiet.html`[现行]与四稿 `dorami-interest-lens-quiet.html`;设计讨论同日完成,四次被否或推翻:一稿「关注动态篇数 + 主题流阅读面」[篇数不明智、混读各形态重犯「今日」混合流的错]、二稿「源栏兴趣组」[标签混进来源清单,栏头名不副实]、三稿「作用域推翻 = 订阅 ∪ 关注隐式并集」[「文章为什么出现」两个隐式解释,永久税]、四稿「左栏三谓词多选 AND 联合」[实现后目检推翻:选中某个源之后「订阅」开关空转——订阅集 ∩ 单源 = 单源;根因是「订阅」与来源列表约束同一个变量,不是正交的第四维];宏观拍板:兴趣先做透镜不做渠道,关注对象以实体为主,等数据再定)。**模型(五稿,用户 idea)**:左栏 = **一根轴**——栏头 = 容器名 + 二段互斥切换 **订阅 | 兴趣**,决定其下列什么:订阅 → 已订阅源分组(今天的源栏),兴趣 → 关注的标签按目录面分组(与源行同一套行语法);点栏头段 = 该轴全集,点一行 = 收窄到一项(`activeSourceId` / `activeTagId` 互斥,切轴即清);整栏只有一种语义(单选:浮白 + 弱高程);**兴趣轴范围 = 全站可见源**(只在订阅内就退化成订阅子集,失去「看见订阅之外」);「全站」浏览态取消;**收藏挪到列头**(与 全部 / 未读 seg 并排的一枚星:逐篇状态与「按什么切分」正交,可与任一轴取值组合;反转 v3.10「收藏进源栏」——当时源栏没有轴语义;社交容器无标签不出轴切换,星在社交头);轴 + 收藏按用户按容器记住(四稿三布尔自动迁移),下钻项不记;点源 / 按 id 打开一篇 = 临时进入来源轴不落盘;**列头回显**订阅 / 源名 / 兴趣 + faint「全站」/ 标签名,收藏开着副文案「收藏」;屏蔽不是开关是全局透镜(条目折成「已屏蔽 · 标签名 · 展开」一行,无数字);命中胶囊也是透镜(顶行一枚,只写命中的那个兴趣标签,与分析主签同名时顶替;**单标签视图不挂**——每行都因同一个标签而在,与分析主签同名时也让位);订阅外文章在兴趣轴直接出现(faint「未订阅」悬停翻「+ 订阅」,阅读窗眉头胶囊);未读仍是 seg,兴趣轴按逐篇读态;交付面不动(早报冻结订阅源 + 兴趣标签,feed/MCP 仍订阅域)。**兴趣编辑面并入发现页第三段「源 · 合集 · 兴趣」**,视图轨撤兴趣钮,首登引导 = 发现页兴趣段顶部横幅(不锁页,完成即落早报),兴趣页卡片篇数撤除;没设兴趣时切到兴趣轴 = 左栏一句引导直落发现页兴趣段;口径统一叫「兴趣」(对单个标签的动作仍叫关注 / 屏蔽)。**实现**:`services/reader_interests.py`(兴趣 / 收藏谓词 exists + 逐条 `interest_hits`/`interest_muted` 标注;准入门槛 `INTEREST_MATCH_MIN_RELEVANCE=0.8` 只认主标签或相关度过线,屏蔽任一指派即命中),`GET /api/articles` 加 `interest_scope`/`interest_tag_id`(下钻只认关注中的那一枚,非关注 id 显式空集)/`favorite_scope`/`with_interest`,`reader_state.unread_filter_condition(include_uncursored)`;前端 `useReaderState` 的 `scope = {axis, favorite}` + `activeTagId`(`buildListFilters` 单一拼装点、`setAxis`/`goTag`/`toggleFavoriteScope`/`goSource` 临时来源轴/`goContainerAll` 读回、`interestGroups`/`listSubtitle`/`showInterestHit`),`utils/listPlan.js` 折叠计划,`ReaderTab` 的 `AxisSeg`/`TagRows`/`MutedFoldRow`(桌面源栏与移动抽屉共用)+ 列头 `.reader-fav-toggle`,`SocialFlow` 头部星,`DiscoverPage` 段位受控 + `interestsPanel`,`InterestPage` `embedded` 变体。**v3.52.2 返修**:兴趣轴的「全部标读」按范围逐篇写行(`POST /api/reader/mark-scope-read`,`reader_state.mark_ids_read` 一条 ON CONFLICT 写整批、上限 2000 取最新、不推水位——兴趣是跨源透镜,推水位越出读者看见的范围;不做兴趣水位:一篇命中多标签 + 与订阅同一批文章两个视角,按标签设水位会跨轴不一致)+ 无水位源未读时效下限 `UNCURSORED_UNREAD_MAX_AGE_DAYS=30`(没有逐篇行且入库超 30 天视为已读,过滤与标注同尺、显式未读压过;留存清理删无水位源窗外已读行,表大小从线性变常量);两根轴同一枚钮,hook 按轴分流。**待拍板**:兴趣轴列表列什么(仅关注 / 露屏蔽)与无兴趣引导形态(现为占位)。本波明确不做:「主要来源」脚部、早报兴趣半取全站、feed/MCP 屏蔽、问答先验、隐式建议关注(列头标签 chip 行由兴趣轴下钻承接,不再需要)。

**v3.50 个人早报「重大事件」通道波**(issue #33 §2,方案 `docs/personal-brief-breaking-lane-plan.md`;拍板「全按推荐实施」):个人早报第三条通道 `breaking`,不看订阅范围只回答「今天有没有对所有读者都是头条的事」。方案由生产 7 天实证(与 §1 评分检视同一份 699 篇样本)定形:官方 ≥9.0 只有 2 个事件,而 **Fable 5.1 的 Anthropic 官博不在归档里**(只以媒体/个人形态 9.2 存在)——issue 原案「官方-only」会漏掉自己举的例子(v3.35「官源缺位」同病);单源高分噪声真实存在(「Fable 遭黑客破解」9.2 仅一家);`DuplicateGroupMemberRecord` 从未写入、URL/标题键分不出 4 条 X·OpenAI 推文是同一件事。故准入双路(官方一手 ≥T | ≥2 个不同来源共享 `entity.*` 标签 ≥T−0.5 且至少一条 ≥T)、并查集按实体码归并同事件、代表官方>非社交>分数>发布更早、24h 窗不回退、每期 ≤N 额外于 target 之上、同实体前 2 个完成日期上过即抑制(GPT-6 三天连续 ≥9.5 官方稿实证)、mute 硬排除、隐藏源/`user_rss_`/日报记录排除、空订阅仍空。落地:`SelectionLane.BREAKING` + CHECK 约束迁移 `c7e1a9d4b2f6`;`digest_selection.select_breaking_events` 纯函数;`personal_digest` 候选取数抽成两段共用 + `load_breaking_candidates`/`_source_role_and_shape`/`_previous_breaking_entities`,KV 旋钮 `personal_digest_breaking_min_score`(9.0)/`_max_items`(2,0=关)经 `/api/admin/analysis/config` 与 AdminTaxonomyPanel「早报重大事件通道」卡;条目 `ranking_features_json` 记 `breaking{basis,source_count,subscribed}` 与 `event_entity_codes`,API 条目透出 `breaking`;前端「重大事件」chip 与报头「N 条重大事件」前置、精选统计排除头条。回放预期:09-02 期 Fable 5.1(印证)、09-05 期 GPT-6(官方,代表取官博非推文)+NVIDIA 收购 HF、之后两天 GPT-6 被抑制;World Labs Atlas 因实体目录无该玩家不进(诚实边界)。§3/§4/§5 后续 PR。

**v3.51 Podcast 简介初评波**(issue #43):`podcast_episode` 继续复用全站唯一 `quality_score`,但按 `podcast-show-notes-v1/news-value-v2-podcast-v1` 专用补丁评估嘉宾权威、议题及时性、新颖度、证据深度、观点互补与实践价值；名气/热度/重复信息不得单独抬到 8 分。RSS 采集归一 Podcasting 2.0 `person`、author 及明确嘉宾，worker 冻结 7 天规范 topic/entity 热度快照(不同来源数/最高分/最近时间/公共日报)并持久化 `analysis_basis`、实际输入 SHA-256、可选 transcript artifact、版本和内部诊断。Reader 与知识台账标“简介初评”；旧 premium guide 只读权威分；Archive Sync v2 原样复制新增字段。全文/ASR 重评、阈值回填和 solo_deep 分别留给 #44–#46。

**运维面播客与分析标签重构波(issue #76,分支 `feat/issue-76-admin-surface-refactor`,PR #88;版本号以实际发版为准;方案 `docs/admin-surface-refactor-plan.md`,样页 `docs/design/dorami-admin-podcast-taxonomy-quiet.html`)**:播客(#43–#46)与标签(v3.44)两波 codex 开发的管理面拉齐到 2026-09 管理面纪律。Claude × codex 各自独立检视后归并 31 条(P1×10 / P2×12 / P3×9,撤回 2:全局 CustomEvent 是仓内既有模式、阶段章不加 `role=status`),样页拍板落码。**拍板**:①「标签」seg 改**「分析与标签」**,页内两区「分析链路」(4 枚 ledger-switch 开关板 / 指标 KPI / 分数分布 / 历史文章完整分析)与「标签治理」(目录版本 KPI / **规范标签 ∪ 候选 统一总账** + 标签·候选抽屉 / 新建标签 form-sheet);早报重大事件 / 订阅外兴趣两旋钮 + 兴趣目录 Top N 迁到「内容」子页新立**「早报与兴趣」**区(它们是 `personal_digest_*` 分发策略,不是分析 worker 或目录的生产状态)。②标签列表 = 一张表 + 抽屉,不设独立编辑列,点整行任意位置开抽屉。③早报策略 = 表单卡,各一枚 secondary 保存,零说明句;播客门槛行同取显式保存。④回填默认档 `missing_or_outdated`。⑤内容子页隐藏页头时间窗(该页三个数据源都不接时间窗参数),其它子页只写「时间窗」。⑥音频表节目列由后端补 `episode_title / source_name`。⑦「内容」子页新立**「播客」**分区 = KPI 六格 → 处理参数开关板(优质门槛可调 / 简介处理线固定 / ASR 配额 chip 深链设置)→ **单集处理表** → **中文精简音频表**,三处整行可点开同一个**单集抽屉**(评分与判定 / 六步处理时间线 / 产物 / 逐字稿 / 强制全文·强制 TTS)。目检返修:判定拆回「阶段章(未初评 / 未入选 / 待全文 / 处理中 / 全文完成 / 待对账 / 失败)+ 章下一行原因」与「判定章只表优质线(优质 / 未达门槛 / 未终评)」,⋯ 详情钮撤除,两表卡头「单集处理」「中文精简音频」标明职责,「安全回收」归音频卡头。**纪律落地**:列头即操作(ThSearch / ThFilter / ThSort)取代独立 filterbar 与 select;rowact 小图标 hover 浮现,强制 / 合并 / 废弃 / 拒绝 / 取消 过 confirm;零常驻说明句;状态章中文化(`utils/taxonomyLabels.js`,未知值「未知状态(raw)」);播客处理状态**归一层** `utils/podcastProcessing.js`(阶段 / 判定 / TTS 谓词 + 章文案 + 动作 + 审计 reason + toast,面板 / 台账 / 抽屉 / 命令组装同源,**待对账不再折进失败**);共享 `admin/Kpi.jsx`(`Kpi` / `KpiState`)、`Pager.TableFoot`、`admin/StaleNotice.jsx`(旧快照存在时刷新失败 / 未接入就地提示);内容 KPI 独立错误态不再挡住同页分区;`refreshTick` 透传各分区(M15 回访刷新);`api.js` 网络失败统一「对象 + 原因 + 下一步」。**后端接口卫生(旧参数与键全兼容)**:任务表 `q/stage/verdict/tts/sort/order` + `breakdown` + 单集详情端点 `GET /api/admin/podcast-premium-tasks/{id}`(时间线只取记录里真有的时间戳);音频表 `offset/q/sort` + `total` + `episode_title/source_name`;`GET /api/admin/taxonomy/ledger` 统一总账(近 7 天命中排序、别名可搜)+ 标签 / 候选详情端点;回填列表 `offset` + `total`。**codex 本地协商式复检**:R2 逐条核对 31 条 + 拍板记录,2 条 P1 返修接受并修——「可选」开关改 Zap / ZapOff 形状对(账户表 AI 列先例,不改行内 switch:样页拍板小图标且 46px 行里 34×19 开关重一档)、loader 保留旧快照后刷新失败被静默 → `StaleNotice`;R3 收口 404 → 「未接入」统一映射与回填卡按快照挂载。观察期(backlog):`DataTab` 行章与阅读抽屉仍用 `utils/analysis.js` 两套投影(下一波让旧投影成为归一层适配器)、`handleApiError` 对非 JSON HTTP 错误补状态类别、标签总账端点 Python 侧合并排序再切页(目录百级可接受)。验证:pytest 全套 + `tests/test_admin_surface_refactor.py` 7 项;前端 lint / build / `node --test` 56;隔离栈 Playwright(临时 ini + 播种沙箱库 + `VITE_PROXY_TARGET`)表头 36 / 行 46 / 整行开抽屉 / 行内动作不冒泡 / pageerror 0;用户本地目检通过。

**弹窗遮罩关闭判定收口(issue #104,分支 `feat/issue-104-modal-overlay-close`;版本号以实际发版为准)**:用户在「添加自定源」里拖选文字、松手落到遮罩,弹窗立刻关、输入全丢。根因是 DOM `click` 派发到 mousedown 与 mouseup 目标的最近公共祖先——面板内按下、遮罩上松开,公共祖先就是遮罩,裸 `onClick={onClose}` 被触发;7acd710 曾把共用外壳 `Modal.jsx` 与设置柜改成 `onMouseDown` 关闭,但手写遮罩的弹窗没迁上来(添加自定源 / 运维新建账户 / 重置密码 / 全站确认框,外加 issue 之后长出的标签总账新建标签 sheet 共五处),且外壳 `closeOnOverlay` 默认关、直到 PWA 波才有第一个调用方——那段修复此前从未真正生效。**拍板(按 issue 方案建议实施)**:①判定改为「按下与松开同在遮罩」——`utils/overlayClose.js` 纯状态机:pointerdown 记是否落在遮罩本身 → pointerup 再核 → click 时两者皆真且目标仍是遮罩才算,pointercancel(触屏滚动接管)清零、只认主键;比纯 mousedown 稳(遮罩按下拖进面板松手也不关),且不需要面板 `stopPropagation`(React 的 stopPropagation 会拦掉 document 级冒泡监听,弹窗里嵌右键菜单 / 分享浮层同款「点外关闭」机制不再失灵);状态机只判定不动作(`shouldClose`),`Modal` 用 `useState` 惰性初始化持有——`react-hooks/refs` 不允许渲染期读 ref。②判定只落 `Modal.jsx` 一处,外壳补迁移口子 `as`(表单弹窗 `form`)/ `panelProps`(`onSubmit`)/ `role`(确认框 `alertdialog`)/ `portal` / `size="none"`(`.sett-cab` / `.csrc-sheet` 自带宽度,utilities 层 `max-w-*` 会压过);五处手写遮罩 + 设置柜 + PWA 安装弹窗全部走 `<Modal closeOnOverlay>`,`ConfirmDialog` 自管的 320ms 退场计时器与 window 级 Esc 监听退役、顺带获得焦点陷阱与焦点归还(此前只有 autoFocus)。③护栏:eslint 本地规则 `dorami/no-raw-modal-overlay`(JSX `className` 含 `modal-overlay` 即报,覆盖模板字面量 / 三元 / 拼接;`Modal.jsx` 本体按文件豁免)+ `frontend/test/modalOverlay.test.mjs`(状态机八条路径 + 用仓库真实 eslint 配置跑 Linter 证明护栏拦得住裸遮罩、放过外壳本体与 `.m-dim`);conventions §8 记「弹窗外壳与遮罩关闭」。移动壳 `.m-dim` 是面板的兄弟节点,天然无此问题,不动。**全站扫描(用户追问「其它弹窗都修了吗」)**:除本波迁完的 7 处 `.modal-overlay` 弹窗(另 3 处 `<Modal>` 调用方本就不点遮罩关闭),仓内其余「点背景关闭」元素共 9 个——5 个 `ledger-scrim` 抽屉(运维账户 / 文章详情 / 媒体热点图 / 播客单集 / 标签总账)、节点页 `inspector-scrim`、移动壳 2 个 `m-dim`——全是**自闭合的兄弟节点**,面板内按下、scrim 上松开时 click 落在共同父容器而非 scrim,结构上免疫;隔离栈实测账户抽屉三步(拖选到 scrim 不关 / scrim 按下拖进抽屉不关 / scrim 单击关)通过。图片灯箱 `reader-lightbox` 是父节点式,但设计即「点图片或任意处都关」且无可拖选文字,保持不动;右键菜单 / 分享浮层 / 日期选择器 / AI 面板走 document mousedown 判外,按下在内拖出不会关,属 popover 语义,不在此列。**`useModalA11y` 层栈(codex R1 P2,升级为本波必修)**:多层同时激活(抽屉上开确认框 / 表单弹窗)时 Esc 由各层 document 捕获监听各自处理、两层一起关,且同一 commit 里抽屉先把焦点还给触发行、确认框再把焦点还给抽屉里已 `aria-hidden` 的按钮——后者是迁移新引入的焦点回归,不能只记观察期;改为模块级层栈,Esc / Tab 只由栈顶消费,栈顶关闭后焦点回到下层触发控件、下层保持打开,第二次 Esc 才关下层(issue 原写「`useModalA11y` 不动」,这是迁移本身要求的最小改动,交接时向用户标明)。层栈落地后真浏览器又揪出两处焦点归还的老缺陷:①「触发者」原在 effect 里才读 `activeElement`,而 React 的 `autoFocus` 在 commit 阶段就已把焦点搬进面板,记下的其实是面板内的输入框,关闭时归还给一个正在卸载的元素、焦点掉到 body(重置密码 / 确认框都有 autoFocus,改前即如此)——改为在 `active` 由假变真的那次渲染里(commit 之前)抓触发者;②开发态 StrictMode 把 effect 跑两遍,cleanup 归还触发者后第二次 setup 只认「首个可聚焦元素」会把 autoFocus 的结果覆盖(取消钮 → 关闭 X)——记住上次 setup 时面板内的焦点元素,重跑时优先还给它,setup / cleanup 幂等。**顺带根治的滚动锁叠加**:迁上外壳后隔离栈实测「抽屉上开确认框、Esc 一次关两层」会把 `overflow:hidden` 留在 body 上(同一 commit 里抽屉的锁先还回 `''`、确认框的锁再把记住的 `'hidden'` 写回;改前确认框悬空后再关也会卡),各处「存旧值 → 还旧值」的锁改为全站一把引用计数锁 `hooks/useBodyScrollLock.js`(第一把存、最后一把还,中间顺序无关),Modal 外壳与运维用户抽屉共用。验证:前端 lint / build / `node --test` 24(含新增 10);隔离栈 Playwright(临时 ini + 沙箱库 + `VITE_PROXY_TARGET`,Chromium 1440×900)48 项全过——新建账户 / 重置密码 / 确认框 / 新建规范标签 / 设置柜 / 添加自定源六处各走「面板内按下→遮罩松开不关且输入不丢」「遮罩按下→拖进面板松开不关」「遮罩按下并松开关」「Esc 关」「关后 body 滚动锁归零」,表单弹窗 Enter 提交经 `as="form"` 建号、确认框遮罩点击 / Esc 均等于取消、`size="none"` 下设置柜 880 宽与自定源面板 460 宽均未被压;层栈落地后加测「抽屉 → 确认框 / 重置密码弹窗」:第一次 Esc 只关最上层且等于取消、抽屉仍开、焦点落回抽屉内,第二次 Esc 才关抽屉、body 锁归零。**codex 本地协商式检视 R1**:无 P1;P2 层栈(上)与 P3 护栏可被 `className={CONST}` / `clsx({ 'modal-overlay': x })` / `[...].join()` 绕过(改为与另外三条 dorami 规则同一扫描面——全部字符串与模板字面量,`Modal.jsx` 按文件豁免;测试补四种间接写法)——两条均接受并一次修完;R2 复检对照修复清单通过、无返修(层栈 / 两处焦点归还 / 护栏扫描面与八种正例逐项核验)。

**输入框聚焦环入 base 层(issue #108,分支 `feat/issue-108-focus-ring-layer`;版本号以实际发版为准)**:登录页账号 / 密码与发现页筛选框聚焦时,容器外环之内又叠一圈矩形亮光。根因是 `index.css` 顶部的全局 `button/input/select/textarea:focus-visible` 兜底环**未分层**,而这些输入框的设计是「容器画环、内部 input 在 `@layer components` 写 `outline: none`」——按 cascade layers 规则未分层恒胜层内,层内的 `outline: none` 从未生效,兜底环照画在 input 盒子外 2px、正落进容器内边距;文本框鼠标点入也命中 `:focus-visible`,故鼠标即现。仓库此前三次(v3.32 AI 问答框 / v3.42 账户表列头搜索 / 自定源浮层)都在文件尾未分层区逐个钉 `outline: none`,每次目检撞上一处补一处。修法=**兜底环移入 `@layer base`**(Tailwind v4 层序 theme → base → components → utilities,preflight 也在 base),层内 `outline: none` 按作者本意生效,尾区三条钉法删除(构建产物核对:该规则只出现在 base 块内一次);层内 20 处 `outline: none` 逐一核对替代指示——19 处已有容器 `:focus-within` 或自身 `:focus` 环,唯一的 button 类 `.section-band-toggle` 自带 `:focus-visible` 环;缺环的两处补上:发现页筛选框补容器 `:focus-within` 环(与条目列头同款 `0 0 0 3px var(--dorami-wash)`),移动端顶栏搜索框(`mobile.css` 是未分层 `@import`,同一机制、issue 未列)补 accent 底线(`inset 0 -1px 0` 阴影,零布局影响)。层内 `.search-box` / `.catalog-search` / `.board-search` 三组规则已无 JSX 引用(死 CSS,本波不清)。**Playwright 审计**(沙箱 = 真实库副本 + e2e 账号,亮暗两主题 + 移动壳):鼠标点入核对登录账号框 / 发现页筛选框 / 自定源 URL 框 / 条目列头搜索 / 台账搜索盒 / 账户表列头搜索(根管理员)/ 移动端顶栏搜索,input 自身 `outline-style` 一律 `none` 且容器(或自身)聚焦态有可见变化;Tab 遍历登录页、早报、发现、文章、台账、节点、运行、运维(含根管理员用户子页)、日报共十页 651 个停靠点,**零「无任何可见聚焦指示」的控件、零「input 自身外环 + 容器环」双环**——降层没有弄丢任何按钮焦点环。AI 问答框(沙箱未配 LLM)与运行史搜索盒(需选中任务)未在浏览器里点到,按 CSS 静态核对(容器 `.reader-ai-composer:focus-within` / `.jr-searchbox:focus-within` + 层内 `outline: none`,与已核对的同构)。conventions §2 / §3 更新。**未动**:同一顶部的 `button/input { font: inherit }` 仍未分层——字号陷阱与尾区十几条字号钉法照旧,同一手法可另波收口(记 backlog)。
**检视记录(本地 codex 协商式,三轮)**:R1 三条(P2×2 / P3×1)全落在新加的 E2E 守卫上,CSS 修法「无问题项」全部通过(它独立盘点了层内 19 处 `outline: none` + JSX 1 处 `focus:outline-none`,与我方结论一致)。F1 Tab 遍历用「视口坐标 + class」判重会在碰撞时提前结束而放过后半页——改元素身份,完整一圈只认「回到首个停靠元素」(直接回绕,或焦点离开文档一次后再按 Tab 回到首元素;codex 补的一层:落到 body 本身不算走完,控件自退焦 / 重渲染卸载也会出现同样状态),陷阱 / 连续落 body / 触上限均失败;F2 未命中 `:focus-visible` 的停靠点被静默跳过——一律断言命中;F3 快照启发式把任何颜色变化都当指示——定点控件改为只认宿主环类属性(box-shadow / 四边描边 / outline,含伪元素)的具体契约,Tab 扫描采样面加宽并含 `::before/::after`,双环只认祖先环类变化,守卫定位改口为「结构性守卫 + Tab 启发式烟测」(不做像素差断言,对比度与裁切留给目检)。R2 抓到 F2 残留(已卸载停靠点的 `isConnected` 早退排在断言之前),R3 终审通过。反向对照两次:全局环改回未分层 → CSSOM 断言失败;删掉发现页容器环 → 定点契约失败。
**发现页按形态批量订阅(issue #105,分支 `feat/issue-105-bulk-subscribe`;版本号以实际发版为准)**:发现页原来只能逐张源卡订阅,文章与播客目录扩容后操作成本过高。文章 / 播客筛选态新增「订阅全部…源（剩余 N 个）」,只计算当前账号尚未订阅的可见源,请求中显示「订阅中…」,归零后禁用为「已全部订阅」;全部形态、目录加载中或该形态无候选源时不展示批量入口,桌面与移动端共用 `DiscoverPage` / `useReaderState` 逻辑。任一形态批量请求在途时全局锁住文章 / 播客两个入口(同步 ref 防同一渲染帧双击),10 秒 `AbortController` 截止后解锁;客户端超时不等于服务端事务失败,提示「结果可能已生效」。服务端让同一用户的批量 / 单源 / 合集 / 自定源 / 订阅管理写操作共用互斥状态,重叠请求返回 409,`GET /api/reader/sources/subscribe-batch/status` 暴露当前账号是否仍在处理;超时 / 409 后前端后台轮询该状态,只在 `processing=false` 后重拉源目录 / 聚合列表 / 未读数,避免提交前脏刷新,也避免任一订阅写操作与原事务争 SQLite 写锁。后端新增单次调用 `POST /api/reader/sources/subscribe-batch`(`shape=article|podcast`),候选集复用 `/api/reader/sources` 的常规可见目录与私有自定源隔离口径:未订阅的隐藏源完全不可枚举,只有账号原本已知的隐藏订阅可计入 `unavailable`,既有可用订阅计入 `already_subscribed`;新增项逐个初始化未读 backlog 水位后统一提交,任一初始化失败整批回滚,重复调用幂等,不同用户仍各自持有订阅关系。Codex review 五条 P2 返修:批量响应不再泄露未知隐藏源 ID;加载中 / 空候选不再误报「已全部订阅」;跨文章 / 播客的并发请求改为全局锁 + 10 秒截止;超时后用服务端互斥 + 状态轮询协调仍在执行的事务;其余订阅写入口也纳入同一用户级互斥,杜绝与超时后的批量事务重叠。验证:后端专项及相关回归 109 项;前端 `node --test` 30 项、lint / build;隔离数据库端到端覆盖桌面文章 + 播客与 iPhone 14 文章流程,按钮剩余数 / 完成态 / 无横向溢出正确,每次操作只有一条 batch POST,不同测试账号可独立订阅同一公共源,pageerror / console error 均为 0。

**GitHub Release 自动部署流水线(issue #102,v3.60.0 首发、v3.60.1 闭环,2026-09-16–19;方案 `docs/auto-deploy-plan.md`,PR #111 / #115 / #117)**:生产长期落后 main、SSH 假死、前置项靠人记、没有部署记录面——改成「tag 即发布」之上再一步:`scripts/release.sh` 推 tag → `release.yml` 核验并自动建 Release → 同一 run 串联可复用 `deploy.yml`,停在 Environment `production`(required reviewers = 发版人,引用只放 `main` 与 `v*`)等批准 → runner 用部署专用密钥经 forced command 触发生产机**仓库外**的 launcher + worker(worker 是锁 / in-progress · last-success 事务 / 方向与单调护栏 / 首装门 / 退出码的唯一 owner,launcher 只起它并跟日志)→ 目标 tag 自己的 `./deploy-docker.sh` 七步(预检 / 构建 / `--check-config` / `--plan-migrations` 只读自检 / 备份 / 切换标记 + `up` / 五项健康核对)→ runner 从公网再核一次 `/api/health` → Job Summary 只登 allowlist 元数据。只有一种 tag,防误触的边界是批准人不是 tag 名;不自动回滚(备份按代际恢复,见 `docs/release-process.md`「回滚」);不接通知;手工 `./deploy-docker.sh` 仍可兜底且与流水线共用锁,手工部署过后流水线靠切换标记与容器构建 sha 交叉核对、不再信任旧记录。设计经 codex 四轮、实现三个增量共八轮协商检视收口(方案 §7 全文)。**上线实证抓到两个本地测不出的坑**:①`actions/checkout` 对 tag 事件在全量 fetch 后再 `fetch +<commit>:refs/tags/<tag>`,把 runner 本地 tag 引用改写成轻量 tag,核验脚本 `git fetch --tags` 撞「would clobber existing tag」且 `--quiet` 吞掉原因静默退出 1(#115:不再 `--tags`,tag 缺失才单独取,轻量判定看 `ls-remote`);②Environment **secrets** 不会自动进 workflow_call 的被调用 job(variables 会),调用方必须 `secrets: inherit`——设计与检视共同的错误假设(#117)。v3.60.0 经 dispatch 路径首次部署(基线自运行容器的构建身份读出,`allow_downgrade` 未起作用,文档相应更正),v3.60.1 经 `release.yml`→`deploy.yml` 串联部署成功(基线自 last-success)。留观察:`actionlint -shellcheck` 关闭;二期(另开 issue)= CI 构建镜像推 GHCR + 专用部署用户 + worker 自动升级;生产旧脚本时代的 rollback 镜像须手工清理(磁盘预检 5 GB)。


## 新闻覆盖可靠性（issue #127，待发布）

ZCode 排查确认 IT之家首页截断、公共日报评分前裁剪后推进全量游标、HN 网关失败与仅 AI 关键词发现三项风险。补上 IT之家 72h 分页、已知条目不占新额度及先发现后写入；日报以节点本地消费表保留暂缓候选，与正文/游标/运行统计同事务提交，默认40积压+80新候选并互补、每源60；HN保留RSS同时直连Algolia，品牌只匹配标题，ID与发现源语义不变。提供默认只读、固定任务ID、离线应用与快照回滚脚本。

Claude Code (`claude-fable-5-1`) 交叉检视先协商后返修：限定空游标 bootstrap，保留删报回退时的旧 pending，同值配置不打断生成，消除首页条数与全页时间游标假设；拒绝按龄期丢弃未评估文章，改用新旧池配额及积压最老时间观测。详细证据、测试、验收和上线边界见 [方案](./news-coverage-reliability-plan.md)。


## 跨平台文件锁(issue #133,待发布)

内网 agent 在 Windows 开发机跑测试:六处顶层裸 `import fcntl`(播客产物、TTS 回执、对象存储、日报生成、备份、存储维护)导入即
`ModuleNotFoundError`,连带 `api.app` 导入链整片测试无法收集。它建议改成空实现 shim,未采纳:六处都是跨进程互斥,空实现会让锁静默失效。
新增 `services/file_lock.py`:POSIX 委托 `fcntl.flock`;Windows 经 ctypes 调 `LockFileEx` / `UnlockFileEx`——真共享锁、真阻塞无超时,
锁区固定在偏移 2^62 处 1 字节永不与数据相交(Windows 字节锁是强制锁),`LOCK_NB` 争用 `ERROR_LOCK_VIOLATION` 映射 `BlockingIOError`,
与调用点既有的 `except BlockingIOError` 一致。六处调用点与 `tests/test_storage_backup.py` 改名接入;Windows 分支用假 Win32 API 单测
flags 组合、锁区偏移、错误映射、解锁静默。codex R1 两条 P2 全部接受(首版用 `msvcrt.locking`:首字节强制锁会挡住播客上传 fd 持锁期间
另一句柄的读取;阻塞模式约 10 s 放弃后调用方进入未获锁的临界区、共享锁降独占自争用)→ 改走 `LockFileEx` 一并解决。
内网 Windows 实测:`file_lock` 用例通过,但播客产物 30 例挂在 `os.replace`(WinError 32)——不是锁的问题,Windows 上任何打开的句柄
都挡住改名 / 删除(`os.open` 不带 `FILE_SHARE_DELETE`),内网 agent 提的「不上锁」治不了。修法:`import_bytes` 与 HTTP 流式导入写完即关
暂存 fd(锁随之释放,关到 rename 之间由 staging_ttl 保护),reconcile 的过期暂存清理改「拿锁 → 关句柄 → 删」(`_unlink_stale_if_unlocked`);
`bailian_tts._atomic_write` / `storage_backup._sync_directory` 的目录 fsync 在 Windows 打不开目录句柄,吞 OSError。POSIX 上用「能否再拿到锁」
代替「句柄是否已关」写回归用例。生产平台不变(仅 Linux)。

