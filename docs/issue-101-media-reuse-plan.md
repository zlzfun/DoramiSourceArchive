# Issue #101：接收端文件复用与同步进度

实现基线：`cdc2a71`。内网接收端默认 local；兼容现有 OSS 写入、缓存读写租约与元数据条件更新。

## 实现边界

- 整页校验与元数据事务保持。文件依照已接纳的 authority、清单大小、SHA-256、MIME/扩展名检查。
- 校验本地内容寻址文件；完整则复用并完成状态登记，否则下载、验证、唯一临时文件原子替换。
- 本地校验按块读取，文件读写及 OSS 持久化期间持有缓存租约；文件 I/O 不持有数据库读事务。
- 复用与下载共用状态提交，提交时条件匹配原始身份、权威及版本，不能覆盖并发新清单或音频撤回。
- 不提前推进 checkpoint，不改变数据流顺序，不加入日期过滤或下载并发。

## 进度契约

Job 增加可空 `progress_json`（配套 Alembic），接口新增 `progress`。
V2 上报绝对累计已处理记录数、当前流及该流已处理记录数、复用/下载文件数及字节数。
阶段切换和首条完成立即落库，其余沿用任务进度节流；失败终态保留最后成功进度。
文件完成状态提交后才计数，媒体页尾不再次累计。音频 tombstone 计入记录数但不计为文件。
前端显示当前阶段和实际计数，不从当前页大小推算全任务百分比；V1 与旧任务保持原有展示。

## 验证

图片与精简音频均覆盖：历史缓存、同内容不同 URL/记录、缺失/截断/同大小损坏、异常响应、
重试复用、逐文件进度、authority/格式/路径约束、OSS 失败与并发变更、完成水位和后续就绪顺序。
验证记录（2026-09-19）：

- 新增文件复用回归 40 项通过；迁移与历史库接管测试 55 项通过。
- `npm run lint`、`npm test`（34 项）、`npm run build` 通过。
- 双实例 `scripts/verify_split_sync_e2e.py --keep` 通过，覆盖全量、增量、删除传播与就绪顺序。
- Playwright 在独立临时数据库中检查设置页：图片运行中、音频失败后保留计数、成功汇总，
  亮色与暗色均目检通过，旧任务百分比兼容展示通过，浏览器控制台无错误或警告。
  截图保存在工作树 `output/playwright/`，不入库。
- 检视修正前完整后端套件 2236 项通过（648.59 秒）；OSS 边界修正后，复用、媒体规范化、
  对象存储及播客同步相关 90 项通过。最终提交另由 PR CI 跑完整套件。
- 不以合成场景推算生产同步耗时。

## 检视记录

PR #116 建立后进行独立 Codex 检视。当前环境没有启用 Herdr，使用本任务的独立审阅代理；
沿用先协商、再集中修正、复检只覆盖修复清单的流程。

- P2：OSS 对象完整但本地工作副本已清理时，重试的 producer 下载失败会把
  `cached/published` 降级，导致原本能从 OSS 恢复的内容被状态门控挡住。接受此意见。
- 协商结论：仅在本地路径确实缺失、OSS 已启用、同身份登记的大小/MIME/bucket/region 匹配，
  且原状态可用时保留状态。仍返回「未复用」并继续 producer 下载，不将 OSS 登记当作本地校验通过。
  已有但损坏的文件继续降级修复；此轮不扩大为 OSS 下载复用优化。
- 补图片与音频各三组回归：冷缓存下载失败仍可恢复、冷缓存成功下载、同大小坏文件即使有 OSS 登记也降级。
- 此 P2 已修复并复检通过：审阅方重跑原始复现 2 例和新增回归 6 例，8 项全部通过。
  其余全面检视未发现确定问题，无待处理检视意见。

合入仍需用户本地端到端验收与目检。
