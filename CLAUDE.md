# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> ## ⚠️ 本分支是 `master`(内网特殊适配分支)——开工前必读
>
> 本块及下述适配**只存在于 master 分支**,main 上没有;若你在 main 上看不到这段属正常。
> (本分支曾名 `intranet`,2026-08 更名 `master`——内网代码托管平台的内部开源仓以
> master 为默认主干名,同名对齐后,外网适配分支与内网主干一一对应,免去第三个分支名的来回同步。)
>
> **定位**:公网主线 main 的内网部署适配分支,长期存在、永不废弃合并。目标环境:
> 出网被企业网关 MITM(自签证书链重签一切 TLS)、Docker 过旧不可用(18.x/无 compose)、
> CentOS + 手动安装的 nginx/node(非包管理器路径)、站点以纯 HTTP + IP 访问。
>
> **三条铁律**:
> 1. **一切内网适配只提交本分支,绝不合并/cherry-pick 回 main**——即使改动看似通用
>    (先例:`disable_tls_verify` 曾按"通用能力"被提交到 main,后按用户要求回退)。
> 2. **同步方向单一**:`git merge main`(main → master),反向永远禁止;也不要把
>    本分支推成 PR。
> 3. **每次开工前先核对与 origin/main 的差异**(SessionStart hook 会自动注入概况):
>    落后较多时,先 merge main、解决冲突、验证后再做新工作,避免差异滚雪球。
>
> **本分支独有内容**(即与 main 的预期差异面,v3.39.0 起已收敛为三项):
> `[network] disable_tls_verify` 开关及 7 处 httpx `verify=settings.network.tls_verify`
> 接线(fetchers/base、legacy_backend、media_store、remote_sync、source_builder、
> x_api、llm/client;原 vector_storage×2 已随 v3.31 RAG 退役清仓消亡)、
> 本须知块、`.claude/settings.json` + hooks。
>
> **部署面文件已不再是本分支独有**:`deploy.sh` / `ecosystem.config.js` /
> `production.example.ini` 的 `[server]`/`[nginx]` 两节于 **v3.39.0 回迁 main 扶正**——
> 起因是一台**公网**机不便装 Docker,证明该路径服务的是「宿主没有 Docker」而非
> 「内网适配」,继续挂在本分支会让分支名与用途脱节。回迁时清理了 RAG 形态判定等内网痕迹,
> 本分支自此**直接用 main 版本,不再本地维护**(裸机路径用法见 `docs/deploy-baremetal.md`)。
> 注意这不是铁律 1 的例外:回迁的是通用裸机部署能力,`disable_tls_verify` 及其接线
> 明确**未随行**——出网跳过 TLS 校验是内网 MITM 网关的专属妥协,公网开它等于自毁。
>
> **merge main 冲突解决原则**:部署面文件(deploy.sh/ecosystem/ini 两节)**以 main 为准**
> (v3.39.0 前是「以本分支为准」,已反转);`src/` 冲突以 main 的演进为准,但**必须保留
> `verify=settings.network.tls_verify` 接线**(main 新增 httpx client 时也要顺手接上);
> 本文件/AGENTS.md 冲突 = 保留须知块 + 采纳 main 的其余更新。
>
> **内网部署**:`./deploy.sh`(用法见 `docs/deploy-baremetal.md`,该文档以公网姿态为默认);
> 内网姿态需在 `production.ini` 反转两处:`[network] disable_tls_verify = true`(公网默认 false)、
> `[auth] cookie_secure = false`(纯 HTTP + IP 访问,公网走 HTTPS 时应为 true)。

## Development Commands

### Backend (Python)

```bash
# Install dependencies (use uv, the project uses uv.lock)
uv sync    # dev 全量:uv sync --extra crawl4ai(浏览器详情后端)

# ⚠️ 改依赖(pyproject dependencies/extras)后的完整流程——uv.lock 按惯例不入库
# (含开发机镜像源改写),镜像构建的版本事实来源是入库的导出清单,改完必须重导出并提交:
#   uv lock && uv sync --extra crawl4ai
#   uv export --frozen --no-dev --no-hashes --no-emit-project -o docker/requirements.txt
# tests/test_docker_requirements.py 守卫清单与 pyproject 的一致性(v3.17.0 生产曾因旧锁静默装回 torch)

# Run the backend server (starts on http://127.0.0.1:8088, hot-reload enabled)
python src/main.py

# API docs available at http://127.0.0.1:8088/docs
```

#### Database migrations (Alembic)

Schema evolution is versioned via **Alembic** (`alembic/`, config `alembic.ini`). `alembic/env.py` uses `SQLModel.metadata` (import `models.db`) as the autogenerate target and reads the DB URL from `settings.storage.database_url` (unless a URL is injected programmatically — see `src/storage/migrations.py`). SQLite has no native `ALTER`, so `render_as_batch=True`.

- **Runtime bootstrap is still `create_all()`** — `DatabaseStorage.__init__` builds tables from metadata for fresh/in-memory DBs (fast, and what the tests rely on). Alembic is the authoritative mechanism for **evolving existing file DBs** and CI/ops.
- **SQLite foreign-key policy is connection-wide and intentional** — runtime `DatabaseStorage` connections set `PRAGMA foreign_keys=ON` so declared CASCADE/SET NULL rules execute; Alembic temporarily uses `foreign_keys=OFF` for SQLite batch-table replacement. Run `PRAGMA foreign_key_check` on a production backup before upgrading an established database.
- **The invariant that keeps the two in sync**: `create_all()` (== metadata) must always equal `alembic upgrade head`. `tests/test_migrations.py::test_upgrade_head_has_no_drift_from_metadata` enforces this — so **every model change needs a matching migration** (or the drift test fails). Author changes as: edit `models/db.py` → `alembic revision --autogenerate -m "..."` → review.
- **Adopting Alembic on a legacy DB**: `storage.migrations.ensure_migrated(db_url)` handles "has tables but no `alembic_version`" by stamping the baseline (`5ee31a7c5393`) then `upgrade head` (avoids re-running baseline `create_table` on existing tables). It runs automatically at every startup entry: the production container entrypoint (`docker/entrypoint.py`) calls it before uvicorn, **and `src/main.py` calls it before `uvicorn.run`** (covers dev bare-start; in-memory DBs are skipped, at-head is a fast no-op). Only importing `api.app` directly (e.g. tests) bypasses it.

```bash
.venv/bin/alembic upgrade head                       # apply migrations to the settings DB
.venv/bin/alembic revision --autogenerate -m "desc"  # generate a migration from model changes
.venv/bin/alembic check                              # report drift between models and the current DB
```

> Note: legacy DBs built by the old hand-written `_ensure_compatible_schema()` `ALTER TABLE ADD COLUMN` path are **missing the `index=True` indexes** those columns declare (raw ALTER never created them) — a real pre-existing drift that a follow-up migration repairs.

### Frontend (React + Vite + Tailwind CSS v4)

```bash
cd frontend
npm install
npm run dev      # Start dev server (port 5173, proxies /api → backend)
npm run build    # Production build
npm run lint     # ESLint
```

**Frontend design & implementation discipline**: before any frontend styling/UI change, follow [`docs/frontend/conventions.md`](docs/frontend/conventions.md) — the durable rules for 文案/accessibility/typography/color tokens/radius/elevation/motion/primary-action/dark-mode. The single source of truth for design tokens and role classes is `frontend/src/index.css` (`:root` tokens + `@layer components`): reuse `--r-*` radii, `--motion-*`, `--dorami-*` color/shadow tokens and the typography role classes (`.body-text`/`.micro-label`/etc.) instead of hand-writing `text-[Npx]`/`rounded-[Npx]`/hex. Work-area surfaces stay restrained; the brand/login cinematic motion is deliberately exempt.

Data is stored in the `data/` directory (SQLite `cms_data.db`,含 FTS5 全文索引的 shadow 表)。

### Production deploy

**两条官方部署路径**:Docker(推荐)与裸机 PM2(装不了 Docker 时)。

**Docker 是生产推荐路径**(2026-07 部署重构,见 [`docs/deploy-docker.md`](docs/deploy-docker.md)):`./deploy-docker.sh` = 构建镜像 → `docker compose up -d` → 全链路健康验证。双容器形态:`backend`(`docker/backend.Dockerfile`:python:3.12-slim-bookworm + uv.lock `--frozen` 锁定依赖 [torch 经 `UV_TORCH_BACKEND=cpu` 改道 CPU 轮子、CUDA 伴生包从安装清单滤除] + Playwright Chromium;入口 `docker/entrypoint.py` 固定监听 0.0.0.0:8088,ini 的 `[server]`/`[nginx]` 节在容器内不生效)+ `nginx`(`docker/nginx.Dockerfile` 多阶段:node 构建前端 → nginx:alpine + `docker/nginx.conf`)。数据(`./data`)与 `config/production.ini` 从宿主挂载,容器无状态;机密走环境变量(`DORAMI_X_BEARER_TOKEN`);`shm_size: 1gb` 是 Chromium 渲染的硬需求;`TZ` 默认 Asia/Shanghai(cron 语义)。

**裸机 PM2 路径**(`deploy.sh` + `ecosystem.config.js` + ini `[server]`/`[nginx]` 两节,见 [`docs/deploy-baremetal.md`](docs/deploy-baremetal.md)):`./deploy.sh` = 装系统依赖 → uv venv + `uv pip install -e .` + Playwright → **DB 备份 + 迁移预检 + `ensure_migrated`** → npm 构建前端 → 生成并 `nginx -T` 复核站点配置(含可选 TLS 与 `/mcp` 的 Host 改写)→ 发布 dist → `pm2 reload` + nginx reload。该路径 v3.15.1 随生产切 Docker 退役删除,删除期间在 intranet 分支为内网环境复活维护,**v3.39.0 因「公网机不便装 Docker」的真实场景回迁 main 扶正**(回迁时清理了当年的 RAG 形态判定——向量子系统已随 v3.31 退役)。裸机路径下 ini 的 `[server]`(PM2 后端监听)与 `[nginx]`(站点生成输入)两节**生效**;Docker 路径下二者是死节(容器不读),`[server]` 另在 dev 裸起(`python src/main.py`)时使用。开机自启需手动跑一次 `pm2 save && pm2 startup`(Docker 路径由 `restart: unless-stopped` 承担)。**内网专属的 `[network] disable_tls_verify` 不随本次回迁进 main**,仍只在内网适配分支 master(曾名 intranet,2026-08 更名对齐内网托管平台主干名)。

## Versioning

单一事实来源是 `src/version.py` 的 `__version__`(SemVer:MAJOR=产品形态级改版 / MINOR=功能波 / PATCH=修复)。升版本改它并同步 `pyproject.toml` 的 `version`(项目非 editable install,importlib.metadata 不可用),**随后跑一次 `uv lock --offline` 把锁里的项目版本对齐**——否则 uv.lock 与 pyproject 漂移,`uv run`(PyCharm uv 解释器的启动方式)每次启动都会重解析并访问镜像源,表现为启动变慢、镜像不可达时直接报错(纯版本号 bump 无依赖变化,离线重锁用缓存即可);`/api/runtime` 透出 `version`,前端「设置 → 关于」展示;合入 main 的版本节点打 annotated git tag `v{__version__}`。纪元:1.x 采集归档原型 → 2.x 读者分发平台(PM2 app 名 dorami-backend-v2 即此纪元遗痕)→ 3.0.0 静默仪器全站重构收官 → 3.6–3.10 阅读器 Folo 化五波(四带式/容器模型/全站轨语言/设置柜归一/发现页) → 3.11 图床波(媒体库 + 媒体热点图) → 3.12 X 社交波(X API v2 采集 + 社交媒体第三容器) → 3.13 登录电影化(信号星座三维摄像机背景 + 居中强调→左移→登录卡浮现两幕开场,LoginScreen 重构)→ 3.14 登录跃迁波(登录成功「跃迁」终幕:星点由慢到快拉光轨 + 闪光跨切换 + App 三段式抵达幕 hold/settle/fading——主题色幕布兼慢网延迟护罩,认证成功即预热 runtime;质感波:登录页字形子集本地打包(思源宋 600/700 + Plex Mono,~10KB)、卡片 3D 倾斜、失败震镜+摇头、密码隐私幕、星等色温、摩擦帧率归一化、gloss 快扫慢歇)→ 3.15 Docker 部署重构(双容器 compose 取代裸机 PM2 路径:镜像内前端构建 + uv.lock 锁定依赖 + CPU torch 瘦身 + Playwright 环境固化;3.15.1 生产观察期满后删除 deploy.sh/ecosystem.config.js/ini [nginx] 节,PM2 正式退役;3.15.2 配置面收敛——删 requirements.txt 重复源/三个死配置节 [wechat][xiaoluban][image_host]/local.ini 备胎链,宿主 TLS 边缘配置入库为 docker/edge-nginx.conf.example)→ 3.16 RAG 依赖分离(sentence-transformers 入 rag-embedded extra,默认镜像 -1GB)→ 3.17 RAG 服务化(chroma server + TEI 双容器 compose profile ["rag"] 按需启用,vector_storage 嵌入/重排 provider 化、显式向量单路径;真 chroma 集成冒烟验证)→ 3.18 互通波(读者反馈收件箱 + 管理员公告横幅 + 远程内容同步——设计 `docs/engage-sync-wave-plan.md`)→ 3.19 管理员平权波(多管理员——任意数量/互相设立,唯一护栏「末位活跃管理员保护」;管理员双界面——轨底隐藏切换钮进出阅读器 + `default_surface` 登录落地偏好;管理写操作审计 `admin_audit_logs` + 运维页审计面板;移除 ini [auth] 账户种子,空库首启自动生成根管理员 admin/admin;3.19.1 铺开前打磨——媒体热点图年份切换轨[GitHub 式,后端 heatmap 加 year 参数与 years 列表]+卡片间距修复、登录跃迁收尾改全局隐出/隐入[弃白光闪切,auth-warp-flash/app-arrive 均平色幕布]、轨底 RailUserFlyout 用户滑出菜单[头像 hover 滑出工具钮+头像变关机退出钮]、轨 tooltip 提速 350→120ms、视图轨「发现」与内容容器间加分隔线、图表微调[排行柱厚 14px/订阅=青绿·收藏=琥珀呼应收藏星;通用亮色调色板两度试验后拍板回归主题色版];3.19.2 设置柜整备波——柜体 640→576 收纵向空置+设置行距 12→14、账户页按钮统一 32px M 档[pw-grid 拉伸 self-center 阻断/sett-seg 34→32]、内容门控改按「当前界面」[`readerSurface` 下沉:adminConsole=admin×管理台才见 MCP 开关/全库令牌文案/数据同步组,admin 切阅读器后设置柜与读者同观感;反馈页仅读者账号;MCP 取数范围说明拆管理版/读者版]、数据同步页重设计[双列虚线卡→单列整幅实线卡,根治 datetime 截断;gzip 换 ledger-switch/file input 换投放区行]、远程同步定时任务[KV `remote_sync:schedule` 凭据只写不回显(沿 X token 范式,手动同步仍不落库)+ GET/POST /api/admin/remote-sync/schedule + APScheduler job `remote_sync` 热生效/增量自游标/防重叠,前端定时卡=开关+凭据+频率预设(每天/每6时/每时/自定义 cron)])→ 3.20–3.21 日报页与阅读器打磨/小特性波(近期日报分页懒加载、hash 语义化 #/mcp→#/brief、发现页选源升级、反馈入口提级、文章阅读量)→ 3.22 新读者引导波(轨底退出两击防呆[首点入 4 秒待确认态+Toast 提示,防好奇误退]+ 新账号预置精选订阅[`DEFAULT_SUBSCRIPTION_SOURCE_IDS` 扩至 5 源:日报/量子位/IT之家AI/Anthropic/OpenAI,播种即建未读水位]+ 阅读窗空态轻引导[文案简化为「选择一篇文章以开始阅读」+ 一行直达发现页的小字,新老用户通用;欢迎卡方案实现后被否决拆除——太啰嗦];3.22.1 退出提示文案统一「再次点击以退出登录」——待确认态头像已翻面成退出钮,不再提「头像」;3.22.2 登录演出品牌豁免——不随 OS「减少动画」降级[Windows 关动画效果即报 prefers-reduced-motion,此前登录页只剩静止一帧;LoginScreen prefersReducedMotion() 恒 false + 全局 1ms 钳制规则放行 .auth-stage/.app-arrive/.app-arriving 子树,工作区仍全面尊重该设置];3.22.3 默认字母头像——单个大写首字母+按字母着色;3.22.4 文章质量校对波——行内空白保真+站点头尾噪声清剿[推广前 55 源全量抽查:_inline_markdown 保空白递归修全站性空格丢失、粗斜体/sr-only/零宽字符/播放器控件等下沉共享转换层,OpenAI/Apple MLR/DeepMind/Reddit/qwen/claude_blog/anthropic/qbitai/aiera 等站点级头尾噪声清剿;**修法与新源抽查清单沉淀于 docs/sources/node_audit_playbook.md《Content-quality proofing》节,补充新源必读**];3.22.5 源命名核对波——标题去渠道机制词+简介去采集话术+取名注册表现取[13 处改名:新智元 Website→新智元、OpenCode GitHub Releases→OpenCode Releases、Z.ai New Released→Z.ai 更新公告(文章标题模板同步归一 site_name 前缀)等;口径拍板:通用体裁词用中文(博客/新闻/公告)、页面专名英文保留(Changelog/Release Notes/Daily Papers)、分隔符统一「·」;发现页直出的 description 全改内容式名词短语;`vector_storage.friendly_source_name()` 成为取名运行时单点(现役源从 registry 现取,`SOURCE_FRIENDLY_NAMES` 只留日报特殊源+下线归档源兜底,名单同步 DECOMMISSIONED_FETCHER_IDS);**命名规范沉淀于 node_audit_playbook《Source naming & description》节;改名后存量 chunk 头部仍旧名,在意则 reindex-all**];3.22.6 新账号首屏竞态修复——预置订阅播种移至登录点[原播种只挂 GET /api/reader/sources,与阅读器首屏并行发出的文章列表(subscribed_scope=only)/未读计数请求竞态,新账号表现为空列表+积压未读被误判成「载入 N 篇新文章」提示;登录响应先于前端挂载,竞态根除;仅 runtime 读者面开启时播种、失败不阻断登录,reader 端点原调用保留兜底])→ 3.23 源扩容 wave4(Microsoft AI 模型、Artificial Analysis、Meta AI 博客、Kimi Research、MiniMax Research、Import AI、Arena 排行榜更新;全批 incubating,全文 RSS/精确正文圈选/单页 Changelog 拆条与真实正文质检)→ 3.24 源可见性波(管理面隐藏节点:KV 隐藏名单 + /api/admin/source-visibility + 节点管理检视器开关,观察期故障/文章缺陷不改代码即时下架;内容交付全量排除、目录呈现分订阅态——已订阅者源栏保留「暂不可用」灰显条目可退订可等恢复,未订阅者完全不可见;顺修 v3.19 双界面遗留缺陷:admin 停留阅读器界面刷新白屏[mountedTabs 刷新恢复未挂载 reader 面板])→ 3.25 分享波(读者把一篇内容发给同事,此前只能截屏。**站内深链**`#/reader/a/{id}`——纯前端 URL 不落库,收到的人登录后直达该篇[App 在导航体系 replace 掉 hash 前就地取走 id,admin 带深链进来一律落阅读器,压过 default_surface 与 sessionStorage 的界面记忆];**公开链接**`#/s/{token}`——免登录只读页 `ArticleShareRecord` + `GET /api/public/share/{token}`,有效期 7/30 天或永久、可随时撤销、管理面总闸 `public_share_enabled` 关闭即让已发出的链接全部失效;公开页在 main.jsx 于 App 之外分流[访客没有账号,不能走登录门]。顺带两修:**日报标题重复**[正文首行 `# 🤖 哆啦美 AI 资讯日报 · 日期` 与阅读窗 reader-pane-title 重复,渲染侧 `stripDuplicateLeadingHeading` 剥离——归档正文与导出契约原样不动,存量日报同修];**节点分类改信息角色单轴**[原「公司 → 板块」两级谱系只登记 17 家公司,现役 44 个 owner 里 27 家掉进「其他来源」兜底占半个目录,同家节点还因 owner 写法不同而分家;`groupBySection`/`SECTIONS` 退役为 `groupByRole`,管理面与阅读器源栏、发现页共用官方/媒体/个人/榜单同一套词汇,公司降为组内排序键(用 company.key 拉丁标识排,避免中文名浮到组首);顺修三处误分类:Import AI[expert_newsletter,Jack Clark 个人执笔]媒体→个人、Arena 排行榜更新[ai_benchmark_platform]官方→榜单、Artificial Analysis[ai_benchmark_analysis]媒体→榜单;节点行上的角色胶囊撤除,组头已承担])→ 3.26 凭据整合波(外部第三方凭据统一保管层 `services/credentials.py`——LLM api_key/X bearer_token 声明式命名空间注册,resolve/field_sources/save/sanitize 四能力统一「env/ini 基线 + KV 覆盖」解析序与只写不回显契约,KV key 沿用存量零迁移;remote_sync 定时凭据 blob 复用 secret 合并语义;LLM 配置响应补 field_sources 收敛与 X 的漂移、脱敏统一 ••••尾四位;前端 SecretField 统一凭据输入件三处就地复用;为内网 SSO 工号密码与微信/微博/小红书/知乎平台凭据预留登记位)→ 3.27 凭据整合台(设置柜 → 管理组新增「凭据」区[拍板 B,弃运维子页方案]——LLM/X API 两张单点编辑卡[SecretField+field_sources 来源徽记+stamp 状态章+测试连通/保存]+「其它凭据」只读回指[定时同步凭据柜内一跳数据同步、GitHub Token 部署级 env-only,`GET /api/admin/credentials` 只回存在性;标题瘦身:说明性副文案撤除];消费页表单撤除换 model-chip 回指:运维 AI 开关板撤 LLM 编辑器、内容页 X 区撤凭据表单[配额 KPI 留守],日报页/运维两 chip 均 openSettings('credentials') 深链;口径=「设置=写入口,运维=观测面」;两版样页 docs/design/dorami-credentials-{quiet,cabinet}.html)→ 3.28 右键菜单波(Folo 式右键上下文菜单——阅读器三落点[条目列文章卡/源栏源条目/社交推文卡]右键弹自绘菜单,动作全为既有函数的并联入口:读态/收藏首组→链接类次组→重操作/破坏性末组[跨落点肌肉记忆],admin 源菜单多「管理」分组[在读者面隐藏/恢复,与节点检视器同 API];`hooks/useContextMenu` 让位判定[卡内**既有**选区/输入域放行浏览器默认菜单;macOS 右键 mousedown 默认行为会自动选词、contextmenu 时刻分不清主动拖选,故 mousedown(button2) 捕获阶段快照右键前选区,自动选词不让位且拦截后清除]+`components/ContextMenu` portal 化单例[fixed 快照定位实测夹取+贴下缘上翻、滚动 capture 一网打尽有 ref 无 ref 容器即关、复用 useModalA11y+方向键循环、disabled 不隐藏];被右键卡挂 `.is-ctx-anchor` wash 锚定态[刻意非 .is-on 选中语义,关菜单即除];`.ctx-menu` 类族入 index.css 暗色 --sh-3 列表;首帧不可 visibility:hidden 遮测量——隐藏元素不可聚焦,useModalA11y 首焦会静默失败;样页 docs/design/dorami-context-menu-quiet.html)→ 3.29 移动端 H5 阅读器波 Wave1+2(手机浏览器/微信内直接打开即用的移动阅读器;小程序方案评估后弃选——request 合法域名须公网 HTTPS+备案、资讯聚合类目审核是硬约束,H5 内外网通吃零审核,小程序留作 H5 验证后的可选二期[推送/转发卡片是其唯一增量]。**Wave1 逻辑抽取**:ReaderTab 数据层整段抽入 `hooks/useReaderState.js`[源目录/订阅、未读体系、收藏、列表加载与无限滚动、正文与 AI 缓存、深链落地、右键/长按菜单 items 三份构建器],桌面只余四带 JSX+视图胶水[overlay 滚动条/latest-ref 行回调/右键弹层/品牌图回退],桌面行为零变化,ArticleRow/骨架屏改具名导出供移动壳复用;**Wave2 移动壳**:App.jsx 按 matchMedia(≤767px) **首帧一次性**分流 lazy `components/mobile/MobileReader`[不做拖窗热切换;登录门/runtime/主题/设置柜/Toast 管道全部共用,admin 手机上同落阅读器、管理台不下放],底部 4-Tab[文章/动态/社交/我的=视图轨的移动翻译,wash 块选中+文字标签]+正文 push 全屏页[衬线/进度线 scroll-timeline/速读卡/翻译/分享/上一下一篇,复用 .reader-pane-* 排印类]+源过滤抽屉[§4.1.2 选源是过滤器不占 Tab;聚合入口+角色分组照搬源栏语法]+**长按动作单**[`hooks/useLongPress` touch 计时+contextmenu 双路径互补——iOS 不派发 contextmenu、Android/桌面会,长按后吞合成 click;ActionSheet 的 items 与桌面右键完全同源同组序,跨端肌肉记忆;被长按卡同挂 .is-ctx-anchor]+我的页[承接 RailUserFlyout 全部职责,退出两击防呆改确认框——触屏 hover 展开会吞首击]+发现页整页层[低频目的地不占 Tab,从 我的/抽屉 进];`mobile.css` 全 .m-* 前缀经 index.css @import 引入、token 化亮暗自动翻转,viewport-fit=cover + env(safe-area-inset-*) 垫边;社交 Tab 本波复用 SocialFlow+CSS 收窄[动作钮常显补 iOS 长按不可达],Wave3 收口;收藏保持容器级过滤语义[我的页收藏入口=当前容器收藏];样页 docs/design/dorami-mobile-quiet.html)→ 3.30 检索扶正波(RAG 退役第一波,方案 **docs/rag-retirement-plan.md**;审视拍板:向量 RAG 从未在生产运行[0 向量化]、生产硬件跑不起推理栈、FTS5 已就绪且结构性零漂移,故取缔向量形态、检索改「LLM 计划检索 + FTS5」两段式。scope=subscription 问答改走 `services/reader_search.py`[规划(1 次轻量 LLM,中英关键词改写+日期窗+意图分流)→ FTS5 召回(订阅域∩日期窗,关键词并集)→ 选篇(1 次轻量 LLM,标题+引子挑 ≤8 篇;候选≤8 短路省调用)→ 全文注入;降级链自持:规划失败→原词 FTS→零命中→时序窗口,任何一级不 5xx;「日报即索引」:跨期盘点型问题在已订阅日报源时收敛检索日报正文;两次调用计费并入 ask];`assemble_reader_context` 收敛为 search_fetch 闭包注入(D11 形态保留);MCP `search_articles`/`get_rag_context` 同名换 FTS 芯[出入参兼容,distance 退役,**RAG 关不再等于 MCP 无搜索**];前端撤 MCP 工具「RAG 未启用」灰显与 bge-m3 硬编码、问答范围选项加 hint。RAG 开关自此无读者面语义)→ 3.31 退役清仓波(Wave B,与 Wave A 连做——跳过观察期的论证:向量层不构成新管线的回退路径[生产 0 向量化,从未运行]、生产硬件从未支持、删列零信息损失。向量子系统整体下架:删 chromadb 依赖与 rag-embedded extra[镜像构建改单清单,requirements-rag.txt 删除]、compose rag profile[chroma+TEI 双服务]与 WITH_RAG 构建分支、[rag]/[models] 配置节与 DORAMI_RAG_* 环境变量、/api/vector*·/api/vectorize*·/api/rag* 全部端点与 routers/vector.py·storage/impl/vector_storage.py·services/vector_reconcile.py 三模块、04:00 对账巡检与 auto_vectorize 抓取钩子、ArticleRecord 的 is_vectorized/index_status 两列[Alembic `f2c9d4e07a11`:SQLite 先删索引再 batch 删列,重建表会连带掉 FTS triggers 故收尾 ensure_fts 幂等补回;历史 index_status 回填迁移加列守卫兼容新库收养回放]、前端向量雷达页签/台账向量列与自动向量化开关/api.js 十函数/EmptyState·StatusBadge 孤儿原语/.vector-status-* 等死 CSS ~150 行/运维内容板「向量化率」KPI;friendly_source_name+SOURCE_FRIENDLY_NAMES 迁至 services/source_naming.py[8 模块引用改址];公开订阅检索端点 /api/public/subscriptions/{id}/vector/search 路径留存换 FTS 芯[与 MCP search_articles 同源];tests/rag/ 评测 harness 与四册向量测试删除。重新引入触发器与轻形态约束见方案 §4)→ 3.32 阅读面 AI 打磨波(拍板方案 A,样页 docs/design/dorami-ask-quiet.html。**问答面板重设计**:浮层 380→440,读者问句保留右缘 wash 胶囊、哆啦美回答**去框化**[无框全宽正文块+行首琥珀星标身份行,面板内 markdown 字阶降档],范围下拉换 composer 内 seg 胶囊[本文/我的订阅],空态建议问题[目检返修后定稿:居中无框引号轻文本行,题池每 5s 轮动/悬停暂停/点击填入],**阶段化等待态**[客户端 ask_id + 内存进度 dict + GET /api/reader/ai/ask/progress 轮询,检索管线 plan→search→select→answer 逐阶段上报;流式 SSE 挂 backlog 以此过渡];**引用联动**:上下文编号 [n] 即引用锚——build_numbered_context/build_sources_payload 同源同序[sources 补 id/source_name/publish_date],QA 提示词改行内 [n] 标记且禁自列清单,前端 [n]→引用 chip[ReaderMarkdown citations 扩展点,越界剥除]可点站内跳转[useReaderState.openArticleById——深链落地抽成可复用函数,引用跳转与 #/reader/a/{id} 同路]+末尾出处列表由 sources 确定性渲染[行内标记尽力而为,集中列表保底必有];**范围四档**:核心层扶正「文章名单→编号上下文+sources」,article=n1 特例[单篇也带出处]/articles=显式多篇≤12[后续多选问答/收藏集问答的原语]/subscription/all=检索圈定[all 走 feed_service.resolve_all_visible_source_ids 全库可见源,发现页预览同口径、隐藏源照旧排除],后端全量落地、前端仍只露两档;目检返修七项:输入法组字回车不误发/composer 聚焦压掉全局 textarea:focus-visible 矩形环/出处行无原文链接时保留等宽外链占位[右缘对齐]/点面板外即收[灯箱 portal 豁免]/FAB 星标与面板字头统一琥珀[撤暗色靛蓝覆盖]/标准态面板给显式高度 600[auto↔定值不可过渡,是尺寸切换生硬的根因]+开合软过冲曲线+轮次浮现动效;二轮返修:规划新增 **chat 意图分流**[闲聊/对话元问题(「你是谁」「我上一个问题是什么」)plan 后直返空上下文纯对话作答,不检索不落时序窗口]+**等待态改动态阶段清单**[只画实际发生的阶段,chat 型只见「理解问题→组织回答」两行;首行文案「规划检索」改「理解问题」;**阶段历史由服务端权威累积**(entry.stages 全量随 GET 返回)——search 阶段存活仅几毫秒,前端轮询采样必漏,靠采样拼历史曾把「没轮询到」误判成「无需检索资料」]、IME 判据加固为 composition 事件对+isComposing+keyCode 229 三重[Safari 确认回车在 compositionend 后到达,单判据漏网]、focus 外环压制移至文件尾未分层区[全局环未分层,@layer 内 outline:none 恒败——项目已知陷阱又一例]、建议问题降至 11.5px faint 且轮换放缓[8s 一换/淡出浮现各 ~0.5s+,建议是氛围不是通知];三轮返修(出处降噪):选篇提示词改**宁缺毋滥**[具体主题只选真正对题的,「沾边但不对题」明令不选——FTS 宽召回是设计内,松的是选篇]+前端出处列表只列**回答里真正引用过的 [n]**[保留原编号与行内 chip 对应,标签「本次回答引用」;通篇无标记才回退列全量并标「参考」];四轮返修(时效感知补课):此前只有检索规划层知道今天日期,选篇层无基准、**作答层既无今天日期也看不到各篇发布日期**——「最近一周」类问题可能把旧闻当新闻;现 build_numbered_context 块头带「来源名 | 发布日期」、QA/选篇 user prompt 均注入今天日期、QA 系统提示词加时效甄别原则;实测弱模型仍会把 7 月旧闻包装成「最近一周」,故再加**机械化诚实层**:降级阶梯改「窗口内零命中 → 先放宽日期窗保主题 → 再丢主题看最新」,且降级一旦改写检索语义就由检索层把事实以「(检索说明:…)」**确定性注入上下文头部**[窗口无命中版/主题无命中版两款文案,时效浏览型是本意不注说明]——诚实性不靠作答模型自觉对照日期;规划提示词的日期折算改硬性要求[「最近一周」类相对表达必须折算 date_gte];顺修 App.jsx **runtime 失败落锁**缺陷[fetchRuntimeInfo 失败时能力位以全 false 落地且 runtimeLoaded 闸门不再重试——dev 后端重启/热重载窗口期撞上一次,AI 入口与管理页签就"消失"到重登录为止,像配置丢失(DB 实际原样);以 account_role 缺失为哨兵每 3s 安静重拉自愈];移动壳问答下放留后续波)→ 3.33 AI 渐变身份色波(AI 家族统一**蓝→紫渐变身份色** `#3f7bf6→#8b52f4`[样页 docs/design/dorami-ai-identity-quiet.html 三轮迭代:琥珀/靛/青瓷三方案→全站渐变试探→收束为 AI 专属;线性中点≈现行靛,与品牌同族、与收藏琥珀色相分家,v3.32 的 FAB 琥珀方案退役、琥珀归还收藏专用]。触点六处:问答 FAB 星/面板字头星/回答身份行[星+「哆啦美」渐变小字头]/空态星/速读卡[渐变 wash 底+渐变字头]/译钮激活态[is-blue→**is-ai**:渐变 wash 底+渐变「译」字,桌面 .reader-pane-iconbtn 与移动 .m-iconbtn 同语;分享/搜索钮的 is-blue 不动]——**全站唯一的渐变=AI 触点,见渐变即 AI**;纪律:渐变只上身份标记,面板内操作件(发送钮/seg/引用 chip/建议问题)与全站主题保持 solid accent。基建:`components/AiGradientDefs.jsx` 文档级 SVG defs[#ai-grad 正向/#ai-grad-inv 反色底——墨底 FAB 亮色主题需亮端、暗色主题底翻浅需深端;stop-color 吃 CSS 变量随 [data-theme] 自动翻转;App 桌面/移动两渲染根各挂一份,同帧仅存其一]+ :root 令牌 `--ai-g1/g2(-inv)`/`--grad-ai`/`--grad-ai-wash`[暗色块端色翻转];图标渐变经 CSS `stroke: url(#ai-grad)`(lucide 描边图标),一律带 `var(--dorami-accent)` 兜底色;文字渐变共享类 `.ai-grad-text`(background-clip: text)。哆啦美速读打磨(目检拍板:否决拆句/句首符/未生成胶囊钮,原卡形态保留):生成中「⟳ 正在总结…」改**三行 shimmer 骨架**[.reader-ai-summary-skel 92/78/85% 错落行宽,复用全局 .skeleton 含暗色与 reduced-motion 降级,桌面/移动同步])→ 3.34 AI 链路优化波(日报与问答两链路的十项审视修复一次落地。**日报 reduce 改确定性渲染**:markdown 由 `render_brief_markdown` 从结构化条目直接排版[分节/来源行/总结/点评/附录格式忠实沿用原 reduce 契约],汇编段 LLM 只剩 `cross_day_dedup` 一次小 JSON 决策[对照近几天日报条目标题判 drop/「(接前报)」增量注,失败降级不查重、要求全删触发安全阀]——整篇长输出的截断/漏条/复制篡改风险根除,REDUCE_SYSTEM_PROMPT 保留仅作技能包风格契约、/api/daily-brief/pipeline 的 reduce_system_prompt 字段改回跨天查重词;日报顺修两处:map 失败条目不再以默认 3 分混入正选[降入「📎 其它收录」附录]、候选裁剪加观测[collect_candidates 回传扫描总数,last_run 记 candidates_scanned/used,被裁条目随游标跳过一事不再静默]。**检索质量**:FTS 召回引入 bm25 相关性主序[`fts_search_ranked` rowid→rank,日期倒序降为次序,两阶段取数免全量载正文——此前纯日期序会把对题旧文挤出候选池]+ 短于 trigram 下限的两字实体词(「豆包」)走标题 LIKE 补召回并集[此前静默失配]+ 检索规划带最近对话[`_history_text` 压缩注入,提示词加「结合上文还原追问再规划、追问不算闲聊」——此前每轮只看当前问句,「再展开讲讲第二点」类追问规划必失败]+ 检索档上下文预算按选中篇数摊分[单篇长文不再只喂开头 2000 字符]。**基建三件**:`[llm] aux_model` 辅助轻模型档[同端点第二模型名,凭据注册表全链+设置柜 LLM 卡输入框;检索规划/选篇、日报 map/聚类/跨天查重走 `for_aux()` 且不下发思考参数,作答/翻译仍主模型——主模型旗舰/思考档时轻调用不陪跑高延迟高成本]、`chat_completion` 支持共享连接[`client_session` 上下文管理器,日报 map 全段与翻译分段共用连接池免逐次 TLS 握手]、译文/速读缓存加正文指纹[sha256 旁键,重抓后失配即重生成,存量无指纹缓存沿用]。**读者面 AI 全局日 token 预算**:KV `ai_beta_daily_token_budget`[0=不限],`_require_reader_ai` 前置 429[只计 translate/ask/summarize,系统任务不占],与逐用户日调用限额互补护全站总成本[IM bot 代答渠道的放大器];/api/admin/ai-beta/global 扩展读写,运维 AI 面板总闸旁加「日预算」输入与今日已用读数)→ 3.35 日报权威与官源波(生产实证驱动:拉近 10 期日报+8 天素材逐条核对,官方源条目在头部名次仅 1/30、76% 条目挤在 7/8 整数分平分段[顺序实际由抓取顺序随机决定]、「官方迟到」论断被时序数据推翻[Grok 4.6/Gemini 3.7 Flash/GPT-5.6 官方全部与媒体同天入库,输在排序;真缺席的 Qwen3.8-27B/GLM-5.3 是**官源缺位**:Qwen 官博未发 27B 博文(一手=HF 模型卡+X)、智谱一手宣发迁至 z.ai/blog 而所盯 docs.z.ai 发布页停更于 GLM-5.2]。**权威机械层**[三处确定性代码,不靠 LLM 自觉]:`source_naming.source_role()` 后端角色镜像[与前端 sourceTaxonomy.sourceRoleOf 同判定序 个人→榜单→媒体→官方,scope 集合两侧须逐字同步;注册表查不到默认 media 不给官方待遇]→ BriefCandidate.source_role 穿透;①同事件代表权:dedup_clusters 簇内官方在分差 ≤1 内优先当代表[门限防官方一行推文压过媒体深度整理,官博自然赢官推],媒体退 extra_sources;②同分排序:select_top 改 `score+0.5×官方` 有界加成排序[整数分布下=同分段官方置顶且绝不跨分数段,官方外围产品照沉,score 本值不改];③跨天查重官方例外:官方条目被判 drop 机械降级为 followup「官方一手确认」[对照物只有标题分不清前报是官方还是转述,删官方=系统性压制一手信息],提示词同步注明+entries 带角色标。**日报缺陷四修**(上轮评估落地):同日重跑改**增量合并**[load_existing_brief_state 从 extensions.items 重建条目(items 补 id/source_id/source_role/followup_note 四键,历史日报按位对齐 included_article_ids 兜底)+merge_same_day 旧∪新去重→同事件聚类→有效分重排,不裁 top_n——旧行为整篇覆盖丢早间条目产残报;fetch_recent_brief_items 加 exclude_date 排除当日自身,此前今天的日报混进对照物把增量剔光]、跨天剔条**回补**[top_n+buffer(max(3,n//3)) 预选→查重→有效分裁回 top_n,回补条目天然过了跨天检查]、候选**两段式轻列取数**[游标重置/长停摆恢复不再全库正文进内存,顺删 seen_ids 死代码]、map 瞬时失败**串行重试一轮**[LLM 端点中段抖动不再把一批文章永久定格成附录裸标题]。**map 提示词**:0.5 步长小数分+厂商主次甄别锚点[头部厂商名号不加分:旗舰模型/核心 API 8-10,外围功能/客户端更新/企业案例/营销叙事压 4-6]。**新官源**:x_zai_org preset[@Zai_org,user_id 留空走 handle 解析 KV 缓存;智谱唯一可稳定采集的一手通道]+hf_qwen_models preset[Qwen 开源权重一手位];z.ai/blog 浏览器源与 x.ai/news 记 backlog;生产日报名单建议勾入 x_alibaba_qwen/x_deepseek_ai/x_openai/rss_deepmind_blog)→ 3.36 新账号 AI 默认开波(新增可配置开关「新账号默认开启 AI」:KV `ai_beta_new_user_default` 默认开,`create_user`/根管理员播种按它落逐账户 `ai_beta_enabled` 初值——只影响创建时刻,存量账户/逐账户开关/总闸互不牵动,总闸仍是即时熔断层;/api/admin/ai-beta/global 增 `new_user_default` 字段[与总闸/日预算同端点三字段独立可改],运维 AI 开关板总闸旁加「新账号默认开」ledger-switch;test_reader_ai 两个「逐账户未开 403」用例改显式关闭再断言)→ 3.37 HN 热门博客源扩充波(7 个人博客 RSS preset[sean_goedecke/giles_thomas/max_woolf/geohot/geoffrey_litt/martin_alderson/anil_dash],名单出自 Karpathy 分享的 Evan Schwartz 2025 HN 人气 RSS 榜,全批 incubating 全文 RSS;共享转换层 compact_text 围栏代码块/嵌套列表缩进保真[全站生效];3.37.1 geoffrey_litt talk 模板头部噪声剥离[日期 h3+重复标题 h1 成对剥,初抓 21 篇质检唯一站点级缺陷])→ 3.38 源合集波(发现页策展合集,方案 docs/source-collections-wave-plan.md。**定调=目录呈现层的策展视图+批量动作,不是订阅实体**——feed/MCP/未读/交付链路零感知,合集新增成员不自动推给已订阅者[「跟随合集」留二期];策展存代码注册表 `services/source_collections.py`[与 preset「代码即记录」同路线,首发五枚:hn-popular-blogs-2025 七源/frontier-labs-official 八源/cn-open-models 国产开源模型动态十三源(跨三形态)/ai-coding-tools 六源/ai-deep-writing 七源,provenance_note 支持公告白名单 markdown 子集;构思纪律=不复刻角色/形态既有轴,被毙与暂缓候选记于方案实施记录];API 三端点 `GET /api/reader/collections`[轻载荷,订阅态由前端与 sources 目录 join]+`POST/DELETE .../collections/{id}/subscribe`[批量,复刻 ensure_default_subscriptions 循环:隐藏/注册表外成员跳过回报 unavailable、include_hidden 幂等基线、单事务一次 commit;退订清水位,同属其它合集的成员也会退——无绑定记录的诚实推论];前端发现页头部「源 ⇄ 合集」二段 seg[合集视图藏形态 seg/排序,搜索过滤合集名/简介/成员名],合集卡=成员头像堆叠+n 源·已订 m+批量按钮三态[订阅全部/订阅其余 k 个/已全部订阅→hover 退订合集,退订走两击确认 4s 回落、title 如实列出将退订成员],点卡进合集详情[返回签+元信息头+成员源卡网格,SourceCard 自源视图提取共用];详情态 discoverCollectionId 提升 useReaderState——移动端注册为独立返回键历史层[返回先退详情再退发现页],批量后不做逐源乐观调整、重拉源目录校准) → 3.39 部署路径双轨波(裸机 PM2 路径自 intranet 分支回迁 main 扶正,与 Docker 并列为两条官方部署路径。动因=真实场景:一台**公网**机不便安装 Docker,而 `deploy.sh` 当时只存在于内网适配分支——它服务的已不是「内网适配」而是「宿主没有 Docker」,分支名与用途脱节。回迁面=`deploy.sh` + `ecosystem.config.js` + `production.example.ini` 的 `[server]`/`[nginx]` 两节;**`[network] disable_tls_verify` 及其 httpx 接线不回迁**——出网跳过 TLS 校验是内网 MITM 网关的专属妥协,公网开它等于自毁,intranet 分支的独有面自此收敛为该开关 + 须知块 + `.claude/` 钩子。回迁时清理三处内网痕迹:RAG 形态判定与 `[models]` 模型目录校验整段删除(向量子系统已随 v3.31 退役,`rag-embedded` extra 早已不存在,ini 若残留 `[rag] enabled=true` 会让部署去装一个不存在的 extra 而失败)、依赖安装收敛为 `uv pip install -e .` 单路径、「内网源」措辞中性化为「受限源/镜像加速」。文档 `docs/deploy-baremetal.md` 新立并与 `deploy-docker.md` 互指,两处「唯一部署路径」措辞同步纠正;example.ini 头部登记两节的**路径相关语义**(Docker 下不生效 / 裸机下是站点生成输入)。公网姿态相对内网姿态的四处反转写进文档:`disable_tls_verify=false`/`disable_ca_bundle=false`/`cookie_secure=true`/`cors.allow_origins` 具体域名——后两者在生产姿态下由启动安全校验兜底(占位符 secret 与 `*`+credentials 直接拒绝启动)。HTTPS 走两趟部署且必须用 `certbot certonly` 而非 `--nginx`:`write_nginx_site_config` 每次部署整体覆写站点文件,certbot 插进去的行会被静默抹掉) → 3.40 用户自定源波(读者自助添加私有 RSS 源,参照 Folo,方案 **docs/user-custom-rss-wave-plan.md**。定调=**私有订阅资产而非归档策展资产**(方案 B 隔离):隔离面四收口——不进公共发现页目录[仅订阅者可见]/不进 `resolve_all_visible_source_ids`[scope=all 问答、admin MCP、IM bot 全不吃]/不进日报[allowlist 天然隔离+`user_rss_` 前缀机械双保险]/不进 archive sync export,另加 articles 按 source_id 直查轻门槛[非订阅者得空,admin 不受限]。**最简正文拍板**:配置行固定 `fetch_detail_if_missing=false`,feed 给什么存什么、不分型不补抓,正文尾部一律「阅读原文 ↗」;preview 仅守门(解析出条目才能存)。技术底座全复用:`SourceConfigRecord` 加 `owner_username` 列(迁移 `d9de7994582c` 带收养回放列守卫)+generic_rss 参数驱动+读者目录 `_configured_source_meta` 既有覆盖。**同 URL 去重共享**:source_id=`user_rss_`+sha256(规范化URL)[:12][碰撞全长重建],第二人添加=订阅既有行,owner 记首建者仅溯源不承担权限;**系统源撞库检测**:撞 registry preset feed_url∪平台 config → 转引导订阅系统源,撞隐藏源统一 404「暂不可用」防影子源。服务层 `services/user_sources.py`;reader 端点四个 `/api/reader/custom-sources[/preview|/{id}]`;删除语义=退订+无人订阅即物理删(配置+文章+SourceState+各用户水位;收藏/读态孤儿沿「无害」口径)。调度=APScheduler `user_rss_refresh` interval job(间隔 KV 可配热生效 `reload_user_rss_schedule`,防重叠 max_instances=1)+连续失败≥10 自动停抓(再添加即复活)+媒体预取豁免(`schedule_media_prefetch` 按前缀滤,私有源图走 proxy 按需)。配置分档:总闸 `user_sources_enabled`(KV,默认开,关=添加/preview 403+调度跳过+runtime 能力位隐藏入口,数据不动)+刷新间隔(KV,默认 60 分钟)可配;三护栏常量(20 源/日增 10/停用阈值 10,沿 DAILY_SHARE_LIMIT 范式)。取名链补第三级 config 表兜底(`friendly_source_name`,顺修 X config 源同病)。admin 治理收敛运维管理→内容「用户自定源」区(`admin/UserSourcesPanel`:总闸+间隔写入口、KPI、源列表停用/启用/两击删除[级联清全部订阅行],`/api/admin/user-sources*` 自动入审计;读者自助动作沿 reader-surface 豁免不入审计);前端「自定源」独立第五分组(`sourceTaxonomy` custom 档,前缀短路判定,后端 source_role 无需镜像)、发现页头部「添加源」入口+`AddCustomSourceModal` 两步流(URL→预览样例+配额→确认,桌面/移动壳共用)、源栏减号与右键「移除自定源」统一走移除路径。测试 `tests/test_user_sources.py` 26 项)→ 3.41 账户管理 V2 波(150 人上量的管理面可用性,审计 M05+M06+M07+M19 一波落地,方案=docs/admin-usability-audit.md 整改分层第二梯队。**M05 批量**:`POST /api/accounts/batch`(`accounts.batch_update_users`)一次改一批账户的角色/启停/AI 开关——**原子语义全成或全不成**[任一账户不存在或整批终态活跃管理员归零即 400 回滚,末位保护按**整批后终态**裁决,天然覆盖「两个 admin 同批停用」组合;幂等项不计 updated 不 bump updated_at;上限 500],批量入审计[中间件 body 白名单加 batch 路径,语义摘要「批量关闭 AI N 个账户(名单前三)」];前端账户表加复选列+页级全选+`ledger-batchbar` 批量条[开/关 AI、启/停用、设为管理员/读者,停用与角色变更过 confirm、含自己时预警,AI 总闸关闭时 AI 批量钮让位提示]。**M06 筛选排序**:`GET /api/admin/accounts` 加 `role/status/ai` 组合过滤(与 q 叠加)+`sort/order` 服务端排序(username/created_at/last_login/logins/reads/ai_calls/ai_tokens/subscriptions;非法键回落不 5xx;summary 与 Top 榜恒聚合全量不随过滤收窄);前端筛选条[表顶 `.acct-filterbar` 三组 mini-seg]+可点排序表头[`.acct-sortbtn` th 内按钮,同列翻向/换列取惯用初始方向,aria-sort+激活列 accent-ink+▲▼]+账户 KPI 下钻[「管理员 N/停用 N」小字 `.kpi-sub-link` 点击即筛,再点还原];过滤/排序/搜索/时间窗变化归位第一页并清勾选。**M07 聚合 SQL 化**:`usage_by_user`/`reads_by_user`/`logins_by_user`/`reads_by_source` 全部改 SQL GROUP BY,不再把整个时间窗明细行载入内存 Python 累加;账户面三张 by_user 聚合同时排除删号墓碑 `deleted:*`[账户列表/活跃榜只看现存账户,墓碑消耗仍进 `ai_usage.summarize` 成本看板口径——历史成本不消失]。**M19 抽屉管理闭环**:活动详情抽屉顶部加就地管理动作行[开/关 AI、设为/取消管理员、启/停用、重置密码、删除,复用表格行同一批 handler;单账户操作若发生在抽屉打开的账户上顺手刷新抽屉快照,activity 端点 account 载荷补 `role` 字段;删除该账户即关抽屉]。Playwright 真渲染验收通过[筛选/排序/批量条/抽屉四路径,pageerror=0])→ 3.42 运维列表规模化波(审计 M08+M09+M11+M17,整改分层第三梯队;测试 tests/test_ops_scaling.py 7 项。**M08 自定源治理**:`admin_overview` N+1 消除——新 `active_subscribers_by_source` 把「每源一次全订阅扫描+每源一次用户查询」压成「一次订阅表扫描+一次用户存在性查询」,查询数与源数解耦[判定语义与 active_subscriber_usernames 逐字一致,测试守卫];UserSourcesPanel 加搜索(名称/地址/创建者)×治理状态过滤(正常/失败中/已停用)×分页(20/页,本地——载荷本就全量轻列)。**M09 运行历史真分页**:`/api/fetch-runs` 与 `/api/collection-job-runs` 响应改 `{items,total}`(破坏性换形,消费方三处同步)+ `days` 时间窗 SQL 过滤;FetchRunsTab 拉取带窗口、上限 100/200→500,total>载入量时页脚**诚实提示截断**(状态警示非常态文案)——「较老失败记录静默消失/日统计与列表矛盾」根除;完全服务端分页刻意不做(父子聚合展开模型需子运行 lazy 化,折中与触发条件记 backlog)。**M11 审计覆盖+检索**:AUDIT_PATH_PREFIXES 补 `/api/articles`(录入带标题/编辑/删除/批量删除带篇数)、`/api/fetch`(手动触发采集)、`/api/archive/import`,body 白名单同步;audit-log 端点加 `operator`(操作者子串)/`q`(跨摘要·目标·路径,contains autoescape)/`status`(ok|denied)检索,与时间窗/分页叠加;审计面板配操作者输入+搜索框+结果 mini-seg,zone-hint 描述文本撤除(M23 docstring 文档债顺手清:「尚未实现留存清理」→retention 已实现)。**M17 消息治理**:反馈收件箱 `q`(正文/提交者)+`category` SQL 端过滤(与状态/分页叠加,counts 恒全量),前端搜索框+分类下拉;公告列表服务端分页+total(默认 limit 200 兼容存量语义)、dismiss_count 只对当前页聚合,前端历史公告 6/页分页。裁剪(记 backlog):反馈批量流转/负责人/优先级、运维数据导出(归 M18)、FetchTab 节点板搜索。**目检返修(多轮,与账户 V2 返修同一审美纪律)**:「列头即操作」沉淀为共享组件 `components/admin/TableTh.jsx` 三件套[ThSort 排序/ThFilter 轮换筛选/ThSearch 就地搜索(inputWidth 可调,长 placeholder 不截断)],账户/审计/自定源三表共用一套操作语言;账户「用户」列排序撤除改列头就地搜索[排序无意义,zone-head 独立搜索框拆除];审计三控件(操作者搜索/操作搜索/结果档)全部入列头,区头只剩刷新;自定源列表从卡行重写为 acct-table 表格[列:源(定宽200+列头搜索 名称/地址)/地址(吃弹性,URL 不截断)/创建者(独立列头搜索)/状态(列头轮换+三态章)/订阅·文章·最近成功(列头排序)/操作(启停/删除小图标 hover 浮现,删除改标准确认框),usrc 卡行 CSS 类族清仓];反馈收件箱头部三轮定稿[计数紧随标题、分类 select 改与状态同款 mini-seg 右上角上下两行、搜索框在标题正下方——左右各两行高度平衡];表头工程三修[**列名纵向参差**=三种列头 display(inline-flex/inline-grid/输入框)合成 baseline 不同+cell 默认 baseline 对齐,根治=`.acct-table.is-fixed .acct-th` 定高 36px+middle;**表头行高随搜索框展开跳变**=36px 定高整个吸收 22px 输入框;**聚焦双环**=全局 input:focus-visible 外环(未分层,恒胜层内 outline:none)溢出行界像"行变高",文件尾未分层区钉掉、聚焦指示收敛为 1px accent 内描边];`table-layout:fixed` 由账户表推广至审计/自定源表,静态行 `.is-static` 去误导 pointer)→ 3.43 数据生命周期与可靠性波(审计 M10/M12/M13/M14/M15/M16/M21/M22 一波收官,整改分层第三梯队完结、25 项仅余 M18/M20/M24/M25 随量低频项;测试 tests/test_data_lifecycle.py 13 项。**M10 收口**:负责人确认内外网两台均 `[runtime] role=all`,当前无风险;顺势把 `retention_cleanup` 注册从 collector 门控解耦——lifespan 里调度器任何角色都启动、留存清理恒注册,抓取类任务(load_tasks/远程同步/自定源刷新)仍只在 collector 注册,将来真拆分部署 reader 库不再无限膨胀。**M14 留存扩面**:`collection_job_runs` 180d(与 child fetch_runs 同窗)+条件性清理三表[谓词不止时间窗:`jobs` 终态 90d(queued/running 永不清,epoch 浮点单独换算截止)、`feedbacks` 已了结 365d(open/处理中永不清)、`article_shares` 已失效(撤销或过期)180d(存活含永久档永不清)];**刻意不清** `announcement_dismissals`(行数有界且承载「一次性通知」语义——公告下线复上不应再打扰关闭过的读者)。**M21 计量唯一约束**:`ai_usage`(day×user×purpose×model)/`reader_reads`(day×user×source)建唯一索引,迁移 `a7e2f95c1d40` 先对存量重复行求和合并再建索引(带收养回放守卫);写侧 `record_usage`/`record_read` 改 SQLite `INSERT…ON CONFLICT DO UPDATE` 原子累加,「先查后插/递增」的并发双插与丢增量竞态根除。**M13**:`/api/source-health`(45s 轮询)运行史回退只查「无 SourceStateRecord 快照」的节点,不再无条件把全部节点 180 天运行行载入内存(自定源照旧不回退)。**M15 刷新语义零新 UI**:AdminOpsTab 接 `active` prop——子页间切换重取目标子页、从其它 Tab 切回重取当前子页(消息子页经 wakeTick 换 key 强制重挂面板自取数),长开运维台不再静默过期。**M16**:`summarize` 的 `by_day_user` 服务端 Top-6(按窗口 total_tokens)+按日聚合「其它」行,载荷有界总量守恒,前端 pivotDaily「其它」中性槽无缝承接、`by_user` 全量榜不受影响。**M12**(落网之鱼,前波「未动名单」漏记):FetchTab 组头全选与单行同一口径——组头复选集只含 `!user_source` 组内成员(全自定源组直接禁用),`runChecked`/`saveCheckedAsJob` 消费端再过滤一层防御。**M22**:单用户抽屉最近登录兜底改 `last_login_for_user` 单人 MAX 标量查询(此前为看一个人全表 GROUP BY)。烟测实证:隔离栈 Playwright 断言五个刷新时机(网络请求计数)+五组组头复选态,pageerror=0;旧 smoke.db 走真实启动路径自动升至 `a7e2f95c1d40` 双唯一索引在位,生产升级路径实证通过)。

补丁节点:3.23.1 阅读器滚动条修复——文章/动态中栏经发现页或社交流条件卸载后重建时，自绘滑块重新绑定新 DOM，修复内容滚动但滑块停滞的偶现问题。3.23.2 Meta AI 列表解析修复——按单文章边界跳过任意语言 CTA 并固定请求语言，从详情页回退标题/发布日期；Meta 显式开启权威元数据刷新，再抓可修正已归档的错标题或抓取时间伪日期，正文不覆盖。3.23.3 动态空态口径统一 + 预置订阅补动态/社交各一源。3.24.1 阅读器切容器残影修复——loadArticles 失败时 catch 不清列表，上一容器旧条目会被新容器错误渲染（文章条目被 SocialFlow 当推文画出、卡片显示非社媒源名；请求成功时骨架屏遮挡故从不显形，需网络级失败触发，内网含内容审查网关的链路首次暴露）；切作用域 effect 先清 articles/articlesTotal 再发请求，失败呈干净空态+Toast。3.25.1 节点管理两优化——吸顶组头透缝修复[Chromium 的 sticky top:0 以滚动容器**内容边**为基准，`.board` 的 4px 顶部 padding 成为吸顶组头上方的透视缝，滚动时节点行从缝里露出；顶部 padding 归零，呼吸空间由组头自身顶距承担]+ 信号条角色筛选[官方/媒体/个人/榜单第二筛选轴放进既有信号灯统计条，与健康灯正交：同 `.signal-stat` 语法、3px 色标条与分组组头 marker 同色呼应，点选过滤/再点还原；健康灯计数随所选角色范围收窄，角色计数恒全量与组头一致，当前健康档在该角色下为空时自动回落「全部」]。3.29.1 移动波收口——社交流 headless 化[SocialFlow 加 headless:移动顶栏统一承担三容器的 seg/标读/搜索控件行,卡片流本体与桌面同源,双层头部合一;桌面社交头原样]+ 设置柜移动页面栈[components/mobile/MobileSettings:一级分组清单→二级分区页,分区组件与桌面设置柜完全同源(components/settings/),移动壳恒读者观感、管理组(数据同步/凭据)不下放;「我的→反馈」深链直落分区并 mark-seen]+ 返回键握手[hooks/useLayerHistory:正文页/发现页/抽屉/动作单/设置两级共六层接浏览器历史——层开着按返回=关最上层而非退出站点(微信内浏览器肌肉记忆),程序性关层消费对应历史条目不堆栈,模块级栈协调多层、与 App hash 路由零干扰]+ 长按单「分享给…」[Web Share 环境注入,载荷=站内深链;不支持不画]+ 登录页 ≤480 轻档[排版收纳,星座环境漂移与跃迁终幕原样——静谧版而非残缺版]+ 公告横幅 safe-area 顶距;顺修 .m-page 壳外取不到 --rd-* 变量的设置栈透底缺陷[变量作用域 .m-shell 扩至 .m-page]。3.29.2 转换层富格式补课——共享 HTML→Markdown 转换层(article_extractor)三处保真升级,全站源一处生效、只惠及新抓正文(存量在意则重抓):<pre>→围栏代码块[缩进/空行原样保留,语法高亮 token span 不破坏空白,language- class 识别为围栏语言标注,内容含 ``` 自动加长定界符;此前 pre 走段落分支被折叠空白]+ <code>/kbd/samp/tt→反引号行内代码[含反引号内容按 CommonMark 加长定界符垫空格;表格单元格「保 code」自此真实成立]+ 有序/嵌套列表[<ol> 保编号含 start 起点,嵌套子列表按父项标记宽度缩进,<br> 多行改同项续行;此前一律压平成 - 平行项]+ 嵌入媒体占位链接[iframe/video/audio 移出 _MD_SKIP_TAGS,降级为「▶ 视频/▶ 音频/嵌入内容 ↗」可点原链而非静默丢弃,块级/行内(p/figure 包裹)双路径覆盖;无 http(s) 地址与 svg 仍丢弃,播放器子树仍不渲染]。前端零改动(react-markdown 原生渲染围栏,.markdown-body pre/code 样式已在);语法高亮未加(需新依赖+亮暗主题适配,独立决策)。3.33.1 日报空正文事故修复——LLM 思考模式参数 + 空产三层防护[生产 2026-08 事故:LLM 切 deepseek-v4-flash(思考型,默认思考 effort=high)后日报 reduce 的 completion 被思考吃满 8192 上限,`message.content` 空串→客户端只拦 None 放行空串→`db_storage.save()` 的 summary 兜底把空串写成 NULL 且 has_content=True、status=success 照写,阅读器「暂无正文」而神灯流水线消费 extensions.items 无感;修复=①`[llm] thinking_mode` 配置(空=不发送/disabled/low/high/max,凭据层+KV+env `DORAMI_LLM_THINKING_MODE` 全链,client 翻译成 `thinking:{type}` + `reasoning_effort`,端点不支持 400 自动降级去掉重试,设置柜 LLM 卡加选择器)②client 空串 content 与 None 同判抛 LLMError、finish_reason=length 打截断告警 ③`_persist_brief` 落库前断言正文非空——失败不写库不推游标,候选下轮重来]。3.32.1 测试库隔离修复——「重启后 AI 已开启状态丢失」的真凶不是重启而是**跑测试**:tests/test_mcp.py 三个测试播种账户前漏了 monkeypatch db_sink,「删除重建 admin/user」直接砸在真实 data/cms_data.db 上(dev 管理员密码被重置为 admin、ai_beta_enabled/头像清零;多年注记「test_mcp admin 登录失败是环境使然」即此因)。修复=三处补 tmp sink + **tests/conftest.py 会话级沙箱**(收集期把 DORAMI_CONFIG_FILE 指向临时 ini,任何疏漏测试都砸不到真实库;外部显式设置时尊重不覆盖);验证:全套跑完真实库 users/articles 快照 byte 级一致。3.33.2 问答语料称呼修复——scope=all 的降级检索说明曾硬编码「读者订阅内容」(`subscription_context` 两档共用同一文案),内网 IM 机器人(all 档代答渠道)据此回出「基于你订阅的文章」而 IM 用户并无订阅概念;`corpus_label` 参数让措辞随检索域走(all=「哆啦美收录内容」,subscription 默认不变)+ QA 系统提示词时效示例话术中性化(「你订阅的内容里…」→「与此相关的最新资讯是…」,防模型照抄归属)。同波拍板:**IM 机器人问答档位 subscription→all**——策展旋钮从未被使用、且有「平台里有但 bot 订阅外→答不知道」的诚实性缺陷,all 本就是 v3.32 给代答场景的预留原语;dorami-bot 的订阅自此仅日报 dfeed 拉取依赖(日报源必须保持订阅)。3.36.1 MCP 管理员令牌空作用域修复——feed_service 管理员 dfeed_ 分支曾返回 `[]` 表「未限定来源」,与 mcp_server「空列表=零订阅应返回空」契约相撞(`source_id IN ('__none__')`),管理员令牌经 MCP 检索恒空而聚合 feed 正常,表现酷似「账号没订阅」;内网 bot(p_dorami 管理员账号)接入实锤。修复=admin 分支改返回 `resolve_all_visible_source_ids` 显式枚举(与问答 scope=all 同口径,隐藏源照旧排除),`[]` 歧义哨兵消灭、空列表自此只有「零订阅」一种含义;附带收益:管理员令牌接 bot 时新上源自动进检索域,免「全量订阅+新源补订」运营纪律。3.39.1 裸机依赖钉版——v3.39.0 首次公网裸机部署即撞上 **mcp 2.0**(2026-07-28 把 `FastMCP` 改名 `MCPServer` 并变更 API):`deploy.sh` 走 `uv pip install -e .` 现解依赖,按 `mcp>=1.0.0` 装到 2.x,后端 `ModuleNotFoundError: No module named 'mcp.server.fastmcp'` 起不来(表现为 127.0.0.1:8088 connection refused),而同版本 Docker 镜像因走入库钉版清单安然无恙——**「宿主现解依赖」是裸机路径的固有弱点,不是这次的偶然**。双修:①`deploy.sh` 改为按 `docker/requirements.txt` 装运行时依赖 + `uv pip install -e . --no-deps` 装项目本身,两条部署路径自此共享同一份版本事实来源(清单缺失时退回现解,由 pyproject 约束兜底);②pyproject 的 mcp 加 `<2` 上限(代码用 1.x FastMCP API,上限是硬约束而非保守),重导出清单后版本未变(仍 mcp==1.27.1)。 3.39.2 日报 map 锚点返修——生产 0827 日报漏报「Claude in Chrome is generally available」(claude.com 官博):逐环节排查(抓取/名单/候选/聚类/跨天查重全清白)锁定 map 评分,生产模型重打 7.5/8.5/8.5 摇摆正卡 8.0 入选线——v3.35「厂商主次甄别」的「外围产品小功能、客户端更新压 4-6」按字面正中浏览器扩展 GA 公告,而 7-8.5 档只点名模型/API,重大产品里程碑落在锚点缝隙。MAP_SYSTEM_PROMPT 三改:①压分条款收窄为「企业客户案例/区域上线/营销叙事/周边生态/纯增量小修小补」(删「外围产品小功能、客户端更新」字面);②新增「Agent/产品线正式 GA、重大里程碑发布」7-8.5 正向档;③新增风向标厂商例外——OpenAI/Anthropic(Claude/Claude Code)的产品功能即使体量小也常有巧思/通用性/范式级讨论,按「新交互范式/新能力面/广泛适用性/讨论度」评估不机械压分。生产模型双向验证:目标文 8.5×3 稳定过线,边角料回归(QwenWork 公测 6.5/获奖认证 4.5/合作稿 5.5)仍远低入选线、压分未破。漏报当期不回补(游标语义,拍板)。 3.38.1 迁移链多头容忍——内网分叉仓自带迁移支线时,合入 main 新迁移后 DAG 双头(git 零冲突),启动路径 `ensure_migrated` 的 `upgrade("head")` 无条件报 "Multiple head revisions" 起不来;改为检测多头即 `upgrade("heads")` 并行全升(main 单链下恒等价于 head,纯为下游分叉仓兜底);同轮更正一处误判:「create_all 出生无版本库被收养回放会撞重复列」不成立——迁移链本按收养回放安全纪律写,test_ensure_migrated_adopts_legacy_db 即该护栏,勿再给收养路径加「探哨兵 stamp head」类捷径(会跳过带数据语义的迁移)。 3.40.4 P0 安全收口波——管理面可用性审计(docs/admin-usability-audit.md,Claude+codex 双独立审计 M01–M25)的 P0 四项一次修复:**M01** `/api/mcp/toggle` 曾对读者裸奔(处理器无授权检查,`/api/mcp` 在 READER_API_PREFIXES 短路放行)→单列进 `account_admin_required` 并入审计前缀(摘要「切换全站 MCP 总闸」),status 保持读者可读;**M02** summarize 三层护栏穿透(端点不接日配额+`_AI_DAILY_CALL_LIMITS` 无键+`VALID_PURPOSES` 漏登记致 record_usage 静默丢行,全站日预算永远读到 0)→三处补齐(50/日),读者面新增 AI 用途须同步三处的纪律注释入 VALID_PURPOSES;**M03** 删号残留与同名继承→`delete_user` 单事务收口,**语义拍板=资产物理删除+计量墓碑化**(订阅/令牌/收藏/公开分享/读态水位/反馈/公告 dismiss 物理删,自定源随退订走「无人订阅即物理删」`purge_account_user_sources`,AI 用量/阅读/登录事件改写 `deleted:<原名>` 保看板与成本历史,审计日志留原名),router 两段提交废除;**M04** 改密不吊销会话/删号重建复活旧 Cookie→`users.session_epoch` 会话世代列(迁移 `b4a1c7e5d2f8`),token 带 `gen` 且校验须与列一致——改密/重置即吊销全部既有 Cookie(自助改密就地续签本人不断线),建号随机世代令同名重建不复活;存量行/旧 token 按 "" 对齐、升级不强制重登。两个历史假绿灯测试(读者 toggle 断言成功、summarize 只断言传参不断言落库)同步修正,新增 7 项护栏测试。 3.43.1 交叉检视返修——v3.43.0 经 codex(gpt-5.6-sol xhigh)独立检视抓出 6 个真问题一次修复(讨论达成一致后执行,终审通过):**高·墓碑撞唯一键**[v3.43 计量唯一索引 × v3.40.4 删号墓碑化的交互回归,codex 实证复现:同名账号「删→重建→同维度计量→再删」时 delete_user 裸 UPDATE username→deleted:x 撞聚合键唯一索引抛 IntegrityError、整个删号事务回滚接口 500;修复=`_merge_into_tombstone` 合并式墓碑化——逐行 ON CONFLICT 累加进墓碑键再删原行,计数守恒、updated_at 取较新值,LoginEventRecord 无唯一键仍裸改写];**中·M10 解耦做一半**[retention 回调夹带的自定源孤儿 GC(物理删配置/文章)随全角色注册跑到 reader 上,且协程回调同步删表阻塞事件循环;修复=自定源维护加 runtime_collector_enabled() 门控(不看总闸,F8「关闸后 GC 无人收」语义保留)+两段清理均 asyncio.to_thread];**中·wakeTick 换 key 丢草稿**[强制重挂销毁管理员写一半的公告/回复草稿;改 refreshTick prop 只驱动两面板加载 effect,不重挂——草稿/检索词/展开态全保留];**中·loader 竞态**[多刷新时机下旧响应后到覆盖新快照;AdminOpsTab 七 loader 各自 claimGen keyed 代次守卫(实例级 useRef,错误 Toast 也过守卫),两消息面板另加本地代次 ref];**低·「其它」用户名碰撞**[「其它」是合法用户名,进 Top 时尾部用户被并进同名桶污染系列;聚合桶键改冒号 sentinel `other:`(用户名禁冒号天然不撞),前端渲染层映射(sentinel→「其它」/真实「其它」→「其它（用户）」),用户维度 pivot 改 topN=Infinity 消除前端二次裁剪(后端已裁 ≤7 系,二次裁剪会再挤真实用户并回桶甚至产重复 key)];**低·分享双时间戳**[过期已久又被补撤销的链接多留一窗;谓词简化 or_(revoked_at<cutoff, expires_at<cutoff),NULL 比较自然为假]。测试补 4 项+2 处反假绿灯加固:墓碑回归/双时间戳/「其它」不并桶/**upsert SQL 结构断言**(record_usage/record_read 必须是单条 ON CONFLICT INSERT 且写前无本表 SELECT——防将来悄悄改回读改写)、M13 测试加 SQL 参数监听断言收窄本身(有快照节点 id 不得进运行史查询参数)。烟测补「切走切回草稿保留且数据仍重取」断言。残余记 backlog:「其它（用户）」极低概率 identity 碰撞(彻底解法=图表层 identity/display 分离)、拆分部署下自定源归属故事未定义。 3.43.2 前三波补充检视返修——codex 对 v3.40.4/v3.41.0/v3.42.0 三波实现补做交叉检视(当时只有测试+烟测未走检视环节),抓出 7 个存活问题+终审再拦 3 个残余,全部达成一致后修复:**高·详情抽屉操作错账户**[快速点开 A→B 时 A 的迟到响应错挂进 B 的抽屉,动作按 detailData.account 实际操作 A;修复=claimGen('detail') 代次守卫+closeDetail 统一关口(bump 代次作废在途)+**detailUserRef**(终审拦截:refreshDetailIfOpen 在操作 handler 的 await 之后调用,读 state 是旧 render 闭包——操作 A 在途时关 A 开 B 仍会作废 B 的请求并挂 A 数据,经 ref 读最新抽屉归属)];**中·AI 成本闸误拦缓存**[translate/summarize 的逐用户配额+全站预算前置在缓存判定之前,缓存命中零成本也 429;修复=授权(_require_reader_ai)与成本闸(_enforce_ai_cost_gates)分拆,成本闸经 service 的 pre_llm_check 回调在缓存 miss 后、真实 LLM 调用前执行,ask 无缓存端点直调;check-before-call 的并发 TOCTOU 窗拍板为**软上限**记 backlog——硬上限需原子预留-核销下沉 LLM 调用层,内网收益低];**中·公告假分页**[前端仍 limit=200 全量拉+本地 slice,第 201 条起永久不可见、末页删空卡空页;修复=真服务端分页 load(page)+total+发布回第一页+越界回退];**中·删号漏用户名级 KV**[reader_defaults_seeded:/feedback_seen:/user_sources_added: 三类标记残留——同名重建拿不到默认订阅播种、继承旧身份当日自定源额度;修复=delete_user 同事务清理,**LIKE 通配符转义**(终审拦截:用户名允许下划线,不转义则 a_b 删号误删 axb 的 key,escape='\\\\' 三字符转义+专项测试),key 格式由测试与三处权威常量比对钉住];**中·审计面板迟到响应覆盖**[非第一页改检索条件时「新条件旧页」与「归位第一页」并发,旧页后到覆盖;loadGenRef 守卫 then/catch/finally];**低·批量审计人数失真**[摘要按原始数组计数而服务层 strip/去空/去重,body 超 32KB 未采集时伪造 0;修复=同口径规范化+「数量未知」如实记录];**低·审计摘要规则错配**[/api/fetch/batch 被通用规则记「手动触发采集节点 batch」、文章路由 {article_id:path} 含斜杠 ID 被 [^/]+ 拒配退化空摘要;修复=batch 精确规则前置+文章规则吞全路径+app.py body 白名单补 /api/fetch/batch]。前三波其余重点逐项 verdict=OK:M01/M04(会话世代无旁路)/M05 数据事务语义(双 admin 同批停用等组合正确)/M06/M07/M08(订阅者映射逐字一致)/M09(消费方全换形)。测试 +8 项(册内 21 项),全套 671 过;TOCTOU 软上限与拆分部署自定源归属记 backlog。codex 额度耗尽于最后一眼,三处终审返修按其点名修法实现+自查推演/专项测试代替签字。 3.43.3 站内深链登录门 hash 导航丢失修复(issue #2)——用户实报:非登录态点深链,登录(密码或内网 SSO)后落空白阅读器空态而非目标文章。Playwright 逐路径排查:新 tab 打开/登录门刷新(v3.33.3 修过)/admin/读者/移动端标准路径全部正常,唯**停在登录门的 tab 被同源 hash-only 导航带入深链**(IM/微信内嵌 webview 复用已开 tab、或往登录页 tab 地址栏粘链接)时不 remount——`deepLinkArticle` 只在 App mount 一次性取值,运行时深链承接的 popstate 监听又只在 authenticated 后注册,登录门阶段 hash 变化无人接,登录后 commitNav 重播覆写 hash,id 永久丢失。修复=App 补登录门专属 hashchange 监听(非 authenticated 阶段挂载,见深链即 setDeepLinkArticle,非深链 hash 不动、与 onPop 同口径),登录后由既有链路(admin 界面切换+initialArticleId 消费)接管。排查顺带确认:深链指向自定源(`user_rss_`)文章而收件人非订阅者时同样呈空态,那是 v3.40 隔离语义的设计内静默(fetchArticle 404→silent),不属本 bug。codex(gpt-5.6-sol)交叉检视抓出 2 项返修,实证后修复、复检通过:**高·admin 覆写竞态**[登录门 hashchange 路径不更新 hadDeepLink,认证后深链 enterReader effect 与 default-surface effect 同 commit 先后跑、后者读同轮闭包 surfaceMode===null,default_surface=console 时把刚设的 'reader' 覆写回 'console'——Playwright 实证文章在隐藏阅读器被消费(titleVisible=false/adminRailVisible=true);修复=default-surface 判定纳入 deepLinkArticle 并加依赖];**低·登录门陈旧深链**[捕获深链后用户退回非深链 hash 再登录仍开旧文章;登录门监听改镜像当前 hash(非深链即清空 pending)——与 onPop 的「非深链不动」刻意不同口径:登录前无在途消费,不清才是错的]。

**v3.44 资讯分析与个人早报波**：文章可靠落盘后由租约 worker 异步生成评分、摘要、内容类型、规范标签、Candidate evidence 和最多 6 个灵活展示标签；受版本控制的 96 项 Taxonomy v1 目录覆盖 Topic/Industry/Entity、Alias、层级、Entity 类型和用户可选资格，必须经目标库审核回执和人工发布。读者兴趣简化为“关注 / 屏蔽”，首次登录引导与左侧工具栏共用目录；个人早报严格冻结订阅范围，兴趣最多 50%，不足由质量补齐，无订阅不从公共池补。历史重跑走可暂停/恢复的 `full_analysis` 作业；公共日报 adapter 保留 off/on 兼容开关且默认 off。上线与历史兼容见 `docs/taxonomy-v1-deployment.md` 和 `docs/full-analysis-backfill.md`。

## 文档地图(分层索引,L0)

本文件 = 架构简报 + 开发命令(Claude Code 自动加载);`AGENTS.md` 是其它 Agent 的同款入口(指回本文)。
更细的文档走三层索引,**每层概括下一层,无需逐篇 grep**:

- **L1 [`docs/README.md`](docs/README.md)** — 全量文档一行摘要 + 状态签(活跃/耐久参考/归档)。
- 高频直达:[`docs/backlog.md`](docs/backlog.md)(**跨波次待办总账**——下一步做什么/搁置原因);
  [`docs/frontend/conventions.md`](docs/frontend/conventions.md)(改前端必读纪律,见下节);
  [`docs/sources/curation_policy.md`](docs/sources/curation_policy.md)(源准入/观察期);
  `docs/contracts/*`(feed/订阅/归档同步三契约);[`docs/configuration.md`](docs/configuration.md)(ini 配置)。
- **L2** 子索引:[`docs/archive/README.md`](docs/archive/README.md)(已完结方案按故事分组——**查决策来龙去脉;
  勿据归档文档判断现状**);[`docs/sources/candidates/README.md`](docs/sources/candidates/README.md)(候选源 13 册的覆盖与消化状态)。

## Architecture Overview

**DoramiSourceArchive** (哆啦美·归档中枢) is an AI content aggregation CMS. It fetches content from multiple sources, stores it in SQLite, and serves it through a reader with FTS5 full-text search and LLM-assisted QA. It splits into two cooperating layers — a **collector/archive** side (fetching, archival) and a **reader/distribution** side (per-user subscriptions, full-text search, tokenized feed/MCP delivery) — gated primarily by login account role (`admin` superuser vs restricted `user` reader), with an optional deployment runtime-role axis for split deployments (see *Access control — login account role*).

### Core Data Flow

```
Fetcher → DataPipeline → DatabaseStorage (SQLite via SQLModel)
                              └─ FTS5 external-content 虚拟表(articles_fts,trigger 同步)
```

检索基础设施只有 SQLite 自身的 FTS5 全文索引(`storage/fts.py`,建表随 `DatabaseStorage.__init__`/Alembic 迁移,trigger 保证与 articles 表结构性一致,零对账)。向量化/ChromaDB 支路已于 v3.31 退役(见 *问答检索与 RAG 退役*)。

### Key Design Decisions

**Dual-dimension content identity**: Every piece of content carries both `content_type` (data shape — `arxiv`, `wechat_article`, `tech_conference`, etc.) and `source_id` (which channel produced it — `wechat_jiqizhixin`, `webhook_dify_workflow`, etc.).

**Article analysis and governed taxonomy (v3.44)**: Article persistence is the reliability boundary; analysis is queued afterward and processed by restart-safe leases in `services/article_analysis.py`. A succeeded current asset contains quality/summary/genre plus ordered canonical assignments and up to six extracted display tags. Canonical recall uses names, aliases and `prompt_description`; extracted tags may remain flexible display/search chips without becoming long-term interests. `services/taxonomy.py` owns global taxonomy versions, aliases, parent relations, Candidate evidence/decisions, publication and retag jobs. The approved v1 catalog is repository-controlled; neither bootstrap nor automatic activation may silently publish product vocabulary.

**Personal brief selection (v3.44)**: User interests are `follow` or `mute`; all three canonical facets are initially selectable, while taxonomy policy may later cap hot choices per facet. A personal edition freezes the user's subscribed source IDs and interest revision. Interest-matched items occupy at most half the target, quality fills the remainder, mute is a hard exclusion, and an empty subscription scope stays empty. Edition items are immutable snapshots. The public daily brief remains a separate product; its adapter is a rollback flag for new runs only.

**Plugin-based fetcher discovery**: `FetcherRegistry` (`src/fetchers/registry.py`) auto-scans `src/fetchers/impl/` for `BaseFetcher` subclasses at import time. Any new fetcher placed there with `source_id`, `content_type`, `name`, `icon`, and `description` class attributes is automatically registered and surfaced in the frontend. The frontend dynamically renders parameter forms based on `get_parameter_schema()`, whose return format is:
```python
[{"field": "limit", "label": "单次获取上限", "type": "number", "default": 5}]
```

Three fetcher base classes cover the major source types:
- `BaseFetcher` — base for all fetchers; provides `_safe_get`/`_safe_post` with retries
- `BaseWebPageListFetcher` (`webpage_fetcher.py`) — scrapes an HTML listing page; subclasses declare `listing_url`, `article_url_patterns`, and optionally set `fetch_detail=True` to extract article body from the detail page. Optional knobs: `drop_empty_content=True` discards entries with no body (nav/footer junk), and `max_listing_pages` + a `_next_listing_page_url()` override paginate the listing (e.g. Cursor's `/changelog/page/N`) to accumulate enough entries for `limit`
- `GenericGitHubReleasesFetcher` (`github_release_fetcher.py`) — hits the GitHub Releases API; `PresetGitHubReleasesFetcher` subclasses hard-code `owner`/`repo` as built-in sources

**Optional crawl4ai Web Content backend (`src/fetchers/web_content/`)**: A `WebContentBackend` abstraction (`backend.py`) unifies "given an article URL → clean body". Two impls: `LegacyArticleExtractorBackend` (httpx, the default/baseline) and `Crawl4AIContentBackend` (headless-browser via the optional `crawl4ai` extra — **not** a default dep). The crawl4ai backend renders + scopes by a per-site `CrawlProfile` (`profiles.py`: `target_elements`/`excluded_selector`/`wait_for`), then runs the project's own `node_to_markdown` over `cleaned_html` (consistent image/lazy-load handling between both paths); it also exposes `render_html()` (raw rendered DOM, anchors intact — used by C-class single-page-split fetchers' segmenter fallback and OpenAI's Cloudflare path) and `extract(url, profile=...)` (explicit profile injection). It's strictly opt-in: a fetcher sets `web_backend_enabled=True`, `BaseFetcher.fetch()` lazily starts/stops the browser, and `_web_backend_detail()` routes detail extraction through it — **falling back to legacy httpx whenever crawl4ai is absent / no profile matches / extraction fails**. So the default environment (no crawl4ai installed) behaves exactly as before. Migrated B-class detail nodes (`web_anthropic_news`, `web_ithome_ai`, `web_qbitai`, `web_claude_blog`, `web_aiera`) and `rss_openai_news` (crawl4ai-first CF bypass, Playwright fallback, summary last) use it.

**Config-driven web fetcher (`generic_web`, the "中级目标")**: `ConfigurableWebFetcher` (`configurable_web_fetcher.py`) is the single, config-driven web fetcher — the `GenericRssFetcher` analogue for web sources. Adding a new website = writing a `SourceConfigRecord` (config), not a new Python subclass. It reads all source identity/config from runtime params (`listing_url`, `article_url_patterns`, detail `CrawlProfile`, optional `listing_css` CSS schema, governance metadata) and delegates discovery to `BaseWebPageListFetcher`'s heuristics (anchor + embedded-JSON), with the optional CSS schema as a precise fallback. `resolve_source_fetcher_id` routes `source_type` web/webpage → `generic_web` (rss/atom → `generic_rss`); `POST /api/source-configs/fetch-active-web` batch-triggers active web sources. Validated to reproduce existing dedicated nodes (discovery URL-set identical, detail byte-identical when given the same profile). **Frontend entry is gated off by the `is_template` flag** (all five `generic_*` fetchers carry `is_template = True`; `App.jsx` filters them from the node catalog — decided 2026-07: generic parameter-driven nodes stay backend-only as source-configs/source_builder execution substrate and as code templates; the sanctioned way to add a source is a hardened preset fetcher).

**AI node onboarding (`source_builder`, the "高级目标")**: `src/services/source_builder.py` turns an arbitrary listing-page URL into a固化 config node: `analyze_url()` detects page type (rss/web/json), collects HTML structural signals, produces a heuristic baseline config, then (when LLM is configured via `daily_brief.resolve_llm_config`) refines it via LLM and analyzes a sample article page to propose the detail `CrawlProfile`; `preview_config()` trial-runs `generic_web`/`generic_rss` for a no-persist sample. Endpoints `POST /api/source-builder/analyze|preview` (collector-gated); save reuses `POST /api/source-configs`. LLM/crawl4ai are both optional (graceful degrade to heuristic/legacy). **Frontend entry (`CustomNodeBuilder.jsx` + the FetchTab "AI 自定义节点" panel) is currently gated off** via `ENABLE_CUSTOM_NODE_BUILDER=false` — backend-only for now.

**`extensions_json` serialization pattern**: `serialize_to_metadata()` splits a content object's fields into base fields (from `BaseContent`) and subclass-specific extension fields. The extensions are serialised as a JSON string into the `ArticleRecord.extensions_json` column. When reconstructing a content object from a record(如导出/加工场景), a `GenericContent` object is used since the ORM only stores the flat record.

**Playwright browser-rendered detail (Cloudflare bypass)**: Most fetchers are pure httpx, but a few sources gate their article bodies behind a Cloudflare Managed Challenge that only a real browser can pass (httpx gets a 403 challenge shell). `src/fetchers/impl/playwright_renderer.py` provides `PlaywrightRenderer`, an async context manager that lazily launches a headless Chromium for the duration of one fetch run, then renders each blocked article: it opens a fresh page per article, throttles requests, polls until the challenge clears and the body text appears, retries, and returns `""` on any failure so the caller degrades gracefully. Currently only `OpenAINewsRssFetcher` uses it — it overrides `_detail_for_url` to prefer the rendered body and fall back to the RSS summary when rendering fails (`openai.com` is the one audited source behind this challenge). Playwright is an opt-in path: when a node needs no detail fetch, no browser is started. (Note: the legacy WeChat Official Account Playwright login fetcher has been removed; only the `WechatArticleContent` type and the `wechat_article` display label remain for historical archived data.)

**媒体库（图床，media store）**: 正文外链图片的本地缓存层（2026-07 图床波，推翻早前「外链直连、不代理」决策）。**归档正文里的原始图链从不改写**（档案忠实性、导出契约不变）——显示层统一经 `GET /api/media/proxy?url=` 取图，`src/services/media_store.py` 负责「URL → 本地缓存文件」：`MediaAssetRecord` 一行一 URL（主键 `url_hash=sha256(url)`），落盘按 `content_hash=sha256(字节)` **跨 URL 内容去重**（`data/media/{hash[:2]}/{hash}{ext}`，删除文件前需查引用）。供给三径共用 `get_or_fetch`：代理端点命中即回文件（`Cache-Control: immutable`）、未命中**即时下载**、抓取入库后 `schedule_media_prefetch`（挂在 `run_fetcher_with_tracking`，fire-and-forget）**随文预取**新文章图链。下载一律带**推导的站内 Referer**（`_referer_for`：`i.qbitai.com` → `https://qbitai.com/`，剥 i./img./cdn./mmbiz 等子域前缀）——防盗链 CDN 无 Referer 即 403，2026-07-20 实测 qbitai 403→200、一举消掉当时失败量的大头。防护：仅 http(s) + SSRF 拦截（环回/私网/链路本地拒绝；**豁免 198.18.0.0/15 fake-ip 段**——本机代理 DNS 接管时一切域名解析到该段，实测误杀后修正）+ 流式大小上限 + **魔数嗅探**（防把 CF 挑战页缓存成图）；失败行是负缓存（`status=failed`，退避窗口随 fail_count 放大，封顶一天；`force=True` 绕过冷却供定点重抓）。降级三层：后端失败 302 回源 → 前端 `ReaderMarkdown` onError 回退原链直连 → 裂图占位；`[media] enabled=false` 时 `media_store` 为 None，全链路退回外链直连。

**存量策略（2026-07-20 拍板）**：生产**只做随文预取，不跑全量回填**——回填对同批域名突发易触发反爬，且死链各吃满超时导致极慢；`POST /api/admin/media/backfill` 端点保留作脚本化应急通道，**前端入口已撤**。存量补录改走**媒体热点图**（运维管理 → 内容 → 媒体库，`MediaHeatmap.jsx`）：GitHub 式 53×7 逐日格阵，格子深浅 = 当日图片缓存覆盖率（`--heat-0…4`，取 accent 靛明度阶，不引第二饱和色系），右上角三角 = 当日有失败（深浅与异常两条正交通道）；点格开抽屉（`ledger-drawer` 语法）看当日逐篇明细 + 失败 URL 与原因（据此区分反爬 / 死链），每篇可**单篇定点重抓**（无突发压力）。数据现算不落表（`GET /api/admin/media/heatmap|days/{date}`：正文提图链 → 按 `url_hash` 批查 → cached/failed/pending 三态），当前归档规模亚秒级，规模涨了再加物化列。注意 archive sync 尚不携带媒体文件（内网 reader 吃到图需未来的媒体伴随包，见 backlog）。

**社交媒体流(X 社交波,v3.12)**: 阅读器的**第三容器**。`content_shape` 从 `article|bulletin` 扩为 **`article | bulletin | social`** —— 「动态」装的是 changelog/release notes/GitHub trending(短条目扫读形态),推文是卡片流直读形态,渲染差异大到要在容器内再分叉,就说明本不该是同一个容器(2026-07-20 用户目检后拍板)。全链路:fetcher 类属性 `content_shape` → `registry` → `api/sources.py:source_shape()`(三态互斥,`social_post` 兜底为 `social`;注意**不可再按「非 bulletin 即 article」二分**,那会把社交源误归文章容器)→ `GET /api/articles?shape=` → 阅读器 `mode`/视图轨/源栏分组。

采集走 **X API v2 官方按量付费**(`GET /2/users/{id}/tweets` + 应用级 Bearer,凭据 `DORAMI_X_BEARER_TOKEN`,不入库/不进 Git/不打日志)。`src/fetchers/impl/x_timeline_fetcher.py` = `XTimelineFetcher`(`is_template=True` 模板,参数驱动 `handle`/`user_id`)+ 8 个 `PresetXTimelineFetcher` 子类(V0 策展名单,全批 `incubating`)。**双路径与 RSS 完全同构**(`GenericRssFetcher` + 23 个 preset 并存):preset 类 = 策展名单(代码即记录),`SourceConfigRecord`(`source_type=x|x_timeline` → 路由到 `generic_x_timeline`)= 管理面自助加账号、不改代码不部署。**一账号一 `source_id`**(`x_karpathy`,平台前缀天然承接 `mastodon_*` 扩展)—— 订阅粒度/源栏/发现页/feed 交付范围全建立在 `source_id` 上,合并成单一源会让读者无法逐账号取舍。抓取策略:`exclude=replies` 保留 retweets/quote(reply 是对话碎片且占量最大;retweet 是账号的主动背书)、`since_id` 增量(游标存 `SourceStateRecord`)、**配额守卫**(`src/services/x_api_quota.py`:按**返回资源**计费而非请求次数 —— Post/Media/Note $0.005、User $0.010,UTC 日内去重;月度累计存 `AppSettingRecord` KV `x_api_usage:YYYY-MM`,预算不足最小请求时**发出请求前即停**;`GET /api/x-api/quota` 可读)。

**凭据与开销的管理面**(v3.27 起分两半:**凭据编辑**收敛到 设置柜 → 凭据[见 *外部凭据统一保管层*],运维管理 → **内容**页保留**配额与开销观测面**[KPI 条+区头凭据 chip 回指],与媒体库并列——外部资源的开销,X 不是 AI 故不放 AI 页):`GET`/`POST /api/x-api/config` + `POST /api/x-api/config/test` 与 LLM 配置同构(`services/x_api_config.py`;token **只写不回显**,只返回 `bearer_token_set` + 掩码尾四位,响应与日志均无明文;`field_sources` 逐字段标出配置来源 `runtime_kv|env|ini|default`,因为凭据可能来自环境变量、此时运行时值不生效)。连通性自测走**最省钱探针**:优先重读已有游标对应的 Post,命中当日去重则 `$0`,否则约 $0.005;响应带 `estimated_cost_usd`/`deduplicated_today`,前端如实转述花费。`_resolve_user` 的 user_id 缓存(`AppSettingRecord` 键 `x_api_user_cache:<source_id>`)**主要服务只有 handle 的 config 源** —— 8 个 preset 已硬编码 user_id,本就不发 `by/username`(2026-07-20 一处误判的更正:实测 23 次 User 读取来自时间线响应的 `includes.users`,即顶层作者 + 引用/转推作者,那是渲染引用推卡的必需开销,省不掉)。**`max_results` 默认 25**:未使用的槽位不收费,调大只增加当次实际取回的内容、不增加参数本身成本,故它降低积压漏抓风险而不涨开销(残余边界:单次积压超 25 且不翻页仍可能漏)。

**外部凭据统一保管层(`services/credentials.py`,v3.26 凭据整合波)**: 我方持有、用于访问外部系统的机密及其伴生配置(LLM api_key、X bearer_token、远程同步登录密码,未来的内网 SSO 工号密码/微信微博等平台会话凭据)统一走本层的**保管契约**——声明式 `CredentialNamespace` 注册(字段清单 + env/ini/KV 三层映射),四能力:`resolve_values`(解析序 runtime_kv > env > ini > default)/`field_sources`(逐字段来源标注,因为凭据可能来自环境变量、此时运行时值不生效)/`save_updates`(只写不回显的写入半边:secret 空值=保留既有、普通字段空串=清 KV 覆盖回落基线)/`sanitize_values`(`{field}_set` 布尔 + `mask_tail` ••••尾四位预览,绝不回明文)。KV key 沿用各域既有存量(**零迁移**,test_credentials 有守卫);`x_api_config`/`daily_brief.resolve_llm_config` 保持公开签名、内部委托本层;remote_sync 定时凭据的单 JSON blob 是历史形态,只复用 `apply_secret_update` 合并语义,新命名空间一律一字段一 key。**边界(勿误并)**:本系统自签发令牌(dsub_/dfeed_ 存 hash、dshr_ 明文)与账号登录密码(accounts.py PBKDF2)不属本层;连通性探针(LLM ping/X 最省钱探针/remote-sync 登录探测)与字段取值校验留在各域 router;凭据的**使用**(client 构造)也留在各域。会话类凭据(cookie,会过期)未来接入:凭据入本层保管,续期流程各平台特质化,健康度走普通字段(last_verified_at/status)约定。前端 `SecretField.jsx` 是契约的表单半边(掩码预览即占位——`••••97c0` 本身就是「已有凭据」的信号,不赘述文字;输入框空值起步,留空=保留、输入=覆盖,语义细节挂 title)。**编辑单点在设置柜 → 管理 → 凭据(v3.27 凭据整合台,拍板 B)**:`settings/CredentialsSection.jsx` 承载 LLM/X 两张编辑卡(来源徽记+stamp+测试/保存)与「其它凭据」只读回指行(定时同步凭据与 cron 同工作流故编辑留数据同步区[柜内一跳];GitHub Token 部署级 env-only,存在性经 `GET /api/admin/credentials`);消费页只留 model-chip 状态签深链至此(日报页头/运维 AI 开关板「模型」、运维内容页 X 区头「凭据」),用量/配额观测面留在运维管理——**设置=写入口,运维=观测面**。**新增一类外部凭据 = 在 REGISTRY 登记命名空间 + 凭据区加一张卡**,即获整套存取/脱敏/来源标注契约。

**作者头像**:`extensions` 带 `author_avatar_url` / `author_avatar_url_large`(X 默认 `_normal` 是 48px,后端派生 `_400x400`),`quoted`/`reposted` 内嵌结构同样带;`GET /api/reader/sources` 透出 `avatar_url`(取自 user 缓存),源栏/发现页的社交源据此用真实头像。前端 `SocialAvatar` 四级降级:媒体库代理 → 原链直连 → 源的 `LogoMark` → handle 首字母色块;转推时头像跟**原作者**走,与名字/handle 同主体。(v3.12 初版曾定「不引外链 avatar、一律 LogoMark」,但社交源在品牌表里没有条目,结果所有账号退化成同一个 X 图标 —— 既不是品牌也不是头像;图床 v3.11 落地后代理链路现成,故改用真实头像。)

**跨平台抽象层**:引用推/转推的可渲染内容**扁平化进 `extensions`**(`quoted` / `reposted` 各含 `author_name`/`author_handle`/`text`/`url`/`media_urls`;无则不写该键),前端不去挖平台原始 JSON —— 将来接 Mastodon/Bluesky 时 `raw_data` 形状完全不同,但 quoted/reposted 语义通用。**转推契约**(注释+测试锁定):顶层 `author_*` = 时间线账号(转推者),`reposted.author_*` = 原作者;顶层 `content` 是 X 的 `RT @xxx: …` 截断形式,**展示须用 `reposted.text`**(完整正文,`note_tweet` 优先)。推文图存 `extensions_json.media_urls`、**不塞进正文 markdown**(推文正文就是文本,把图伪造成正文会污染向量化/日报/feed 导出),`media_store.extract_image_urls()` 已扩展为同时读该键,故随文预取照常。

前端 `SocialFlow.jsx` 占「条目列 + 阅读窗」整幅(`grid-column: 3/-1`,与发现页同构)、单列宽卡、全文直出。**不展示点赞/转发/评论数** —— 不是取数成本(`public_metrics` 随请求返回、不额外计费),而是诚实:抓取时刻的数字是永久快照,三天后仍显示当时数值等于主动展示错误信息;**字段照常入库,只是不展示**。时间戳即原推链接(不另设外链按钮),右上角悬停浮出**收藏 / 标读** —— 只读权限下仅有的两个真实动作。**平台是「源」的属性、不是每条内容的属性**:源栏按「平台 · 分层」分组、发现页源卡标平台(始终),而卡片头像角标**仅当订阅了 ≥2 个平台时挂载**(单平台时每卡同一图标是纯噪声);`platform` 由 `GET /api/reader/sources` 透出。**社交流不计入 reads 指标**(该指标语义是「刻意打开」,而卡片流全文直出、没有 open 动作;灌进去会污染运维看板的活跃判断),只维护未读/已读态。「今日」混合流仍包含社交条目,以列表紧凑形 + 「社交」chip 呈现。方案与名单裁决见 `docs/social-x-wave-plan.md`,样页 `docs/design/dorami-social-quiet.html`。

**问答检索与 RAG 退役(v3.30–3.31)**: 向量 RAG 子系统(ChromaDB + sentence-transformers/TEI、双状态列、对账巡检)已于 v3.31 **退役清仓**,全程记录 [`docs/rag-retirement-plan.md`](docs/rag-retirement-plan.md)(审视结论存档/目标架构/两波清单/重新引入触发器)。核心论证:生产从未运行(0 向量化)且生产硬件(1.6GB 内存)跑不起推理栈;FTS5 已就绪且结构性零漂移;增长曲线下向量形态运维成本涨得比收益快。现行检索栈:**FTS5 全文检索是唯一检索基础设施**;读者问答 scope=subscription 走 `services/reader_search.py` 的「LLM 计划检索 + FTS5」两段式(规划[中英关键词改写+日期窗+意图分流]→ FTS 召回[订阅域∩日期窗]→ 选篇[标题+引子挑 ≤8 篇]→ 全文注入;降级链自持:规划失败→原词 FTS→零命中→时序窗口;「日报即索引」:跨期盘点型问题在已订阅日报源时收敛检索日报正文);MCP `search_articles`/`get_rag_context` 与 `POST /api/public/subscriptions/{id}/vector/search`(路径留存换 FTS 芯,出入参兼容)同源复用 `mcp_server._search_articles_impl`。取名运行时单点 `friendly_source_name` 已迁至 `services/source_naming.py`。**重新引入触发器**(方案 §4,同时满足才重启评估):语料 ≫10 万篇 **且** ask 用量数据证明模糊语义查询成为真实模式——届时按「派生数据、外部推理、可随时全量重建」的轻形态重建,不恢复双状态机/对账架构。

**Fetch run tracking**: Every fetcher execution (manual or scheduled) writes a `FetchRunRecord` and upserts a `SourceStateRecord`. The state record is the authoritative health/cursor store per source; `build_fetcher_health_from_state()` in `app.py` derives the `/api/source-health` response from it, falling back to aggregating raw `FetchRunRecord` rows when no state exists.

**Accounts are database-managed（多管理员,v3.19 平权波）**: Login accounts live in the `users` ORM table (`UserRecord`), passwords stored as PBKDF2-HMAC-SHA256 hashes. `src/services/accounts.py` centralizes hashing/verify, user CRUD, the **last-active-admin guard**, and `seed_root_admin_if_empty`. `[auth]` no longer seeds accounts: **on first boot (empty `users` table) the system auto-creates a root admin `admin`/`admin`** (log in and change the password immediately); the root admin has **no special status** — it's an ordinary admin, fully peer with any other. **Admins are unlimited and self-service**: any admin can create accounts of either role, promote a reader to admin, or demote/disable/delete another admin. The **single guard** is last-active-admin protection (`_guard_last_active_admin`): the last remaining *active* admin cannot be demoted/disabled/deleted (an inactive admin doesn't count and can be deleted). Self-demotion/self-disable follows the same rule — the acting admin's own cookie is then revoked on the next request (by design; the frontend confirm dialog warns first). Admin manages all accounts under `/api/accounts` (list/create[role]/update role|active|per-account AI toggle/reset-password/delete — admin-only via `account_admin_required()`, independent of the runtime axis); `/api/admin/accounts` lists **all accounts including admins** (rows carry `role`; frontend shows a role column + promote/demote actions). Any logged-in user self-changes password via `POST /api/auth/change-password`; `POST /api/auth/preferences` stores per-account preferences (currently `default_surface`, see *Admin dual-surface*). `login_admin` and `read_auth_token` validate against the DB (account must exist, be `is_active`, role must match the token), so disabling/deleting/role-changing a user revokes their existing cookie on the next request. `username` is the immutable identity (it keys `owner_username` on subscriptions/feed tokens — no rename; deleting a user cascades their subscriptions + feed token). The account-management UI lives in the **运维管理 Tab** (`AdminOpsTab`, see *Admin Ops console*); the 设置柜's 账户 section holds the self password-change/avatar/logout available to every account (+ the admin-only default-surface picker).

**Admin audit log（管理操作审计,v3.19）**: with multiple admins, every management write is recorded so admins can audit each other. `src/services/admin_audit.py` hooks into the tail of the `require_admin_session` middleware: any non-GET request by an admin session hitting the management prefixes (`AUDIT_PATH_PREFIXES`: `/api/accounts`, `/api/admin`, `/api/x-api`, `/api/source-configs`, `/api/collection-jobs`, `/api/llm`, `/api/daily-brief`) writes one `AdminAuditRecord` row (operator/method/path/status_code/semantic `summary`/`target`/timestamp) — reader-surface actions (`/api/reader/*`, `/api/auth/*`) are deliberately exempt (an admin reading/subscribing is not a management operation). Requests rejected by the middleware itself (401/403) are not recorded; handler-level 400s (e.g. last-admin guard refusals) **are** — "who tried to delete the last admin" is part of the audit's job. A small path-pattern registry renders semantic summaries for account ops (never reading password fields); `record_audit` swallows all exceptions (same pattern as the AI-usage recorder). Read back via `GET /api/admin/audit-log?days=&limit=` (admin-only); surfaced in 运维管理 → 用户 → 操作审计 (`AdminAuditPanel.jsx`). No retention/cleanup yet.

**Admin dual-surface（管理员双界面,v3.19）**: an admin is also a reader. The backend reader gate already admits admins (`reader_role_enabled`); the frontend gives admins a **hidden surface switch**: a `BookOpen` button at the bottom of the app rail enters the 阅读器 in its standalone full-page form (subscribe/read/favorite like any reader), and a symmetric `LayoutDashboard` button in the reader vrail bottom (rendered only when `onExitReader` is passed) returns to the console. `App.jsx` keeps two orthogonal notions: `isReaderRole` (account semantics — tabs/redirects/copy) vs `readerView` (layout fork — rail hidden, ReaderTab shown); admin's `surfaceMode` (`console`|`reader`) lives in sessionStorage (`dorami-surface`, cleared on logout) and doesn't touch tabs/activeTab/hash, so exiting returns to the original tab. The per-account `UserRecord.default_surface` (`console`|`reader`, exposed via `/api/runtime`, edited in 设置柜 → 账户) decides the landing surface at login when the session has no prior switch.

**Access control — login account role (primary axis)**: In the default single-node `all` deployment the only axis that matters is the **login account role** (`admin` | `user`, stored per-account in the `users` table). **`admin` is a superuser** — collector surfaces (节点管理/任务运行, article CRUD) plus every reader surface; **a `user` is a restricted reader** — reader surfaces only (subscription delivery, full-text search, MCP/接入集成, surfaced as the full-page 阅读器 + the 设置柜's 接入集成 group), open to any logged-in account except archive import (admin-only, it mutates the whole archive). `disabled_runtime_surface()` enforces this per request via `COLLECTOR_API_PREFIXES` / `READER_API_PREFIXES` (reader-prefix matches short-circuit, allowing reader/collector prefix overlap). The frontend mirrors it through `runtime_capabilities()` → `collector_enabled` / `reader_enabled` / `account_role` per session.

> **Optional second axis — deployment runtime role** (`[runtime] role`, default `all`): only relevant for a *split deployment* where collection and distribution live on separate hosts (`collector` = collect/archive only, external network; `reader` = distribution only, intranet; they exchange data via *Archive Sync*). It ANDs with the account role (`collector_enabled = runtime allows collector AND account is admin`). For the default `all` single-node setup this axis is inert and `collector_enabled`/`reader_enabled` reduce to the account role above — you can ignore it unless you actually split the deployment.

**Reader subscription & distribution layer**: Reader accounts build a personalized subscription scope over already-archived records (it never triggers fetching). One-click subscribe (`POST`/`DELETE /api/reader/sources/{source_id}/subscribe`) creates/removes a per-user, single-source `ReaderSubscriptionRecord` (owned via `owner_username`). "我订阅" = the union of `source_id`s across a user's active subscriptions; for a `user` account it hard-scopes that user's search/QA/MCP retrieval and is the scope of the 阅读器 (the user's primary surface — its 我的订阅 view aggregates subscribed sources via `GET /api/articles?subscribed_scope=only`). Downstream consumers pull via tokens (HMAC-SHA256, stored only as hashes): a per-subscription token (`dsub_`) or the per-user **aggregated feed token** (`dfeed_`, one row per user in `ReaderFeedTokenRecord`) used at `GET /api/public/feed/articles[.md]` — a single endpoint covering all the user's subscribed sources with publish-time/source/type filters. Full contract in `docs/contracts/reader_subscription.md`.

**Daily Brief (每日 AI 资讯日报) — LLM map + deterministic render over the archive**: `src/services/daily_brief.py` orchestrates a scheduled/manual digest of already-archived articles: `collect_candidates` (returns scanned-total observability; `last_run` records `candidates_scanned`/`candidates_used` — items trimmed by `per_source_cap`/`max_total` are skipped past by the cursor, so the trimming is surfaced, not silent) → `map_summarize` (per-article LLM summarize+score, `map_concurrency` in parallel, aux-model tier + shared HTTP connection pool) → `dedup_clusters` (one LLM call clusters same-event items; keeps the highest-scored representative, folds the rest's links into its `extra_sources`; degrades to no-op on LLM failure) → map-failed items retry serially once then drop to the 「📎 其它收录」 appendix (never selected on their placeholder score, v3.34; serial retry v3.35) → `select_top` (**effective score = score + 0.5×official** [v3.35 权威机械层: bounded bonus so officials top their score band but never cross bands; `BriefCandidate.source_role` from `source_naming.source_role()`, the backend mirror of the frontend role axis] + source/realm diversity + a `paper_cap` that limits 学术论文 share; preselects top_n+buffer so cross-day drops can be refilled) → `cross_day_dedup` (one small aux-tier LLM JSON call against recent briefs' item titles [excluding the report date itself]: pure repeats dropped, follow-ups annotated with a「（接前报）」增量注; **official items are never silently dropped** — a drop verdict on an official-source item mechanically downgrades to a followup note; degrades to no-op, all-drop verdicts ignored as misjudgment; survivors trimmed back to top_n by effective score) → **same-day merge** (v3.35: if a brief already exists for the date, old∪new items are merged by article id + re-clustered + re-ranked instead of overwritten — a rerun no longer wipes the morning's entries; `dedup_clusters` also prefers an **official representative** within a 1.0 score gap, folding media links into `extra_sources`) → `render_brief_markdown` (**deterministic code-side rendering**, v3.34 — sections ordered 🚀 模型发布 first … 📄 学术论文 last, within-section by importance; format faithfully follows the retired LLM-reduce contract, whose `REDUCE_SYSTEM_PROMPT` is kept only as the downloadable Skill's style guide; whole-report LLM long-output truncation/omission risks eliminated) → idempotent write of a `daily_brief` content record (`source_id=dorami_daily_brief`, `content_type=daily_brief`). The map stage scores against a reader-interest profile (model/capability releases > AI apps/products > big-tech & industry news > novel research), penalizes 营销稿/软广 and downweights 车载/智能座舱. **Three-layer dedup**: ① a deterministic high-water cursor `daily_brief_cursor` (over `fetched_date`) that only advances after a successful write; ② the `dedup_clusters` stage collapses same-day same-event items before select; ③ `cross_day_dedup` collapses cross-day repeats against recent briefs' item titles. (Export to the shendeng platform — `scripts/export_shendeng_daily_news.py` — collapses the rich `classification` back to shendeng's two categories: 学术论文 stays, everything else → 产业资讯.) All daily-brief run state/config (cron, top-N, last run, cursor, **source-scope list**, LLM overrides) lives in `AppSettingRecord` KV — **no new ORM table**. The source scope (`daily_brief_source_ids`) is a **hand-maintained allowlist** (deliberate 2026-07 decision — no shape/tier rule filtering, since high-noise-but-timely sources can be brief-worthy; noise handling belongs to the list + map-stage LLM scoring): unset = all sources (back-compat); set = `collect_candidates` scans only listed sources and the cursor advances only over them, so newly added sources stay out of the brief until explicitly checked in on the config panel (「全部来源 ⇄ 自定名单」 multi-select in DailyBriefPanel). The scheduler registers exactly one APScheduler job `daily_brief`; `reload_daily_brief_schedule()` hot-adds/removes it on config change. **Manual generation (`POST /api/daily-brief/generate`) is a persistent background job** (阶段3): it validates `top_n` synchronously, then submits a `daily_brief`-type job via `services.jobs.launch` and returns `{status: "accepted", job_id}` immediately (no more minutes-long request); the frontend `generateDailyBrief` polls `GET /api/jobs/{job_id}` (shared `pollJob`) for the terminal result. The **scheduled** run (`execute_daily_brief_job`) still awaits `generate_daily_brief` directly on the scheduler thread (not an HTTP request; already tracked by its own run record). Fine-grained stage progress remains in-memory (`get_progress()`, polled by `GET /api/daily-brief/progress`, driving the DailyBriefFlow animation) — migrating that to DB is a later 阶段3 step. Daily-brief endpoints are **collector(admin)-gated** but the panel (`DailyBriefPanel`) is surfaced in the admin-only 「AI 日报」 tab (`DailyBriefTab`).

**LLM client (OpenAI-compatible)**: `src/llm/client.py` is a thin httpx wrapper over `{base_url}/chat/completions` covering OpenAI/DeepSeek/Kimi/智谱/通义/火山方舟/OpenRouter/Ollama/vLLM. `chat_completion()` does async completion + exponential-backoff retry + optional JSON mode; `parse_json_object()` robustly extracts a pure JSON object from model output (strips code fences, slices outer braces); `ping()` tests connectivity. It takes an `LLMConfig` and **never logs the api_key**. Config comes from `[llm]` in the ini (or `DORAMI_LLM_*` env), and can be overridden at runtime via admin settings (persisted to `AppSettingRecord` KV). Prompts live in `src/llm/prompts.py`. `LLMConfig.configured` is true only when `base_url`+`api_key`+`model` are all set.

**Collection Jobs are the single collection-scheduling entity**: A `CollectionJobRecord` is a savable, schedulable collection job that bundles multiple fetcher nodes (`fetcher_ids_json`), shared + per-node params, **one job-level cron** (per-node cron overrides retired 2026-07 by migration `d41acead77b0` — a job is "one set of nodes + one schedule"; want a different cadence, create another job; legacy overrides were faithfully split into separate jobs), and a `downstream_policy_json`. Running one writes a job-level `CollectionJobRunRecord` (`run_scope` = `ad_hoc`/`saved_job`/`legacy_task`) that **aggregates** the per-node `FetchRunRecord` rows it spawned (counts, child run IDs, partial-failure status). **Node groups (`NodeGroupRecord`, "采集范围") and legacy fetch tasks (`FetchTaskRecord`, `/api/tasks`) are fully retired** (实体简化, see `docs/archive/entity-simplification-plan.md`): 阶段 1 removed the UI, 阶段 2 removed the tables/endpoints — the Alembic migration `8f6d93196258` inlines referenced groups into their jobs (faithful param-merge precedence), converts standalone/cron-bearing groups and legacy tasks into collection jobs (provenance via `legacy_task_id`), then drops `node_groups`/`fetch_tasks` and `collection_jobs.group_id`. Historical run/article rows keep their `task_id`/`group_id`/`source_group_id` columns for traceability (`run_scope=legacy_task` still renders in run history). (`pipeline/progress.py` exposes in-memory per-`fetcher_id` `{current,total}` counts surfaced by `GET /api/fetch-runs/running-progress`.)

**Archive Sync (collector → reader)**: A collector runtime (external network) exports faithful archive records as JSON Lines (`application/x-ndjson`) via `GET /api/archive/export/articles.jsonl`; an intranet reader runtime imports them via `POST /api/archive/import/articles.jsonl` **without performing any public fetch**. Import is admin-only (it mutates the whole archive). Full contract in `docs/contracts/archive_sync.md`.

**Downloadable Claude skill**: `src/api/skill_router.py` zips `src/skill_templates/dorami-daily-brief/` on the fly (templating `{BASE_URL}` into the live host) and serves it at `GET /api/skill/daily-brief`, so a user can install a ready-made Claude skill that talks to this deployment's feed/MCP endpoints.

**Persistent background jobs (阶段3)**: Long admin tasks (采集任务运行、全量媒体回填、远程内容同步等) submit-and-return a `job_id`; the frontend polls `GET /api/jobs/{job_id}`. `src/services/jobs.py` is the **DB-backed** state machine (`JobRecord` table, statuses `queued/running/succeeded/failed/cancelled`) that replaced the process-in-memory `background_jobs` — so job state/progress/result survive a restart and are cross-process queryable (foundation for the scheduler↔worker split). `launch(engine, type, work, *, created_by, payload)` spawns an `asyncio` task running `work(job)`; the `Job` handle's `set_total`/`advance` persist progress **throttled** (every `_FLUSH_EVERY=25` steps or `_FLUSH_INTERVAL=1s`) so per-item loops don't hammer the DB, while status transitions and `set_total` flush immediately. `get_job`/`list_jobs` read back a dict whose shape matches the old `to_dict` (epoch-float timestamps) so the poll contract is unchanged. (Live per-fetcher fetch progress still uses the in-memory `pipeline/progress.py`; migrating it to DB is a later 阶段3 step.)

**Reader-facing AI Beta (用户面翻译 + 问答助手)**: `src/services/reader_ai.py` gives the 阅读器 two LLM features over the **same global `resolve_llm_config()`** the Daily Brief uses: `translate_article()` (full-body → 简体中文, paragraph-split + concurrent, cached under `extensions_json.translation_zh` so it never re-translates) and `answer_question()` (multi-turn QA; **范围四档,v3.32 核心层统一为「文章名单 → 编号上下文 + sources」**:scope=article/articles 显式名单直取正文[article 是 n=1 特例,单篇也带出处;articles ≤12 篇上限、缺失跳过、全缺 404,供上层功能圈定任意一批文章问答],scope=subscription/all 走 **`services/reader_search.py` 的「LLM 计划检索 + FTS5」两段式**——规划→FTS 召回→选篇→全文注入,降级链自持[规划失败→原词 FTS→零命中→时序窗口],跨期盘点型问题在已订阅日报源时「日报即索引」,两档只差检索域[订阅并集 vs `resolve_all_visible_source_ids` 全库可见源——与发现页预览同口径,隐藏源照旧排除];v3.30 检索扶正波奠基,见 `docs/rag-retirement-plan.md` §2;**后端四档全量落地,前端仅露 article/subscription**,all/articles 是后续功能的预留原语。**引用联动(v3.32)**:上下文编号 `[n]` 即引用锚——sources 载荷带 `id`/`source_name`/`publish_date` 且与上下文**同源同序**,QA 提示词要求行内 `[n]` 标记且禁止自列清单,前端把 `[n]` 转成可点引用 chip[站内跳转走 `openArticleById`,与 v3.25 深链落地同路]并按 sources 确定性渲染末尾出处列表[保底必有,不靠 LLM]). Endpoints `POST /api/reader/ai/translate|ask` are gated by `_require_reader_ai()`, which checks **global master switch AND per-account flag AND LLM configured** (else 403). Access is **two-layer**: a per-account `UserRecord.ai_beta_enabled` flag ⊕ a global master switch `ai_beta_global_enabled` (`AppSettingRecord` KV, default on) — the master switch is a kill/gray-out 总闸 that instantly suspends everyone without touching per-account flags; `_ai_capabilities()` ANDs the two into the `runtime.ai_beta_enabled` the frontend reads (so the 阅读器 entry auto-hides when either is off). Prompts (translate/QA, 哆啦美 persona) live in `src/llm/prompts.py`.

**Admin Ops console (运维管理 Tab)**: An **admin-only** top-level Tab (`AdminOpsTab.jsx`, gated by `account_role === 'admin'`) is the operational hub, laid out as a **Grafana-style monitoring 看板** (charts via `recharts`, themed through dorami tokens in `components/charts/DashboardCharts.jsx` + `chartUtils.js`; numbers hidden until hover-tooltip). It is split by a `.segmented-control` into three sub-pages (`sub` state: `ai` | `user` | `content`): **① AI** — the AI Beta master switch as a compact status-light + toggle in the panel header (`/api/admin/ai-beta/global`), a read-only **model-status chip** (the LLM editor lived here v3.9–v3.26; v3.27 凭据整合台 moved editing to 设置柜 → 凭据, the chip deep-links there via `openSettings('credentials')` — same pattern as the Daily Brief panel's chip), and an **AI usage dashboard** charted from `/api/admin/ai-usage` — a time-range dropdown (近 7/14/30/90 天) drives two daily multi-series **area charts** (每日调用次数 / 每日 tokens), each with an in-header 按用途/按用户 segmented toggle that re-pivots the series from `summarize.by_day_purpose` / `by_day_user`; series use a categorical palette (not just the brand token) for distinguishability; **② 用户** — a **windowed** reader-OPS board (migrated out of `SettingsModal`): a 近 7/14/30/90 天 time-range dropdown drives the whole sub-page (default 30) — top KPI tiles (读者数 / 窗口内登录过 / 窗口阅读 / 窗口 AI 调用) + an 活跃用户 Top rank bar with an **阅读 / 登录 segmented toggle** (re-ranks by the chosen activity dimension), then a list where **each row shows window metrics, not lifetime counters** (最近登录 + 窗口登录次数 + 窗口阅读次数 + 窗口 AI 调用 + 订阅数). The window fields (`reads` from `reader_activity.reads_by_user`, `logins` from `accounts.logins_by_user`, `ai_calls`/`ai_tokens` from `ai_usage.usage_by_user`, `logged_in_window` derived from `last_login_at`) come from `GET /api/admin/accounts?days=`. **Clicking a row opens a read-only per-user activity drawer** (`GET /api/admin/accounts/{username}/activity?days=`, powered by `ai_usage.summarize_user` + `reader_activity.summarize_user_reads` + `accounts.summarize_user_logins` + a per-user favorites-by-source join): an **expandable 登录 card** (count + click-to-reveal 最近 N 次登录时间 list) alongside 阅读/AI/订阅 snapshot tiles + a 各源 · 阅读/收藏 grouped-bar chart (`source_engagement` = reads ∪ favorites per source) + a 每日 AI 用量 stacked-area chart (调用/tokens toggle, purpose-stacked). Row-level management (create-account[role picker]/promote-demote admin/active/per-account AI toggle/reset-password/delete) stays inline (`stopPropagation` so it doesn't open the drawer); the account list includes admins with a role column (v3.19 多管理员), and an 操作审计 panel (`AdminAuditPanel.jsx`, `GET /api/admin/audit-log`) sits below it. Windowed richness derives from `AiUsageRecord` + `ReaderReadRecord` + `LoginEventRecord`. (`ChartPanel` flex-centers its chart so a short rank chart sits vertically centered when a taller sibling stretches the row; `RankBars` takes an optional `bars` prop for grouped multi-series.) **③ 内容** — a charted content board (`/api/admin/content` → 各源收藏/订阅 rank bars + 收藏文章榜). (The old overview KPI stat board was removed as low-signal; `/api/admin/overview` still exists but the UI no longer calls it.) New admin endpoints live under the `/api/admin/*` prefix (`overview`, `accounts`, `accounts/{username}/activity`, `ai-usage`, `content`, `ai-beta/global`), all forced to admin via `account_admin_required()` (which matches `/api/accounts` **and** `/api/admin`). The shared recharts wrapper `MultiSeriesArea` takes a `dims` prop so the same stacked-area component serves both the AI-usage 按用途/按用户 toggle and the per-user 调用/tokens toggle.

**AI usage metering (token tracking)**: `src/llm/client.py` keeps `chat_completion()` returning a `str` but optionally surfaces the response `usage` (prompt/completion/total tokens): pass a `UsageMeta(purpose, username)` and the client hands token usage to a registered recorder callback (`set_usage_recorder`, wired in `app.py` to write the DB) — **metering never blocks the main flow** (recorder exceptions are swallowed; `ping()` passes no meta so connectivity tests aren't counted). `src/services/ai_usage.py` aggregates one row per `(day × username × purpose × model)` into the `AiUsageRecord` table (`record_usage` upserts/accumulates; `summarize` powers `GET /api/admin/ai-usage`). Purposes: `translate`/`ask` (attributed to the logged-in reader), `daily_brief_map|dedup|reduce` (attributed to the **admin who manually triggered** the run via `generate_daily_brief(triggered_by=...)`, else `"system"` for scheduled runs), `source_config`/`detail_profile`. Lightweight per-account counters (`UserRecord.ai_translate_count`/`ai_ask_count`/`last_login_at`) remain a separate cheap snapshot for the account row; `AiUsageRecord` is the token dashboard's source of truth.

**Reading metering (阅读计量)**: parallel to AI metering but for plain reads — `src/services/reader_activity.py` aggregates one row per `(day × username × source_id)` into `ReaderReadRecord` (`record_read` upserts/accumulates). A read is recorded when a reader **deliberately opens an article** in the 阅读器: `POST /api/reader/articles/{id}/read` (reader-gated, fire-and-forget, dedup'd frontend-side against consecutive re-clicks, silently ignores missing articles) resolves the article's `source_id` and bumps the aggregate. To keep the signal clean the reader **no longer auto-opens a default article** (auto-opens would be passive, not deliberate). `reads_by_user` (windowed per-user totals) feeds the admin account list / 活跃用户 Top; `summarize_user_reads` (total + by_source + by_day) feeds the per-user activity drawer's 各源阅读/收藏 board. The same `/read` call also bumps a per-article lifetime counter `ArticleRecord.read_count` (migration `263e8e8b6c3d`) and returns it — the 阅读窗 meta line shows「阅读量 N」(article/bulletin panes; social flow has no open action), and `serialize_article_list_item` exposes `read_count`.

**Login metering (登录计量)**: `accounts.touch_login` writes a `LoginEventRecord` (raw event with precise timestamp) on every successful login **in addition to** updating the `UserRecord.last_login_at` snapshot — the event stream backs windowed login counts and the "最近若干次登录时间" list (logins are low-frequency, so raw retention is fine). `accounts.logins_by_user` (windowed per-user count) enriches the account list / 活跃用户 Top's 登录 dimension; `accounts.summarize_user_logins` (count + by_day + recent N timestamps) feeds the per-user drawer's expandable 最近登录 list.

### Project Structure

```
src/
├── main.py                  # Entry point: starts uvicorn with reload=True
├── config.py                # load_config() → settings singleton; reads DORAMI_CONFIG_FILE (else config/backend.ini)
├── api/
│   ├── app.py               # FastAPI app — 鉴权中间件/前缀门控表/include_router 注册 + APScheduler init(端点大多已拆入 routers/)
│   ├── routers/             # 按域拆分的 Router 群(articles/reader/admin/archive_sync/…); v3.18 新增 feedback.py / announcements.py / remote_sync.py; v3.25 新增 share.py(公开分享只读端点); vector.py 已随 v3.31 退役清仓删除
│   └── skill_router.py      # GET /api/skill/daily-brief: zips src/skill_templates/dorami-daily-brief on the fly
├── llm/
│   ├── client.py            # OpenAI-compatible chat_completion + parse_json_object + ping (httpx; never logs api_key); optional UsageMeta + set_usage_recorder surface token usage without changing the str return
│   └── prompts.py           # Daily-brief map/reduce + reader-AI translate/QA (哆啦美 persona) prompt templates
├── services/
│   ├── daily_brief.py       # Daily-brief map-reduce orchestration + same-event dedup_clusters + paper_cap + cursor dedup + in-memory progress; usage attributed to triggered_by (manual admin) else "system"
│   ├── reader_ai.py         # 用户面 AI Beta: translate_article (cached zh translation) + answer_question (multi-turn QA); shares resolve_llm_config; 核心层「文章名单→编号上下文+sources」(v3.32,[n] 即引用锚) + assemble_reader_context 范围四档(article/articles 显式名单,subscription/all 经 search_fetch 闭包注入)
│   ├── reader_search.py     # 订阅域问答检索(v3.30 检索扶正波): LLM 规划(中英关键词+日期窗+意图分流)→ FTS5 召回 → LLM 选篇 → 全文注入; 降级链自持; 日报即索引
│   ├── source_naming.py     # 来源取名与角色运行时单点(v3.31 自 vector_storage 迁出): friendly_source_name + SOURCE_FRIENDLY_NAMES 兜底表; v3.35 增 source_role(前端 sourceRoleOf 的后端镜像,scope 集合两侧须逐字同步,日报权威机械层消费)
│   ├── accounts.py          # Account CRUD + PBKDF2 + last-active-admin guard (v3.19 多管理员:可建/提升/降级 admin,末位活跃管理员不可降级/停用/删除) + seed_root_admin_if_empty(空库首启自动生成 admin/admin) + default_surface 偏好; login/AI-usage 埋点 (touch_login writes LoginEventRecord + last_login_at; record_ai_usage) + login aggregation (logins_by_user/summarize_user_logins) + AI Beta global master switch
│   ├── admin_audit.py       # 管理操作审计(v3.19): should_audit 前缀×方法判定 + record_audit(语义摘要注册表,吞异常不阻断) — 挂在 require_admin_session 中间件尾部
│   ├── ai_usage.py          # AI token metering: record_usage upserts AiUsageRecord per (day×user×purpose×model); summarize / usage_by_user / summarize_user → admin dashboards
│   ├── reader_activity.py   # 阅读计量: record_read upserts ReaderReadRecord per (day×user×source_id); reads_by_user / summarize_user_reads → admin user-OPS board
│   ├── article_share.py     # 文章分享(v3.25): 公开只读链接签发/撤销/解析 + 总闸 public_share_enabled + 单日限额; 失效原因一律不区分(过期/撤销/隐藏源/不存在 → 同一个 404)
│   ├── credentials.py       # 外部凭据统一保管层(v3.26): CredentialNamespace 声明式注册(LLM/X API)+ resolve/field_sources/save/sanitize 四能力 + mask_tail/apply_secret_update 共享助手; 自签发令牌与账号密码不属此层
│   ├── x_api_config.py      # X API 凭据/参数配置(保管契约委托 credentials 层)+ user_id/头像缓存
│   ├── x_api_quota.py       # X API 配额守卫:按返回资源计费(Post/Media/Note $0.005、User $0.010,UTC 日去重),月度累计存 KV,预算不足即停
│   ├── media_store.py       # 媒体库(图床): extract_image_urls + MediaStore(get_or_fetch/prefetch/stats) — URL 哈希寻址、内容哈希去重落盘、SSRF/魔数/大小防护、失败负缓存
│   ├── source_builder.py    # AI node onboarding: URL → detect type + signals + (LLM) config + detail-profile → preview (frontend gated off; backend only)
│   └── remote_sync.py       # 远程内容同步(v3.18): 登录远端(Set-Cookie 显式回传)→ 分页拉 archive export → 本地幂等导入; KV 增量游标; 手动同步凭据绝不落库(v3.19.2 例外:定时同步凭据存 KV remote_sync:schedule,只写不回显)
├── models/
│   ├── content.py           # Dataclass content models (BaseContent + subtypes)
│   └── db.py                # SQLModel ORM tables: ArticleRecord,
│                            #   FetchRunRecord, SourceStateRecord, SourceConfigRecord,
│                            #   CollectionJobRecord, CollectionJobRunRecord,
│                            #   ReaderSubscriptionRecord, ReaderFeedTokenRecord, AppSettingRecord,
│                            #   UserRecord (+ai_beta_enabled/default_surface/last_login_at/ai_*_count 列), AiUsageRecord, ReaderReadRecord, LoginEventRecord,
│                            #   AdminAuditRecord (v3.19 管理操作审计),
│                            #   JobRecord (阶段3 持久化后台任务状态机), MediaAssetRecord (图床缓存登记),
│                            #   FeedbackRecord + AnnouncementRecord + AnnouncementDismissRecord (v3.18 互通波),
│                            #   ArticleShareRecord (v3.25 分享波:公开只读链接,令牌明文存——与 dsub_/dfeed_ 的有意口径差异)
├── fetchers/
│   ├── base.py              # BaseFetcher: httpx client, retries, template method
│   ├── registry.py          # FetcherRegistry singleton — auto-discovers impl/ on import
│   └── impl/
│       ├── rss_fetcher.py               # GenericRssFetcher + PresetRssFetcher (23+ built-in RSS sources); OpenAINewsRssFetcher renders detail via Playwright (CF bypass); HackerNewsAiRssFetcher applies a configurable min_points/min_comments hnrss threshold to de-noise the q=AI firehose and is treated as a discovery source (external-link posts degrade to title+URL+discussion+heat with no body; only Ask/Show/Tell self-posts keep a body; external detail fetch off by default)
│       ├── github_release_fetcher.py    # GenericGitHubReleasesFetcher + preset subclasses (13 built-in)
│       ├── repository_model_fetcher.py  # GitHub repo + HuggingFace model fetchers (content_type=github_repository / huggingface_model); GitHub repo fetcher backfills a cleaned README excerpt when a repo has no description (dedup-gated, GITHUB_TOKEN-aware)
│       ├── webpage_fetcher.py           # BaseWebPageListFetcher + preset subclasses (6 built-in)
│       ├── curated_core_fetcher.py      # Curated AI-source presets: SinglePageDocumentFetcher (changelogs/release notes) + per-site BaseWebPageListFetcher/BaseFetcher subclasses (量子位, 新智元, HF Daily Papers, etc.)
│       ├── configurable_web_fetcher.py  # ConfigurableWebFetcher (generic_web): single config-driven web fetcher; params carry listing/patterns/detail-profile/listing_css (frontend gated off; backend only)
│       ├── article_extractor.py         # Shared HTML→article-body extractor (helper module, not a fetcher); used by webpage/rss fetchers to backfill detail
│       ├── x_timeline_fetcher.py         # X 时间线:XTimelineFetcher(is_template 模板)+ 8 个 preset;since_id 增量、exclude=replies、quoted/reposted 扁平化
│       ├── playwright_renderer.py       # PlaywrightRenderer: headless-Chromium detail rendering for Cloudflare-challenged sources (used by OpenAINewsRssFetcher)
│       └── webhook_trigger.py           # Outbound Dify workflow trigger (not an inbound content source)
├── fetchers/web_content/    # Optional crawl4ai Web Content backend: backend.py (WebContentBackend ABC + DetailResult), legacy_backend.py (httpx baseline), crawl4ai_backend.py (browser, opt-in extra), profiles.py (per-site CrawlProfile), compare.py (bypass A/B)
├── mcp_server.py            # build_mcp_app(): FastMCP streamable-HTTP server, mounted at /mcp by app.py
├── pipeline/
│   ├── core.py              # DataPipeline: drives fetcher → broadcasts to registered storages
│   └── progress.py          # In-memory per-fetcher {current,total} run progress (polled by frontend)
└── storage/
    ├── base.py              # BaseStorage abstract class
    ├── fts.py               # SQLite FTS5 全文索引(articles_fts 建表 DDL + trigger 同步 + fts_search_ids 查询;运行期与迁移共享单一实现)
    └── impl/
        └── db_storage.py    # SQLite storage(create_all 引导 + FTS 建表)

frontend/src/
├── api.js                   # All fetch() calls to the backend (single source of truth)
├── sourceTaxonomy.js        # 来源谱系: LogoMark 品牌注册表 + LOGO_SIZES 刻度 + **信息角色单轴** (sourceRoleOf/SOURCE_ROLES + groupByRole — 官方/媒体/个人/榜单, 判定序 个人→榜单→媒体→官方; 阅读器源栏/发现页/管理面共用一套词汇; 形态交给容器+seg, 组头只表角色; **v3.25 起管理面也用这根轴**——原「公司→板块」两级谱系 groupBySection/SECTIONS 已退役, 公司仅作组内排序键)
├── App.jsx                  # Root: login gate + tab routing. admin 走左侧应用导轨 (56px, 与阅读器视图轨同形制 — 复用 .reader-vrail-* 「轨语言」类族, icon-only + 墨底 tooltip; 轨底 = RailUserFlyout 用户滑出菜单 — 常态只见头像, hover/focus 滑出 进入阅读器/主题/设置, 头像同帧变关机退出钮 [2026-07-24 拍板: 原「头像点击进设置」与设置钮重复]; 退出两击防呆 v3.22——首点仅入 4 秒待确认态[红圈脉动+Toast 提示], 再点才退); readerOnly 隐藏应用导轨 (整页即阅读器, ReaderTab standalone 轨底同用 RailUserFlyout). tabs 按 runtime capabilities × account_role 过滤
└── components/
    ├── LoginScreen.jsx      # Account login
    ├── AnnouncementBanner.jsx # 公告横幅 (v3.18): **Apple 系悬浮玻璃胶囊卡**——absolute 悬浮、整页水平居中(width min(560px,·)), 不占布局、关闭/隐藏零位移(用户拍板:布局推移显得视图不固定); 玻璃四要素 = surface **18%** color-mix 半透明底 + **blur(6px)** saturate(1.8)(blur 刻意小于底下小字字高——半径过大会把透出内容抹平成均匀色,"物理透明、视觉实心") + 顶部光泽渐变与 inset --dorami-edge 高光 + hairline 外缘/--sh-3 落影(亮暗自动翻转); **入场动效两段式**(Chrome 硬限制:元素或祖先带 active 动画时 backdrop-filter 一律不取样,最小复现锁死)——entering = 纯 transform 从屏幕顶缘滑入(无 opacity!opacity 会把实底打薄透字)+ 78% 实底 + 无 blur,JS 380ms 摘类落定 → blur 挂上 + 底色 transition 溶解回 18% 玻璃,全程无"清晰直透"帧; **彩色玻璃 tint**(用户拍板:按档位上彩色半透明底;左缘竖条有 AI 感已去除)——`--ann-tint` 变量逐档覆写(通知=muted 10%/强调=accent 19%/警示=warn 19%), 文字与图标取同档 token 色(accent-ink/state-warn 亮暗成对), 三档语义 = 图标形状+色+tint 三通道(Info/Megaphone/AlertTriangle); 大圆角 --r-overlay; **8s 自动隐藏**(淡出上移动效, hover/聚焦暂停计时按剩余续走; 隐藏≠dismiss——不落库, 下次会话再现); 正文走 utils/announcementText.jsx 的白名单渲染器(仅 **加粗** 与 http(s) 链接, 零依赖零注入——不用 ReaderMarkdown); AnnouncementCard 供管理面预览复用(所见即所得); dismiss 本地即时移除 + fire-and-forget 上报
    ├── ReaderTab.jsx        # 阅读器 (读者唯一主界面, 四带式: 视图轨56/源栏232/条目列372/阅读窗): **容器模型** mode=article(默认)|bulletin|social 与单源 activeSourceId 共存(social 走 SocialFlow 整幅卡片流,不用条目列+阅读窗; 点源跳所属容器); 「今日」跨容器混合流已取缔(它以文章形态渲染推文, 违反容器模型前提——三宇宙渲染形态不同才需分容器; 各容器默认倒序 + 未读体系已覆盖「最近/未看」); **收藏=源栏容器级入口**(「只看收藏」与「全部XX」并列的一行,点击看本容器全部收藏、不再逐源;三容器同构,已从列头/社交头移出——Folo 语义;源栏聚合入口选中态统一:名字加粗全墨 + 左侧图标着色,全部XX=品牌靛、只看收藏=琥珀星);收藏文章在条目卡右缘挂琥珀星标(已收藏常显、未收藏悬停浮出可点亮);**未读=标题左侧栏小蓝点**(绝对定位于左 gutter、与右缘收藏星标错开;标题一律全墨、不再降灰——2026-07 翻案:灰标题与圆点二者取圆点,减一层灰阶冗余);**搜索命中高亮**(`utils/highlight.jsx` 的 `highlightMatch`——条目列标题/摘要、社交流推文、发现页源名/简介共用,accent 淡底 `.search-hl`);源栏「我的订阅」头与「全部XX」行**均不再显示未读数字**(减噪);未读体系 (全部/未读 seg + 标读) + 日期分组 sticky 组头 + 衬线阅读窗 (serif 标题/进度线/上一下一篇/日报报头); **无限滚动**(IntersectionObserver 哨兵,提前 400/500px 触发,骨架条占位随文追加,取代「加载更多」按钮); **浮层滚动条**(条目列/社交流:隐藏原生条使卡片左右满宽铺满、右缘不被 gutter 占位,改用 `hooks/useOverlayScrollbar.js` 自绘半透明滑块 `.ovl-thumb` 压在卡片上——Folo 式;macOS「始终显示」下纯 CSS 无法让原生条 overlay,Chrome 124+ 已移除 `overflow:overlay`;`scrollbar-width:none` 须钉未分层区,否则被全局 `* {scrollbar-width:thin}` 压回;时间/星标右移 8px 避开滑块,正文/摘要仍满宽可压滑块下); 视图轨含「发现」(Compass, discover 态);搜索=各容器内容头就地展开(列头/社交头,图标↔输入框顶替标题,已从视图轨降级); **no default article auto-open** (deliberate-read 计量: POST /api/reader/articles/{id}/read fire-and-forget); AI Beta 入口 (译为中文 toggle + 问哆啦美 fab — 默认收缩圆钮, 悬停展开) 当 ai_beta_enabled && llm_configured
    ├── mobile/              # 移动壳 (移动波 v3.29, ≤767px matchMedia 首帧分流, lazy 独立 chunk): MobileReader (底部4-Tab 页面栈壳+条目流) + MobileArticlePage (push 全屏正文页) + MobileSourceDrawer (源过滤抽屉) + MobileMePage (承接 RailUserFlyout 职责+退出确认框) + ActionSheet (长按动作单=桌面右键菜单的 bottom-sheet 翻译, items 同源同组序); 数据层与桌面共用 hooks/useReaderState.js (Wave1 自 ReaderTab 抽取), 长按手势 hooks/useLongPress.js, 样式 mobile.css (全 .m-* 前缀, index.css @import); v3.29.1 收口: MobileSettings (设置柜的两级页面栈翻译, 分区组件与桌面同源) + hooks/useLayerHistory (层×浏览器返回键握手) + SocialFlow headless 化 + 长按单 Web Share 项 + 登录 ≤480 轻档
    ├── SocialFlow.jsx       # 社交媒体流 (含 SocialAvatar 四级降级头像) (第三容器 shape=social, v3.12): 占条目列+阅读窗整幅、单列宽卡、全文直出; 图片网格/引用推嵌套/转推归属行; 不展示互动数字(快照会过时); 时间戳即原推链接, 卡片悬停浮出单条收藏/标读; 头部 = 标题(常显于左,不随搜索隐藏)+ seg+标读+搜索(**收藏过滤钮已移至源栏容器级入口**,搜索在右侧 actions 组内展开、**紧邻开关钮**——社交头太宽满宽会失衡;「N 个账号 · M 未读」副标已撤,收藏筛选时标题仅「收藏」); 推文文本 `highlightMatch` 命中高亮; 初次加载 + 无限滚动追加均用推文卡骨架(SocialCardsSkeleton),取代 spinner/「加载更多」; 平台角标仅在 >=2 平台时挂
    ├── DiscoverPage.jsx     # 发现页 (参照 Folo, v3.10): 全站源目录占据 条目列+阅读窗 (grid-column 3/-1), 编辑分层分组 + 双列源卡 (简介/收录 meta/订阅胶囊) + 形态/搜索过滤; 预览=跳该源条目列表, 未订阅时 ReaderTab 渲染源栏锚点行 + 条目列「＋订阅」横幅 (activeUnsubscribed)
    ├── AdminOpsTab.jsx     # 运维管理 (admin-only): Grafana 式看板 — KPI + AI Beta 总闸 & global model config + AI usage charts (recharts, time-range dropdown) + content board charts (含媒体库统计 + MediaHeatmap 逐日热点图) + reader-account management; calls /api/admin/* + /api/llm/config; v3.18 增第四子页「消息」(sub='engage': FeedbackInboxPanel 反馈收件箱 + AnnouncementsPanel 公告管理)
    │   ├── charts/         # DashboardCharts.jsx (token 化 recharts 包装件 + ThemedTooltip) + chartUtils.js (配色常量 + fillDailySeries 零填充)
    │   └── admin/          # adminUtils.js + MediaHeatmap.jsx (媒体逐日热点图 53×7 格阵 + 当日明细抽屉 + 单篇定点重抓;手写 CSS grid,不用图表库) + AdminAuditPanel.jsx (操作审计列表,v3.19)
    ├── DataTab.jsx          # 知识台账: article list, filters, CRUD; admin-facing (hidden for `user`); 总账条=三格收录量看板(v3.31 向量列/自动向量化开关随 RAG 退役移除)
    ├── FetchTab.jsx         # 节点管理: fetcher catalog/triggers (collector)
    ├── FetchRunsTab.jsx     # 任务与运行: scheduled tasks + fetch-run history (collector)
    ├── DailyBriefTab.jsx    # AI 日报页签 (原「接入集成」页签瘦身改名 v3.9; tab id 已更名 'brief', 历史书签 #/mcp 由 hashToRoute 归一兼容; gating collector): DailyBriefPanel + 只读模型 chip; 交付通道内容 (MCP/聚合接口/技能包) 已并入设置柜
    ├── DailyBriefPanel.jsx   # 每日 AI 资讯日报: config + manual generate + run history (admin-managed)
    ├── DailyBriefFlow.jsx    # Animated map-reduce stage visualization for the daily-brief generation progress
    ├── SettingsModal.jsx    # 设置柜 (880×576, v3.9 「接入集成并入设置」; v3.19.2 整备): 分组左导航 通用(账户/外观/反馈与建议 — 反馈仅读者账号)/接入集成(聚合接口/MCP 接入/Agent 技能包 — 两角色同享)/管理(数据同步/凭据 — v3.27 凭据整合台)/关于; wash 块选中 (轨语言); initialSection 深链。**内容门控按「当前界面」而非仅角色**(v3.19.2): App 下传 readerSurface, adminConsole = admin 账号 × 管理台界面 —— MCP 启停 switch/「管理员令牌拉走全库」文案/数据同步组/归档中枢产品名只在 adminConsole 出现, admin 切阅读器后设置柜与读者同观感。分区组件在 components/settings/ (FeedTokenSection/McpAccessSection — adminConsole 含 MCP 启停 switch + 向量统计行, 取数范围说明分管理版/读者版/SkillSection/AccountSection/AppearanceSection/DataSyncSection — v3.19.2 重设计为单列整幅实线卡(根治 datetime 截断), 含 RemoteSyncCard 远程同步块(测连/范围三选/进度轮询)与 RemoteSyncScheduleCard 定时同步卡(开关+只写不回显凭据+频率预设)/CredentialsSection — v3.27 凭据整合台:LLM/X 编辑卡+只读回指行,onNavigate 柜内跳数据同步/FeedbackSection — v3.18 读者反馈入口/AboutSection)
    ├── SecretField.jsx      # 统一凭据输入件(v3.26): 只写不回显契约的表单半边——掩码预览即占位(无预览时「已设置」), 空值起步 留空=保留/输入=覆盖, 语义挂 title; X/LLM/定时同步三处共用
    ├── ManualAddModal.jsx   # Manual article entry form
    ├── ArticleDetailModal.jsx
    ├── DateRangePicker.jsx
    └── Toast.jsx
```

**User layer is a reader, not a console**: A `user` (restricted reader) account gets exactly one full-page surface — the 阅读器 (`ReaderTab`, app rail hidden, its own 视图轨 is the only chrome). Everything non-reading lives in the **设置柜** (`SettingsModal`): account/appearance plus the whole 接入集成 group (dfeed_ aggregated feed token, MCP client config, Agent skill download). Subscription management is in-reader: the 源栏 is the subscription list (hover 减号 to unsubscribe), the 发现页 (视图轨 Compass) is the full-page catalog with one-click subscribe/preview. So **subscription = the user's reading list (what the reader shows) + the downstream feed/MCP delivery scope**. Admin's rail keeps 台账/节点/运行/雷达/AI 日报/运维 + the same 设置柜 (with 管理 group); since v3.19 an admin is also a reader — the rail-bottom hidden switch enters the same standalone 阅读器 (see *Admin dual-surface*). The `/api/subscriptions` REST lifecycle remains as an advanced/automation path with no dedicated UI.

### Key Endpoints

**Articles**
- `GET /api/articles` — list/query (filters: `content_type`, `source_id`, `has_content`, `search`(FTS5 全文,标题+正文), `publish_date_start/end`, `fetched_date_start/end`, `subscribed_scope` = `off`|`only`|`prioritize`, `skip`, `limit`)
- `POST /api/articles` — manual entry
- `PUT /api/articles/{id}` — update
- `DELETE /api/articles/{id}` — delete(必要时回退日报游标)
- `POST /api/articles/batch-delete`

**Article analysis / Taxonomy / Personal briefs (v3.44)**
- `/api/admin/analysis/*` — analysis worker status, retry, release smoke and resumable historical backfill.
- `/api/admin/cms-tags*`, `/api/admin/taxonomy/*` — canonical catalog, aliases, Candidate review, target-DB receipt and manual publication.
- `/api/reader/interests/*` — onboarding catalog and follow/mute preferences.
- `/api/reader/briefs/*` — personal brief readiness, generation and immutable edition retrieval.

**Feed Delivery** (recommended contract for downstream LLM consumers)
- `GET /api/feed/articles` — delivery-shaped JSON with `metadata.extensions` parsed; supports `content_types` (CSV), `source_ids` (CSV), `has_content`, `include_content`; `limit` capped at 500
- `GET /api/feed/articles.md` — same filtered records as a Markdown batch; capped at 200 records

**Import Bridge**
- `POST /api/import/social-posts` — ingest external social posts (X/Twitter, etc.) as `social_post` content; idempotent by `source_id + post_id`

**Archive Sync** (collector → reader, see *Archive Sync* above)
- `GET /api/archive/export/articles.jsonl` — collector exports archive records as JSON Lines
- `POST /api/archive/import/articles.jsonl` — reader imports them (admin-only; no public fetch)

**Daily Brief** (collector/admin-gated; surfaced in the 「AI 日报」 tab)
- `GET`/`POST /api/daily-brief/config` — read/set cron, top-N, LLM overrides (persisted in `AppSettingRecord` KV)
- `POST /api/daily-brief/generate` — submit a background digest job; returns `{status, job_id}` (poll `GET /api/jobs/{job_id}` for the result)
- `GET /api/daily-brief/runs` — recent run history; `GET /api/daily-brief/progress` — live in-memory generation progress; `GET /api/daily-brief/pipeline` — stage/pipeline view
- `GET /api/skill/daily-brief` — download the templated `dorami-daily-brief` Claude skill zip

**Collection Jobs** (the single scheduling entity; see *Collection Jobs* above)
- `GET/POST/PUT/DELETE /api/collection-jobs` — savable, schedulable multi-node job CRUD
- `POST /api/collection-jobs/{job_id}/run` — submit a background job (阶段3) that runs the collection job (still writes an aggregating `CollectionJobRunRecord`); returns `{status, job_id}`, poll `GET /api/jobs/{job_id}` for the aggregate (validation 404/400 stays synchronous)
- `GET /api/collection-job-runs` + `GET /api/collection-job-runs/{job_run_id}` — job-level run history
- `GET /api/fetch-runs/running-progress` — in-memory per-fetcher live progress
- (retired: `/api/node-groups*`, `/api/tasks*`, `/api/collection-jobs/migrate-legacy-tasks` — 实体简化阶段 2; existing data auto-migrated by Alembic `8f6d93196258`)

**Fetchers**
- `GET /api/fetchers` — list all discovered fetchers with parameter schemas
- `POST /api/fetch/{fetcher_id}` — trigger a specific fetcher (also writes `FetchRunRecord` and updates `SourceStateRecord`)

**Source Configs** (user-defined source definitions, advanced extension surface)
- `GET /api/source-configs` — list all source configs
- `POST /api/source-configs` — create a new source config
- `PUT /api/source-configs/{source_id}` — update a source config
- `POST /api/source-configs/{source_id}/toggle` — enable/disable a source
- `DELETE /api/source-configs/{source_id}` — delete a source config
- `POST /api/source-configs/{source_id}/fetch` — trigger fetch for a specific source config
- `POST /api/source-configs/fetch-active-rss` — trigger all active RSS source configs (阶段3: background job, returns `{status, job_id}`)
- `POST /api/source-configs/fetch-active-web` — trigger all active web/webpage source configs (via `generic_web`; 阶段3: background job, returns `{status, job_id}`)

**Source Builder** (AI node onboarding, collector-gated; frontend entry currently gated off — backend only)
- `POST /api/source-builder/analyze` — body `{url}`; detect page type + analyze + (LLM) propose a `SourceConfigCreate`-shaped node config (+ sample-article detail `CrawlProfile`)
- `POST /api/source-builder/preview` — body = proposed config; trial-run `generic_web`/`generic_rss` and return sample entries (no persist). Save via `POST /api/source-configs`.

**Monitoring & Observability**
- `GET /api/source-health` — per-fetcher health summary (derived from `SourceStateRecord`, falls back to `FetchRunRecord` aggregation); sorted by category then name
- `GET /api/source-states` — raw `SourceStateRecord` rows (filterable by `status`, `fetcher_id`)
- `GET /api/fetch-runs` — paginated fetch run history
- `GET /api/fetch-runs/{run_id}` — single run detail

**Reader Subscriptions & Personal Feed** (reader surface)
- `GET /api/reader/sources` — subscribable source catalog (registry ∪ archived ∪ subscribed; enriched name/description/icon; `subscribed` flag)
- `POST`/`DELETE /api/reader/sources/{source_id}/subscribe` — one-click subscribe / unsubscribe (per-user)
- `POST /api/reader/articles/{article_id}/read` — record one deliberate article open (reader-gated, fire-and-forget; bumps `ReaderReadRecord` by the article's source)
- `GET/POST/PUT/DELETE /api/subscriptions` + `POST /api/subscriptions/{id}/rotate-token` — subscription lifecycle (owner-scoped); REST-only advanced/custom path
- `GET /api/reader/feed-token` + `POST /api/reader/feed-token/rotate` — the per-user aggregated feed token (`dfeed_`)
- `GET /api/public/feed/articles[.md]` — token-auth aggregated pull across all the user's subscribed sources (filters: `publish_date_start/end`, `content_types`, `source_ids`, `search`, `include_content`); per-subscription pulls at `GET /api/public/subscriptions/{id}/articles` and `POST .../vector/search`(路径留存,v3.31 起 FTS5 全文检索芯)

**MCP** (reader surface)
- `/mcp` — FastMCP streamable-HTTP server (`build_mcp_app`); tools accept an optional `subscription_token` (`dsub_` or `dfeed_`) to scope results to that subscription / the user's whole subscription union

**Reader AI Beta** (reader surface; gated by global master switch + per-account flag + LLM configured)
- `POST /api/reader/ai/translate` — translate an article body to 简体中文 (cached in `extensions_json`)
- `POST /api/reader/ai/ask` — multi-turn QA,scope 四档 `article|articles|subscription|all`(v3.32:显式名单 vs 检索圈定;检索档走「LLM 计划检索 + FTS5」两段式);响应 `sources` 与上下文编号 [n] 同源同序(行内引用锚);可带 `ask_id` 登记阶段进度
- `GET /api/reader/ai/ask/progress?ask_id=` — ask 阶段进度轮询(plan→search→select→answer,内存瞬时态,完成即清;阶段化等待态数据源)

**Media（图床）** (see *媒体库*)
- `GET /api/media/proxy?url=` — 正文外链图片代理（reader surface）：命中本地缓存回文件（长缓存头），未命中即时下载入库，失败/停用 302 回源降级
- `GET /api/admin/media/stats` — 缓存统计（按 URL 计数 / 内容去重文件数 / 落盘字节 / 失败数，admin）
- `GET /api/admin/media/heatmap?days=` — 逐日缓存覆盖聚合（热点图数据；现算不落表，admin）
- `GET /api/admin/media/days/{date}` — 当日逐篇明细 + 逐图链状态与失败原因（格子抽屉，admin）
- `POST /api/admin/media/articles/{id}/prefetch` — 单篇定点重抓（`force` 绕过失败退避冷却，admin）
- `POST /api/admin/media/backfill` — 全量回填后台 job；**前端入口已撤**，保留作脚本化应急通道 (admin)

**X 社交源**(admin-only,见 *社交媒体流*)
- `GET`/`POST /api/x-api/config` — 读/写 Bearer Token 与采集参数(token 只写不回显;`field_sources` 标出逐字段来源)
- `POST /api/x-api/config/test` — 连通性自测(最省钱探针,响应含本次 `estimated_cost_usd`/`deduplicated_today`)
- `GET /api/x-api/quota` — 本月 X API 资源计数与估算成本 + `by_source` 按源分解(按量付费守卫读数)

**互通波(v3.18):反馈 / 公告 / 远程同步**(设计 `docs/engage-sync-wave-plan.md`;读者端点走 `/api/reader/*` 前缀天然 reader 门控,管理端点走 `/api/admin/*` 前缀天然 admin-only——零中间件改动)
- `POST`/`GET /api/reader/feedback` + `DELETE /api/reader/feedback/{id}` — 读者提交(单日 10 条限额 429)/查看自己的反馈(含管理员回复)/撤回 open 态;`GET /api/admin/feedback?status=` + `POST /api/admin/feedback/{id}/status` — 管理员收件箱(items+counts)与状态流转/回复
- `GET/POST/PUT/DELETE /api/admin/announcements`(+`/{id}/toggle`,GET 含 dismiss_count 触达计数)— 公告管理;`GET /api/reader/announcements` + `POST /api/reader/announcements/{id}/dismiss` — 读者取未关闭的 active 公告 / 逐条一次性关闭(`announcement_dismissals` 复合主键落库,跨设备一致;删除公告级联清 dismissal)
- `POST /api/admin/remote-sync/test|start` + `GET /api/admin/remote-sync/status` — 远程内容同步(接收方拉取):`services/remote_sync.py` 登录远端(Set-Cookie 手工抽取显式回传,兼容远端 cookie_secure)→ 分页拉 `GET /api/archive/export/articles.jsonl` → 本地 `import_archive_sync_jsonl` 幂等导入,跑 `remote_archive_sync` 后台 job;成功后把最大 `fetched_date` 记入 KV `remote_sync:state`(按 base_url 分目标)支撑「增量自上次」;**手动同步凭据只进任务内存,job payload 与 KV 游标绝不含密码**;带过滤时不设进度 total(远端全库总量会误导进度条)
- `GET`/`POST /api/admin/remote-sync/schedule` — 定时同步(v3.19.2):无人值守到点自动增量拉取。**凭据存 KV `remote_sync:schedule`,只写不回显**(定时任务必须持久化凭据——有意的契约例外,沿 X API token 范式:GET 只给 `password_set`,POST 空密码=保留已存;日志/审计摘要绝不碰密码);cron 经 `CronTrigger.from_crontab` 校验,保存即 `reload_remote_sync_schedule()` 热生效 APScheduler job `remote_sync`;`execute_remote_sync_job` 以该远端 KV 游标为起点增量、上轮 job 未完则跳过本轮(防重叠),经 `launch_remote_sync_job`(与手动 start 同构)提交,`created_by="system"` 与手动运行同列

**分享波(v3.25):把一篇内容发给同事**(两档并列,深浅分明;设计取舍见 `services/article_share.py` 模块注释)
- **站内深链**(零后端):`#/reader/a/{article_id}`,纯前端 URL、不落库、不外泄,收到的人登录后直达该篇。拼装与解析在 `frontend/src/utils/shareLink.js`;`App.jsx` 在 `commitNav` 初始播种 replace 掉 hash **之前**就地取走 id(晚一步就读不到),admin 带深链进来一律 `enterReader()`——压过 `default_surface` 与本 tab 会话的界面记忆,否则链接会开在看不见的隐藏阅读器里;ReaderTab 深链落地用 `deepLinkKeepRef` 通知作用域清场 effect **保留右栏**——深链要「切作用域+选中该篇」一次完成,而清场 effect(切源即 setActiveArticle(null))会把刚选中的文章抹掉,v3.25 首版即栽在这上
- **公开链接**(免登录只读单篇,**一篇一链**——同一用户对同一篇至多一条存活链接,再次生成先撤销旧的[rotate 语义,同 feed token],否则重复点生成会堆出一串各自有效的平行链接):`POST /api/reader/articles/{id}/share`(body `{expires_in_days: 7|30|null}`,返回明文令牌)/ `GET /api/reader/shares[?article_id=]` / `DELETE /api/reader/shares/{id}`(撤销,幂等,owner-scoped);访客侧 `GET /api/public/share/{token}` 走 `/api/public/*` 既有免登录豁免(零中间件改动),响应**刻意贫瘠**——只给这一篇的标题/来源/时间/正文,不带目录、检索、相邻篇目,令牌泄露的最坏后果钉死在「这一篇被看到」;正文图走 `GET /api/public/share/{token}/media?url=`(同套护栏 + **url 必须属于该文章图链集合** extract_image_urls——不是开放图片代理;`/api/media/proxy` 对访客 401、防盗链 CDN 直连又 403,没有它分享出去的中文源文章整页裂图;前端经 ReaderMarkdown 的 `resolveImageSrc` prop 注入,不计 view_count)
- **令牌明文入库**是与 `dsub_`/`dfeed_` 的**有意口径差异**:那两者存 hash、明文只回显一次(能拉走整个订阅库);分享令牌权限极小(单篇/只读/可过期/可撤销)而天然是「生成一次、反复复制」的东西,只存 hash 会让读者关掉浮层后再也复制不到、只能重新生成,链接越堆越多且旧的仍有效
- **护栏**:`dshr_` + 32 字节 urlsafe 不可枚举;隐藏源(source_visibility)不可分享且既有链接立即 404(与「读者面隐藏 = 内容交付全量排除」同口径);单人单日 30 条上限(429);**失败一律不区分原因**(不存在/已撤销/已过期/源已隐藏/文章已删 → 同一个 404,区分等于把令牌是否有效告诉猜令牌的人)
- **总闸** `GET`/`POST /api/admin/public-share`(KV `public_share_enabled`,默认开,读数含 live_count/total_count):**关闭立即让已发出的链接全部 404**——不必逐条撤销,且不销毁签发记录,重开即回归;开关在运维管理 → 内容(与媒体库、X 接入同类:对外的内容出口)
- 前端:阅读窗工具条 `Share2` 钮 → `ShareMenu.jsx` 浮层(站内链接=wash 可点行整行即复制;公开链接 hairline 之下退一档,两态:未生成=mini-seg 档位+唯一实心 CTA / 已生成=存活点+过期·触达 meta+复制/撤销 rowact 钮;换有效期=撤销后重生成);公开页 `SharedArticlePage.jsx` 在 **`main.jsx` 于 App 之外分流**——访客没有账号不能走登录门,且 App 的历史锚点会把令牌从 hash 里冲掉

**Admin Ops** (admin-only via `account_admin_required`, `/api/admin/*` prefix)
- `GET /api/admin/overview` — account/archive/AI stat board + recent logins
- `GET /api/admin/accounts?days=` — account list enriched with `subscription_count` + **窗口指标** (`reads`, `logins`, `ai_calls`/`ai_tokens` over the last `days`, `logged_in_window`) + 埋点 fields
- `GET /api/admin/accounts/{username}/activity?days=` — per-reader activity detail: windowed AI usage (`ai_usage.summarize_user`) + reads (`reader_activity.summarize_user_reads`) + logins (`accounts.summarize_user_logins`: count/by_day/recent) + `source_engagement` (per-source reads ∪ favorites, friendly-named) + `favorites_total` + account/subscription snapshot
- `GET /api/admin/ai-usage?days=` — AI usage dashboard (calls + tokens by 用途/用户/日期)
- `GET /api/admin/audit-log?days=&limit=` — 管理操作审计(v3.19):中间件落行的管理写操作,按时间倒序(见 *Admin audit log*)
- `GET`/`POST /api/admin/ai-beta/global` — read/set the AI Beta global master switch
- `GET /api/admin/credentials` — 设置柜凭据区只读补充:部署级 env-only 机密的存在性(`github_token_set`,只回布尔绝不回值;LLM/X 状态走各自 config 端点)
- `GET /api/admin/source-visibility` + `POST /api/admin/source-visibility/{source_id}` — 读者面源可见性(管理面隐藏节点):隐藏名单存 KV `reader_hidden_source_ids`(`services/source_visibility.py`,无新表),按 `source_id` 生效。语义=「读者面临时下架」(观察期故障/文章缺陷即时止损,不改代码)。**内容交付全量排除**:文章列表与单条详情(非 admin 会话)、收藏列表、聚合 feed(dfeed/dsub)、MCP/问答检索范围——范围收窄的单一咽喉是 `feed_service.resolve_subscribed_source_ids` 默认减隐藏(`include_hidden=True` 取原始并集,供目录/subscribe 响应/播种幂等判定)。**目录呈现分订阅态**(2026-07-27 拍板,替代初版「已订阅者也整体消失」——静默消失像数据丢失):未订阅者完全不可见(发现页不出现,新订阅 404);已订阅者源栏保留条目、`/api/reader/sources` 标 `hidden:true` → 前端灰显+「暂不可用」细签,点击短路成空态(不发列表请求,admin/读者同观感),读者可退订也可留着等恢复;既有订阅与归档原样保留、恢复即回归。admin 会话内容不受影响(知识台账/管理员令牌仍见全档)。开关在节点管理检视器(「在读者面隐藏」+ 节点卡「已隐藏」徽记),操作入审计
- `GET`/`POST /api/llm/config` + `POST /api/llm/config/test` — global model config (shared by Daily Brief + reader AI; edited from 运维管理)
- `POST /api/auth/preferences` — 任意登录账户写自己的偏好(目前仅 `default_surface`,管理员登录落地界面)

### Tests

Unit tests live directly under `tests/` as `test_*.py`. Fetcher/extraction: `rss_fetcher`, `webpage_fetcher`, `github_release_fetcher`, `repository_model_fetcher`, `ithome_web_fetcher`, `article_extractor`, `fetcher_curation`, `fetch_concurrency`, `fetch_failures`, `progress`. Platform/role: `mcp`, `runtime_role`, `subscriptions` (`runtime_role`/`subscriptions` exercise the dual-role gating, subscriptions, aggregated feed, and the token-scoped FTS search endpoint). Daily-brief/LLM/sync: `daily_brief`, `llm_client`, `ensure_daily_collection_job`, `archive_sync`, `shendeng_export` (`daily_brief` also covers AI-usage attribution to the triggering admin vs `system`). Accounts/admin-ops: `accounts`, `admin_ops` (`admin_ops` covers login/AI 埋点, AI Beta global master switch + 熔断, `/api/admin/*` aggregation & admin-gating, and AI token metering — `record_usage` accumulation, `summarize`, recorder gating / ping-excluded; plus windowed per-user OPS — `usage_by_user`/`summarize_user` aggregation and the `/api/admin/accounts?days=` window fields + `/api/admin/accounts/{u}/activity` detail/404/admin-gating; and reading metering — `reader_activity` `record_read`/`reads_by_user`/`summarize_user_reads` aggregation + the `POST /api/reader/articles/{id}/read` endpoint records/ignores-missing; and login metering — `accounts` `touch_login` event-write + `logins_by_user`/`summarize_user_logins` aggregation, plus the activity endpoint's `logins`/`source_engagement`/`favorites_total`). Data layer (阶段2): `migrations` (Alembic baseline: fresh-DB `upgrade head` has zero drift from `SQLModel.metadata`, legacy-DB baseline adoption via `ensure_migrated`, index-reconcile migration restores dropped declared indexes, the 实体简化 retirement migration — group-inline merge semantics / standalone-group & legacy-task conversion / table+column drops, the historical `index_status` backfill replay guard, and the v3.31 vector-column drop migration `f2c9d4e07a11`). Jobs (阶段3): `jobs` (persistent `JobRecord` state machine — launch/run-to-terminal, success-result/failure-error persistence, progress flush, `get_job`/`list_jobs` read-back incl. survives a new engine = restart); `collection_run_job` (collection-job run submits a background job). 社交(X 社交波): `x_api_endpoints`(配置端点脱敏/门控/探针计费)、`x_timeline`(字段映射与 quoted/reposted 扁平化、转推作者契约、since_id 增量、配额守卫计费与停抓、config 路径 handle 驱动;全程 httpx.MockTransport,不打真网)。媒体库: `media_store` (图链提取/下载缓存与内容去重/失败负缓存冷却与 `force` 绕过/SSRF 判定含 fake-ip 豁免/Referer 推导与发送/按文章预取/三态盘点 + `/api/media/proxy` 命中-302 降级-鉴权 + `/api/admin/media/*` admin 门控、热点图聚合、单日明细、单篇定点重抓与回填 job 端到端). 互通波(v3.18): `feedback`(提交/限额 429/撤回 open 态与 409/越权 404/管理员收件箱 counts 与状态流转/读者可见回复/门控)、`announcements`(CRUD 校验/toggle/读者可见性与升序/dismiss 幂等与跨下线保留/删除级联清 dismissal/门控)、`remote_sync`(httpx.MockTransport 假远端:探针版本与契约、错误凭据与非 admin 拒绝、分页拉取+真实导入端到端、增量游标与空跑保游标、checksum 坏行计数、source_ids 透传、端点门控、job payload 无密码;Secure cookie 显式头回传为机制性验证——假远端 Set-Cookie 带 Secure 且 base_url 为 http,cookiejar 必不发;v3.19.2 定时同步:schedule 往返密码不回显+空密码保留、非法 cron/缺凭据 400、execute_remote_sync_job 取游标+system 归属+运行中跳过、reload 注册/移除)。凭据整合波(v3.26): `credentials`(统一保管层契约——KV>基线解析序与坏值回落、field_sources 四来源标注、save_updates 只写不回显写入半边、mask_tail/sanitize 脱敏形状、blob 形态 secret 合并、注册表 KV key 与历史存量一致的零迁移守卫);v3.27 凭据整合台在 `admin_ops` 补 `/api/admin/credentials` 存在性端点(布尔不泄值/admin 门控)。分享波(v3.25): `article_share`(签发/明文令牌前缀/永久档无过期、公开端点免登录取数与响应贫瘠性、访问计量累加、撤销与过期各自 404、未知与非 dshr_ 前缀令牌 404、归属隔离[他人看不到也撤不掉]、隐藏源不可分享且既有链接立即失效、总闸关闭杀链接且重开即回归、总闸 admin-only、单日限额 429、媒体端点仅放行文章自身图链且撤销同步失效)。检索扶正波(v3.30): `reader_search`(规划 JSON 解析清洗/失败降级、FTS 并集×订阅域×日期窗、选篇短路/索引校验/诚实空选/失败回退、编排降级链与日报即索引作用域)、`reader_context` 改版为 search_fetch 委托、`reader_ai` 订阅域用例改检索管线委托+窗口降级、`mcp` 检索工具换芯用例(FTS 命中/订阅域/日期过滤/上下文块格式)。阅读面 AI 打磨波(v3.32): `reader_context` 改范围四档(article 编号上下文+sources 同源、articles 去重/跳缺失/上限截断/全缺 404、subscription/all 同委托 search_fetch)、`reader_ai` 增 articles 端到端编号用例 + all 检索域用例(未订阅源可达/隐藏源排除/范围提示语)+ ask 进度生命周期(完成即清/在途可读/非法 ask_id 清洗)。 Each file self-bootstraps `sys.path` to `src/` so imports resolve without an editable install. **`tests/conftest.py` 会话级数据库沙箱(v3.32.1)**:收集期把 `DORAMI_CONFIG_FILE` 指向临时 ini,整个测试会话的 settings 库即一次性沙箱——api.app 在 import 期就按 settings 建 db_sink,忘记 monkeypatch 的测试曾把真实 `data/cms_data.db` 的账户行整个重置;新增 TestClient 类测试仍应显式建 tmp sink,沙箱只是最后防线。 Run with pytest:

```bash
.venv/bin/python -m pytest tests/test_rss_fetcher.py
.venv/bin/python -m pytest tests/                       # whole suite
```

`pyproject.toml` does not configure pytest, so discovery uses pytest defaults; pass an explicit path/`-k` filter when targeting a subset.

### Configuration (`config/backend.ini`)

Loaded by `src/config.py` (`load_config()`) into the `settings` singleton (read live in `app.py`; tests monkeypatch it). The config file path is `DORAMI_CONFIG_FILE` if set (production uses `config/production.ini`), else `config/backend.ini`.

- `[runtime] role` — keep the default `all` (single-node all-in-one) unless you run a *split deployment*: `collector` (external collection/archive) | `reader` (intranet distribution). Inert for `all`; see *Access control — login account role*. Overridable via `DORAMI_RUNTIME_ROLE`.
- `[auth]` — no longer seeds accounts (v3.19: `admin_users`/`user_users` removed; on first boot with an empty `users` table the system auto-creates root admin `admin`/`admin` — see *Accounts are database-managed*). `admin` accounts are collector+reader superusers; `user` accounts are reader-only. `[auth] secret` salts the session and subscription/feed token HMACs (keep it stable — changing it invalidates issued sessions/tokens). When unset it falls back to a **non-password-derived** local key (`database_url` + fixed salt — no longer mixes in the seed passwords). **Startup security validation** (`api/security_checks.py`, run in `lifespan`) grades config by posture: in a production posture (`[auth] cookie_secure = true`, i.e. HTTPS) it **refuses to boot** on critical misconfig — an unset/placeholder `secret`, or `[cors] allow_origins=*` together with `allow_credentials=true`; in dev posture (`cookie_secure=false`) the same issues are logged as warnings only. It also warns when `[network] disable_ca_bundle=true` in production.
- `[llm] base_url` / `api_key` / `model` (+ `timeout_seconds`, `temperature`, `max_tokens`, `map_concurrency`) — OpenAI-compatible LLM for the Daily Brief. Empty by default (Daily Brief is inert until configured). Overridable via `DORAMI_LLM_BASE_URL` / `DORAMI_LLM_API_KEY` / `DORAMI_LLM_MODEL` and at runtime via admin settings (KV). See *LLM client*.
- `[media] enabled` — `true` (default) | `false`. 媒体库（图床）总开关；关闭时代理 302 回源、抓取后不预取，退回外链直连。另有 `media_dir`（默认 `data/media`）、`max_file_mb`（20）、`timeout_seconds`（20）、`prefetch_concurrency`（4）。Overridable via `DORAMI_MEDIA_ENABLED`. See *媒体库*.
- `[x_api] bearer_token` — X API v2 应用级 Bearer(**优先用环境变量 `DORAMI_X_BEARER_TOKEN`,真 token 绝不入库入仓**)。另有 `base_url`(默认 `https://api.x.com/2`)、`timeout_seconds`(30)、`max_results`(单次拉取上限 10)、`monthly_budget_usd`(月度预算硬上限,默认 5.0 —— 按返回资源计费,到顶即停抓)。留空时社交源不可用,其余功能不受影响。See *社交媒体流*.

### Environment Variables

| Variable | Purpose |
|---|---|
| `HF_ENDPOINT` | HuggingFace mirror(config [network] hf_endpoint 兜底设置;HF 相关抓取器受益) |
| `DORAMI_RUNTIME_ROLE` | Override `[runtime] role` (`all`/`collector`/`reader`) |
| `DORAMI_CONFIG_FILE` | Path to the ini config file to load (defaults to `config/backend.ini`); production deploy sets it to `config/production.ini` |
| `DORAMI_LLM_BASE_URL` / `DORAMI_LLM_API_KEY` / `DORAMI_LLM_MODEL` | Override the `[llm]` OpenAI-compatible client config (used by the Daily Brief) |
| `DORAMI_MEDIA_ENABLED` | Override `[media] enabled`（`1`/`true`/`yes`/`on` 开启媒体库图床） |
| `DORAMI_X_BEARER_TOKEN` | X API v2 应用级 Bearer Token(社交源采集;不入库、不进 Git、不打日志) |
| `GITHUB_TOKEN` / `GH_TOKEN` | Optional GitHub API token for the GitHub repo fetchers; raises the rate limit (60→5000/hr) for repo listing + README backfill |
