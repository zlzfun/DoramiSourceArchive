严重度(P2)| src/api/routers/reader.py:756 | 固定 `LIMIT 2000` 使“全部标读”静默变成“最新 2000 篇标读” | 当范围内有 2001 篇未读命中时，接口仍返回成功且前端提示“已全部标为已读”，但刷新后至少一篇仍未读；响应也没有 `has_more` 或剩余数供前端续写 | 后端按固定快照分批写完，或返回明确的 `has_more` 并由前端循环调用；完成前不要显示“全部完成”或乐观清点全部已加载条目。

严重度(P2)| src/services/retention.py:180 | 留存清理与未读时效使用了不同的 30 天边界，会让已读文章短暂复活为未读 | 未读判断使用精确的 `now - 30 days`，清理却复用按日历日计算的 `_cutoff(30)`，即 `today - 29 days`。例如 9 月 10 日清理会删除 8 月 11 日 12:00 的已读行，但该文章在当天 12:00 前仍未满 30×24 小时；删除后无水位判断会再次把它标为未读。实测该条件会删除状态并使 `unread_ids_among(..., include_uncursored=True)` 返回该文章 | 清理直接复用 `uncursored_unread_cutoff(now)` 的精确时间戳，文章时间和 `read_at` 使用同一截点，并补充临界时间测试。

严重度(P2)| src/api/routers/reader.py:735 | 管理员兴趣列表的可见范围与“全部标读”写入范围不一致 | `/api/articles` 在管理员会话下跳过隐藏源和私有自定源过滤，但新端点无条件排除隐藏源，并只允许管理员自己订阅的用户源。管理员进入受支持的阅读器界面时，可以看到这些兴趣命中且标为未读；点击全部标读返回 `marked=0`，刷新后仍未读 | 将列表与标读的可见性条件抽成共享函数，并按同一个会话角色/阅读表面应用；若管理员阅读器也应受读者可见性约束，则列表端也必须采用同一约束。