# Dorami 容器内 Nginx 站点配置(与 deploy.sh 生成的裸机版同构)。
# 容器只做 HTTP:80,TLS 终止交给宿主/外层(见 docs/deploy-docker.md 的 HTTPS 节)。
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 100m;

    # 安全响应头与裸机版对齐
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    # ASR provider fetch authorization lives in the query string.  This exact
    # route must never write request/error lines containing the signed target;
    # Uvicorn keeps a matching application-side redaction guard.
    location = /api/public/podcast-asr/source-audio {
        access_log off;
        error_log /dev/null crit;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_buffering off;
        proxy_pass http://backend:8088;
    }

    location /api/ {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_pass http://backend:8088;
    }

    location /mcp {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_pass http://backend:8088;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
